# PLAN: Wave 5 - Repositories Index

---
wave: 5
depends_on:
  - 02-PLAN-05
  - 02-PLAN-06
  - 02-PLAN-07
files_modified:
  - src/repositories/index.ts
autonomous: true
requirements:
  - DATA-04
  - DATA-05
  - DATA-06
---

## Goal

创建 Repositories 层统一导出文件，简化导入路径。

## Context

- 已创建 `settings.repository.ts`、`download.repository.ts`、`wallpaper.repository.ts`
- 需要统一导出入口便于使用

## Tasks

### Task 5.1: 创建 Repositories 统一导出

<read_first>
- src/repositories/settings.repository.ts (刚创建的文件)
- src/repositories/download.repository.ts (刚创建的文件)
- src/repositories/wallpaper.repository.ts (刚创建的文件)
</read_first>

<acceptance_criteria>
- 文件存在：src/repositories/index.ts
- 导出 settingsRepository
- 导出 downloadRepository
- 导出 wallpaperRepository
- TypeScript 编译无错误
</acceptance_criteria>

<action>
创建文件 `src/repositories/index.ts`：

```typescript
/**
 * Repositories 层统一导出
 */

// 设置仓储
export { settingsRepository } from './settings.repository'

// 下载仓储
export { downloadRepository } from './download.repository'

// 壁纸仓储
export { wallpaperRepository } from './wallpaper.repository'
```
</action>

---

## Verification

### 编译验证
```bash
npx tsc --noEmit
```
预期结果：无错误输出

### 导入测试
```bash
# 检查导出是否正确
grep -E "export.*settingsRepository|export.*downloadRepository|export.*wallpaperRepository" src/repositories/index.ts
```

---

## must_haves

- [ ] 文件 `src/repositories/index.ts` 存在
- [ ] 导出 `settingsRepository`
- [ ] 导出 `downloadRepository`
- [ ] 导出 `wallpaperRepository`
- [ ] TypeScript 编译通过

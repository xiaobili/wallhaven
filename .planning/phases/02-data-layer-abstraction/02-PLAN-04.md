# PLAN: Wave 3 - Clients Index

---
wave: 3
depends_on:
  - 02-PLAN-02
  - 02-PLAN-03
files_modified:
  - src/clients/index.ts
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
---

## Goal

创建 Clients 层统一导出文件，简化导入路径。

## Context

- 已创建 `constants.ts`、`electron.client.ts`、`api.client.ts`
- 需要统一导出入口便于使用

## Decisions Applied

- **D-03**: 单例模式导出

## Tasks

### Task 3.1: 创建 Clients 统一导出

<read_first>
- src/clients/constants.ts (刚创建的文件)
- src/clients/electron.client.ts (刚创建的文件)
- src/clients/api.client.ts (刚创建的文件)
</read_first>

<acceptance_criteria>
- 文件存在：src/clients/index.ts
- 导出 STORAGE_KEYS 常量
- 导出 StorageKey 类型
- 导出 electronClient 单例
- 导出 apiClient 单例
- TypeScript 编译无错误
</acceptance_criteria>

<action>
创建文件 `src/clients/index.ts`：

```typescript
/**
 * Clients 层统一导出
 */

// 存储键常量
export { STORAGE_KEYS, type StorageKey } from './constants'

// Electron 客户端
export { electronClient } from './electron.client'

// API 客户端
export { apiClient } from './api.client'
```
</action>

---

## Verification

### 编译验证
```bash
npx tsc --noEmit
```
预期结果：无错误输出

### 导入测试
```bash
# 检查导出是否正确
grep -E "export.*STORAGE_KEYS|export.*electronClient|export.*apiClient" src/clients/index.ts
```

---

## must_haves

- [ ] 文件 `src/clients/index.ts` 存在
- [ ] 导出 `STORAGE_KEYS` 和 `StorageKey`
- [ ] 导出 `electronClient`
- [ ] 导出 `apiClient`
- [ ] TypeScript 编译通过

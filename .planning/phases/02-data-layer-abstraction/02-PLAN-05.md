# PLAN: Wave 4 - SettingsRepository

---
wave: 4
depends_on:
  - 02-PLAN-04
files_modified:
  - src/repositories/settings.repository.ts
autonomous: true
requirements:
  - DATA-04
---

## Goal

创建 SettingsRepository，抽象应用设置数据访问。

## Context

- 存储键：`appSettings`（已在 STORAGE_KEYS 中定义）
- 数据类型：`AppSettings`（在 `src/types/index.ts` 中定义）
- 现有实现：`src/stores/modules/wallpaper/settings-storage.ts`
- 阶段 2 只新建，不迁移

## Decisions Applied

- **D-04**: Repository 方法返回 `Promise<IpcResponse<T>>`
- **D-05**: 方法命名采用 `get/set/delete` 风格
- **D-06**: Repository 不缓存数据
- **D-10**: Repository 只做 CRUD

## Tasks

### Task 4.1: 创建 SettingsRepository

<read_first>
- src/clients/electron.client.ts (storeGet/storeSet 方法签名)
- src/clients/constants.ts (STORAGE_KEYS.APP_SETTINGS)
- src/types/index.ts (AppSettings 类型定义，第 218-228 行)
- src/shared/types/ipc.ts (IpcResponse 类型)
- src/stores/modules/wallpaper/settings-storage.ts (现有实现参考)
</read_first>

<acceptance_criteria>
- 文件存在：src/repositories/settings.repository.ts
- 导入 IpcResponse 类型
- 导入 AppSettings 类型
- 导入 electronClient
- 导入 STORAGE_KEYS
- 导出 settingsRepository 对象
- 包含 get() 方法，返回 Promise<IpcResponse<AppSettings | null>>
- 包含 set(settings) 方法，返回 Promise<IpcResponse<void>>
- 包含 delete() 方法，返回 Promise<IpcResponse<void>>
- TypeScript 编译无错误
</acceptance_criteria>

<action>
创建目录和文件 `src/repositories/settings.repository.ts`：

```typescript
/**
 * 设置仓储
 * 管理应用设置的持久化存储
 */

import type { IpcResponse } from '@/shared/types/ipc'
import type { AppSettings } from '@/types'
import { electronClient, STORAGE_KEYS } from '@/clients'

/**
 * 设置仓储
 */
export const settingsRepository = {
  /**
   * 获取应用设置
   * @returns 返回设置数据，如果未设置则返回 null
   */
  async get(): Promise<IpcResponse<AppSettings | null>> {
    return electronClient.storeGet<AppSettings>(STORAGE_KEYS.APP_SETTINGS)
  },

  /**
   * 保存应用设置
   * @param settings - 应用设置对象
   */
  async set(settings: AppSettings): Promise<IpcResponse<void>> {
    return electronClient.storeSet(STORAGE_KEYS.APP_SETTINGS, settings)
  },

  /**
   * 删除应用设置
   */
  async delete(): Promise<IpcResponse<void>> {
    return electronClient.storeDelete(STORAGE_KEYS.APP_SETTINGS)
  },
}
```
</action>

---

## Verification

### 编译验证
```bash
npx tsc --noEmit
```
预期结果：无错误输出

### 文件验证
```bash
# 检查文件存在
ls -la src/repositories/settings.repository.ts

# 检查关键方法存在
grep -E "async get\(\)|async set\(settings|async delete\(\)" src/repositories/settings.repository.ts

# 检查导出
grep "export const settingsRepository" src/repositories/settings.repository.ts
```

---

## must_haves

- [ ] 文件 `src/repositories/settings.repository.ts` 存在
- [ ] 包含 `get()` 方法
- [ ] 包含 `set(settings)` 方法
- [ ] 包含 `delete()` 方法
- [ ] 所有方法返回 `Promise<IpcResponse<T>>`
- [ ] TypeScript 编译通过

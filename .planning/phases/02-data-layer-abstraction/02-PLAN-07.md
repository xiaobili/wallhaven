# PLAN: Wave 4 - WallpaperRepository

---
wave: 4
depends_on:
  - 02-PLAN-04
files_modified:
  - src/repositories/wallpaper.repository.ts
autonomous: true
requirements:
  - DATA-06
---

## Goal

创建 WallpaperRepository，抽象壁纸查询参数数据访问。

## Context

- 存储键：`wallpaperQueryParams`
- 数据类型：`CustomParams`（在 `src/types/index.ts` 中定义）
- 现有实现：`src/stores/modules/wallpaper/storage.ts`
- 壁纸搜索 API 调用由阶段 3 Service 层处理

## Decisions Applied

- **D-04**: Repository 方法返回 `Promise<IpcResponse<T>>`
- **D-05**: 方法命名采用 `get/set/delete` 风格
- **D-06**: Repository 不缓存数据
- **D-10**: Repository 只做 CRUD
- **D-12**: API Key 由 Service 层处理

## Tasks

### Task 4.1: 创建 WallpaperRepository

<read_first>
- src/clients/electron.client.ts (storeGet/storeSet 方法签名)
- src/clients/constants.ts (STORAGE_KEYS.WALLPAPER_QUERY_PARAMS)
- src/types/index.ts (CustomParams 类型定义，第 81-97 行)
- src/shared/types/ipc.ts (IpcResponse 类型)
- src/stores/modules/wallpaper/storage.ts (现有实现参考)
</read_first>

<acceptance_criteria>
- 文件存在：src/repositories/wallpaper.repository.ts
- 导入 IpcResponse 类型
- 导入 CustomParams 类型
- 导入 electronClient
- 导入 STORAGE_KEYS
- 导出 wallpaperRepository 对象
- 包含 getQueryParams() 方法，返回 Promise<IpcResponse<CustomParams | null>>
- 包含 setQueryParams(params) 方法，返回 Promise<IpcResponse<void>>
- 包含 deleteQueryParams() 方法，返回 Promise<IpcResponse<void>>
- TypeScript 编译无错误
</acceptance_criteria>

<action>
创建文件 `src/repositories/wallpaper.repository.ts`：

```typescript
/**
 * 壁纸仓储
 * 管理壁纸查询参数的持久化存储
 */

import type { IpcResponse } from '@/shared/types/ipc'
import type { CustomParams } from '@/types'
import { electronClient, STORAGE_KEYS } from '@/clients'

/**
 * 壁纸仓储
 */
export const wallpaperRepository = {
  /**
   * 获取保存的查询参数
   * @returns 返回查询参数，如果未设置则返回 null
   */
  async getQueryParams(): Promise<IpcResponse<CustomParams | null>> {
    return electronClient.storeGet<CustomParams>(STORAGE_KEYS.WALLPAPER_QUERY_PARAMS)
  },

  /**
   * 保存查询参数
   * @param params - 查询参数对象
   */
  async setQueryParams(params: CustomParams): Promise<IpcResponse<void>> {
    return electronClient.storeSet(STORAGE_KEYS.WALLPAPER_QUERY_PARAMS, params)
  },

  /**
   * 删除查询参数
   */
  async deleteQueryParams(): Promise<IpcResponse<void>> {
    return electronClient.storeDelete(STORAGE_KEYS.WALLPAPER_QUERY_PARAMS)
  },
}
```
</action>

---

## Verification

### 编译验证
```bash
npx tsc --noEmit
```
预期结果：无错误输出

### 文件验证
```bash
# 检查文件存在
ls -la src/repositories/wallpaper.repository.ts

# 检查关键方法存在
grep -E "async getQueryParams|async setQueryParams|async deleteQueryParams" src/repositories/wallpaper.repository.ts

# 检查导出
grep "export const wallpaperRepository" src/repositories/wallpaper.repository.ts
```

---

## must_haves

- [ ] 文件 `src/repositories/wallpaper.repository.ts` 存在
- [ ] 包含 `getQueryParams()` 方法
- [ ] 包含 `setQueryParams(params)` 方法
- [ ] 包含 `deleteQueryParams()` 方法
- [ ] 所有方法返回 `Promise<IpcResponse<T>>`
- [ ] TypeScript 编译通过

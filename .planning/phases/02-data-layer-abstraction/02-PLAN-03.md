# PLAN: Wave 2 - ApiClient

---
wave: 2
depends_on:
  - 02-PLAN-01
files_modified:
  - src/clients/api.client.ts
autonomous: true
requirements:
  - DATA-02
---

## Goal

创建 ApiClient，封装 HTTP 请求逻辑，支持开发/生产两种模式，统一返回 `IpcResponse<T>` 格式。

## Context

- 现有 `src/services/wallpaperApi.ts` 混杂了 HTTP 请求、缓存、业务逻辑
- 开发环境：直接 axios 请求（Vite 代理）
- 生产环境：通过 Electron IPC 代理
- 阶段 2 只新建，不修改现有文件

## Decisions Applied

- **D-02**: 所有方法返回 `IpcResponse<T>` 格式
- **D-03**: 单例模式导出 `apiClient`
- **D-12**: API Key 由 Service 层处理（作为参数传入）

## Tasks

### Task 2.1: 创建 ApiClient

<read_first>
- src/services/wallpaperApi.ts (现有 HTTP 请求模式，第 81-119 行)
- src/shared/types/ipc.ts (IpcResponse 类型定义)
- src/errors/types.ts (ErrorCodes 常量)
</read_first>

<acceptance_criteria>
- 文件存在：src/clients/api.client.ts
- 导入 axios 库
- 导入 IpcResponse 和 IpcErrorInfo 类型
- 定义 ApiClientImpl 类
- 包含 private axiosInstance
- 包含 private isProduction() 方法
- 包含 private getErrorCode() 方法
- 导出 apiClient 单例实例
- TypeScript 编译无错误
</acceptance_criteria>

<action>
创建文件 `src/clients/api.client.ts`：

```typescript
/**
 * API 客户端
 * 封装 HTTP 请求逻辑，支持开发/生产两种模式
 */

import axios, { type AxiosRequestConfig, type AxiosError } from 'axios'
import type { IpcResponse } from '@/shared/types/ipc'
import { ErrorCodes } from '@/errors'

/**
 * API 客户端实现类
 */
class ApiClientImpl {
  private axiosInstance = axios.create({
    baseURL: '/api',
    timeout: 15000,
    headers: { 'Content-Type': 'application/json' },
  })

  /**
   * 检查是否为生产环境
   */
  private isProduction(): boolean {
    return typeof window !== 'undefined' && 
           !!window.electronAPI && 
           import.meta.env.PROD
  }

  /**
   * 根据错误类型获取错误码
   */
  private getErrorCode(error: unknown): string {
    if (!axios.isAxiosError(error)) {
      return ErrorCodes.NETWORK_ERROR
    }

    const axiosError = error as AxiosError
    
    if (axiosError.code === 'ECONNABORTED') {
      return ErrorCodes.NETWORK_TIMEOUT
    }
    
    if (axiosError.response) {
      const status = axiosError.response.status
      if (status === 401) return ErrorCodes.NETWORK_UNAUTHORIZED
      if (status === 403) return ErrorCodes.NETWORK_FORBIDDEN
      if (status === 404) return ErrorCodes.NETWORK_NOT_FOUND
      if (status >= 500) return ErrorCodes.NETWORK_SERVER_ERROR
    }
    
    return ErrorCodes.NETWORK_ERROR
  }

  /**
   * 获取错误消息
   */
  private getErrorMessage(error: unknown): string {
    if (axios.isAxiosError(error)) {
      return error.message || 'Network error'
    }
    return String(error)
  }

  /**
   * GET 请求
   */
  async get<T>(
    url: string,
    params?: Record<string, unknown>,
    apiKey?: string
  ): Promise<IpcResponse<T>> {
    // 实现在下一个任务
    return { success: false, error: { code: 'NOT_IMPLEMENTED', message: 'Not implemented' } }
  }

  /**
   * POST 请求
   */
  async post<T>(
    url: string,
    data?: unknown,
    apiKey?: string
  ): Promise<IpcResponse<T>> {
    // 实现在下一个任务
    return { success: false, error: { code: 'NOT_IMPLEMENTED', message: 'Not implemented' } }
  }
}

export const apiClient = new ApiClientImpl()
```
</action>

### Task 2.2: 实现 GET 和 POST 方法

<read_first>
- src/clients/api.client.ts (刚创建的文件)
- src/services/wallpaperApi.ts (现有请求模式)
</read_first>

<acceptance_criteria>
- 文件包含 get<T>(url, params?, apiKey?) 方法
- 文件包含 post<T>(url, data?, apiKey?) 方法
- 生产环境通过 window.electronAPI.wallhavenApiRequest 发送请求
- 开发环境直接使用 axios
- 所有方法返回 Promise<IpcResponse<T>>
- TypeScript 编译无错误
</acceptance_criteria>

<action>
替换 `src/clients/api.client.ts` 中的 get 和 post 方法：

```typescript
  /**
   * GET 请求
   */
  async get<T>(
    url: string,
    params?: Record<string, unknown>,
    apiKey?: string
  ): Promise<IpcResponse<T>> {
    try {
      // 生产环境：通过 Electron IPC 代理
      if (this.isProduction() && window.electronAPI) {
        const result = await window.electronAPI.wallhavenApiRequest({
          endpoint: url,
          params,
        })

        if (result.success && result.data !== null) {
          return { success: true, data: result.data as T }
        }
        return {
          success: false,
          error: {
            code: result.status === 401 ? ErrorCodes.NETWORK_UNAUTHORIZED : 'API_ERROR',
            message: result.error || 'API request failed',
          },
        }
      }

      // 开发环境：直接 axios 请求
      const config: AxiosRequestConfig = { params }
      if (apiKey) {
        config.headers = { 'X-API-Key': apiKey }
      }

      const response = await this.axiosInstance.get<T>(url, config)
      return { success: true, data: response.data }
    } catch (error) {
      return {
        success: false,
        error: {
          code: this.getErrorCode(error),
          message: this.getErrorMessage(error),
        },
      }
    }
  }

  /**
   * POST 请求
   */
  async post<T>(
    url: string,
    data?: unknown,
    apiKey?: string
  ): Promise<IpcResponse<T>> {
    try {
      // 生产环境：通过 Electron IPC 代理
      if (this.isProduction() && window.electronAPI) {
        const result = await window.electronAPI.wallhavenApiRequest({
          endpoint: url,
          params: { method: 'POST', body: data },
        })

        if (result.success && result.data !== null) {
          return { success: true, data: result.data as T }
        }
        return {
          success: false,
          error: {
            code: 'API_ERROR',
            message: result.error || 'API request failed',
          },
        }
      }

      // 开发环境：直接 axios 请求
      const config: AxiosRequestConfig = {}
      if (apiKey) {
        config.headers = { 'X-API-Key': apiKey }
      }

      const response = await this.axiosInstance.post<T>(url, data, config)
      return { success: true, data: response.data }
    } catch (error) {
      return {
        success: false,
        error: {
          code: this.getErrorCode(error),
          message: this.getErrorMessage(error),
        },
      }
    }
  }
```
</action>

---

## Verification

### 编译验证
```bash
npx tsc --noEmit
```
预期结果：无错误输出

### 文件验证
```bash
# 检查文件存在
ls -la src/clients/api.client.ts

# 检查关键方法存在
grep -E "async get<T>|async post<T>|private isProduction|private getErrorCode" src/clients/api.client.ts

# 检查单例导出
grep "export const apiClient" src/clients/api.client.ts
```

---

## must_haves

- [ ] 文件 `src/clients/api.client.ts` 存在
- [ ] 类 `ApiClientImpl` 包含 `get` 和 `post` 方法
- [ ] 所有方法返回 `Promise<IpcResponse<T>>`
- [ ] 支持开发/生产两种模式
- [ ] 导出 `apiClient` 单例实例
- [ ] TypeScript 编译通过

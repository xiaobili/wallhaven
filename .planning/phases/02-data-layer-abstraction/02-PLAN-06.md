# PLAN: Wave 4 - DownloadRepository

---
wave: 4
depends_on:
  - 02-PLAN-04
files_modified:
  - src/repositories/download.repository.ts
autonomous: true
requirements:
  - DATA-05
---

## Goal

创建 DownloadRepository，抽象下载记录数据访问。

## Context

- 存储键：`downloadFinishedList`
- 数据类型：`FinishedDownloadItem[]`（在 `src/types/index.ts` 中定义）
- 现有实现：`src/stores/modules/download/index.ts`
- 最大保存数量：50 条（与现有实现一致）

## Decisions Applied

- **D-04**: Repository 方法返回 `Promise<IpcResponse<T>>`
- **D-05**: 方法命名采用 `get/set/add/remove/clear` 风格
- **D-06**: Repository 不缓存数据
- **D-10**: Repository 只做 CRUD
- **D-11**: DownloadRepository 只存储下载记录，不处理下载执行

## Tasks

### Task 4.1: 创建 DownloadRepository

<read_first>
- src/clients/electron.client.ts (storeGet/storeSet 方法签名)
- src/clients/constants.ts (STORAGE_KEYS.DOWNLOAD_FINISHED_LIST)
- src/types/index.ts (FinishedDownloadItem 类型定义，第 203-206 行)
- src/shared/types/ipc.ts (IpcResponse 类型)
- src/stores/modules/download/index.ts (现有实现，第 284-300 行)
</read_first>

<acceptance_criteria>
- 文件存在：src/repositories/download.repository.ts
- 导入 IpcResponse 类型
- 导入 FinishedDownloadItem 类型
- 导入 electronClient
- 导入 STORAGE_KEYS
- 定义 MAX_FINISHED_ITEMS = 50 常量
- 导出 downloadRepository 对象
- 包含 get() 方法，返回 Promise<IpcResponse<FinishedDownloadItem[]>>
- 包含 set(items) 方法，返回 Promise<IpcResponse<void>>
- 包含 add(item) 方法，返回 Promise<IpcResponse<void>>
- 包含 remove(id) 方法，返回 Promise<IpcResponse<void>>
- 包含 clear() 方法，返回 Promise<IpcResponse<void>>
- add 方法限制列表最大长度为 50
- TypeScript 编译无错误
</acceptance_criteria>

<action>
创建文件 `src/repositories/download.repository.ts`：

```typescript
/**
 * 下载仓储
 * 管理已完成下载记录的持久化存储
 */

import type { IpcResponse } from '@/shared/types/ipc'
import type { FinishedDownloadItem } from '@/types'
import { electronClient, STORAGE_KEYS } from '@/clients'

/** 最大保存已完成下载记录数量 */
const MAX_FINISHED_ITEMS = 50

/**
 * 下载仓储
 */
export const downloadRepository = {
  /**
   * 获取已完成下载列表
   * @returns 返回下载记录列表，如果未设置则返回空数组
   */
  async get(): Promise<IpcResponse<FinishedDownloadItem[]>> {
    const result = await electronClient.storeGet<FinishedDownloadItem[]>(
      STORAGE_KEYS.DOWNLOAD_FINISHED_LIST
    )

    if (result.success) {
      // 返回数据或空数组
      return { success: true, data: result.data || [] }
    }

    // 失败时返回空数组作为默认值
    return { success: false, data: [], error: result.error }
  },

  /**
   * 保存已完成下载列表
   * @param items - 下载记录列表
   */
  async set(items: FinishedDownloadItem[]): Promise<IpcResponse<void>> {
    // 限制列表长度
    const limitedItems = items.slice(0, MAX_FINISHED_ITEMS)
    return electronClient.storeSet(STORAGE_KEYS.DOWNLOAD_FINISHED_LIST, limitedItems)
  },

  /**
   * 添加已完成下载项（添加到列表头部）
   * @param item - 下载记录项
   */
  async add(item: FinishedDownloadItem): Promise<IpcResponse<void>> {
    const result = await this.get()
    if (!result.success) {
      return { success: false, error: result.error }
    }

    // 添加到头部，并限制长度
    const items = [item, ...result.data].slice(0, MAX_FINISHED_ITEMS)
    return this.set(items)
  },

  /**
   * 移除指定下载项
   * @param id - 下载记录 ID
   */
  async remove(id: string): Promise<IpcResponse<void>> {
    const result = await this.get()
    if (!result.success) {
      return { success: false, error: result.error }
    }

    const items = result.data.filter((item) => item.id !== id)
    return this.set(items)
  },

  /**
   * 清空已完成下载列表
   */
  async clear(): Promise<IpcResponse<void>> {
    return electronClient.storeDelete(STORAGE_KEYS.DOWNLOAD_FINISHED_LIST)
  },
}
```
</action>

---

## Verification

### 编译验证
```bash
npx tsc --noEmit
```
预期结果：无错误输出

### 文件验证
```bash
# 检查文件存在
ls -la src/repositories/download.repository.ts

# 检查关键方法存在
grep -E "async get\(\)|async set\(items|async add\(item|async remove\(id|async clear\(\)" src/repositories/download.repository.ts

# 检查最大数量限制
grep "MAX_FINISHED_ITEMS = 50" src/repositories/download.repository.ts

# 检查导出
grep "export const downloadRepository" src/repositories/download.repository.ts
```

---

## must_haves

- [ ] 文件 `src/repositories/download.repository.ts` 存在
- [ ] 包含 `get()` 方法
- [ ] 包含 `set(items)` 方法
- [ ] 包含 `add(item)` 方法
- [ ] 包含 `remove(id)` 方法
- [ ] 包含 `clear()` 方法
- [ ] 所有方法返回 `Promise<IpcResponse<T>>`
- [ ] 列表最大长度限制为 50
- [ ] TypeScript 编译通过

---
phase: 35-retry-status-ui
plan: 03
type: execute
wave: 3
depends_on:
  - 35-02
files_modified:
  - src/views/DownloadWallpaper.vue
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03

must_haves:
  truths:
    - "User sees '重试中 (第X次/共3次)' for retrying downloads (UI-01)"
    - "User sees live countdown '下次重试: Xs' for retrying downloads (UI-02)"
    - "When countdown reaches 0, displays '即将重试...' (Specifics item 2)"
    - "User sees '下载失败 — 已重试 3 次' for exhausted retries with gray progress bar (UI-03, D-04)"
    - "Cancel button remains visible during retry (D-03)"
  artifacts:
    - path: "src/views/DownloadWallpaper.vue"
      provides: "Retrying state display + exhausted-failure display in download item template"
      contains: "重试中 (第"
  key_links:
    - from: "DownloadWallpaper.vue template"
      to: "useDownload composable"
      via: "getRetryRemaining function destructured from useDownload()"
      pattern: "getRetryRemaining"
    - from: "DownloadWallpaper.vue template"
      to: "helpers.ts"
      via: "formatCountdown import"
      pattern: "formatCountdown"
---

<objective>
Add retry state UI and exhausted-failure display to the download center template.

**Purpose:** Two new `v-show` blocks in the download item template: one for `'retrying'` state showing retry count and countdown, another for exhausted failures showing the failure message with a gray progress bar. Follows the existing `v-show` pattern used by `'downloading'`, `'waiting'`, and `'paused'` states.

**Output:** DownloadWallpaper.vue with retry-aware template and associated CSS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
<interfaces>
Current DownloadWallpaper.vue template (lines 20-93, key sections):

```html
<div v-for="(item) in downloadList" :key="item.id"
     class="dowload-item"
     :class="item.state === 'paused' ? 'pause-item' : ''">
  <div class="img-view">...</div>
  <div class="down-content">
    <div class="img-info">
      <div class="rigth-top">
        <!-- pause/resume/cancel buttons -->
        <div v-show="item.state === 'downloading'" class="op-pause" @click="onPauseDownload(item.id)">
          <i class="fas fw fa-pause-circle" />
        </div>
        <div v-show="item.state === 'paused'" class="op-resume" @click="onResumeDownload(item.id)">
          <i class="fas fw fa-play-circle" />
        </div>
        <div class="op-cancel" @click="onCancelDownload(item.id)">
          <i class="fas fw fa-times-circle" />
        </div>
      </div>
      <div class="img-resolution">尺寸：{{ formatResolution(item.resolution) }}</div>
      <div class="file-size">图片大小：{{ formatFileSize(item.size) }}</div>
      <div class="rigth-bottoim">
        <div v-show="item.state === 'downloading'" class="dowload-speed">
          下载速度：{{ formatSpeed(item.speed) }}
        </div>
        <div v-show="item.state === 'waiting'" class="dowload-state">等待中</div>
        <div v-show="item.state === 'paused'" class="dowload-state">已暂停</div>
        <div class="dowloaded-size">已下载：{{ formatFileSize(item.offset) }}</div>
      </div>
      <div class="dowloaded-process">
        <div class="dowloaded-process-block" :style="{ width: item.progress + '%' }" />
      </div>
    </div>
  </div>
</div>
```

Current script imports (lines 166-169):
```typescript
import { onMounted, onUnmounted } from 'vue'
import { useDownload, useAlert } from '@/composables'
import { formatFileSize, formatResolution, formatSpeed, formatTime } from '@/utils/helpers'
import Alert from '@/components/Alert.vue'
```

Current useDownload destructure (lines 172-180):
```typescript
const {
  downloadingList: downloadList,
  finishedList: downloadFinishedList,
  removeFinished,
  pauseDownload,
  cancelDownload,
  resumeDownload,
  clearFinished
} = useDownload()
```

Current CSS for paused-item (lines 375-377):
```css
.pause-item .dowloaded-process-block {
  background: linear-gradient(to right, rgba(230, 160, 60, 255), 50%, rgba(230, 160, 60, 255));
}
```

After Plan 35-02, `useDownload()` returns `getRetryRemaining` and the store's `downloadingList` includes tasks in `'retrying'` state with `retryCount`, `retryDelay`, `retryStartedAt`, and `error` fields.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add retrying + exhausted-failure template blocks, imports, and CSS</name>
  <files>src/views/DownloadWallpaper.vue</files>
  <read_first>src/views/DownloadWallpaper.vue (all 449 lines — full file)</read_first>
  <action>
    Make 5 changes to src/views/DownloadWallpaper.vue:

    **1a. Add formatCountdown import (line 168)**

    Change the import line from:
    ```typescript
    import { formatFileSize, formatResolution, formatSpeed, formatTime } from '@/utils/helpers'
    ```
    to:
    ```typescript
    import { formatFileSize, formatResolution, formatSpeed, formatTime, formatCountdown } from '@/utils/helpers'
    ```

    **1b. Add getRetryRemaining to useDownload destructure (line ~177)**

    Change from:
    ```typescript
    const {
      downloadingList: downloadList,
      finishedList: downloadFinishedList,
      removeFinished,
      pauseDownload,
      cancelDownload,
      resumeDownload,
      clearFinished
    } = useDownload()
    ```
    to:
    ```typescript
    const {
      downloadingList: downloadList,
      finishedList: downloadFinishedList,
      removeFinished,
      pauseDownload,
      cancelDownload,
      resumeDownload,
      clearFinished,
      getRetryRemaining,
    } = useDownload()
    ```

    **1c. Update :class binding on the download item (line 24)**

    Change from:
    ```html
    :class="item.state === 'paused' ? 'pause-item' : ''"
    ```
    to (using object syntax for multiple classes):
    ```html
    :class="{
      'pause-item': item.state === 'paused',
      'failed-item': item.state === 'failed' && item.retryCount === 3
    }"
    ```

    **1d. Add retrying + exhausted-failure v-show blocks in the rigth-bottoim area (after line 80)**

    Insert the following blocks between the `v-show="item.state === 'paused'"` block (line 79) and the `<div class="dowloaded-size">` block (line 81):

    ```html
                <!-- UI-01: Retrying state — show retry count -->
                <div v-show="item.state === 'retrying'" class="dowload-state">
                  重试中 (第{{ item.retryCount }}次/共3次)
                </div>
                <!-- UI-02: Live countdown to next retry attempt -->
                <div v-show="item.state === 'retrying'" class="dowload-countdown">
                  {{ formatCountdown(getRetryRemaining(item)) }}
                </div>
                <!-- UI-03: Exhausted retries — show failure message inline -->
                <div
                  v-show="item.state === 'failed' && item.retryCount === 3"
                  class="dowload-state failed-message"
                >
                  下载失败 — 已重试 3 次
                </div>
    ```

    Note: The `failed-message` class is for styling only (red tint for the message text, different from the standard gray state text).

    **1e. Add CSS for failed-item and countdown styles (after line 377, after .pause-item CSS)**

    Add the following CSS rules:

    ```css
    /* Phase 35: Exhausted retry — gray progress bar (D-04) */
    .failed-item .dowloaded-process-block {
      background: rgba(120, 120, 120, 0.6);
    }

    /* Phase 35: Countdown text style */
    .dowload-countdown {
      margin-right: 10px;
    }

    /* Phase 35: Exhausted failure message — red tint to distinguish from normal gray text */
    .failed-message {
      color: rgb(255, 140, 140) !important;
    }
    ```

    **Order of edits:**
    1. Line 168 — add `formatCountdown` to import
    2. Line ~177 — add `getRetryRemaining` to destructure
    3. Line 24 — update :class binding to object syntax with `failed-item`
    4. Lines 80-81 — insert retrying + exhausted-failure v-show blocks
    5. After line 377 — append CSS rules
  </action>
  <acceptance_criteria>
    - `grep -c "formatCountdown" /Volumes/DATA/Code/Vscode/wallhaven/src/views/DownloadWallpaper.vue` returns exactly 1 (import)
    - `grep -c "getRetryRemaining" /Volumes/DATA/Code/Vscode/wallhaven/src/views/DownloadWallpaper.vue` returns exactly 2 (destructure + template call)
    - `grep -c "重试中 (第" /Volumes/DATA/Code/Vscode/wallhaven/src/views/DownloadWallpaper.vue` returns exactly 1 (UI-01)
    - `grep -c "formatCountdown(getRetryRemaining" /Volumes/DATA/Code/Vscode/wallhaven/src/views/DownloadWallpaper.vue` returns exactly 1 (UI-02 countdown)
    - `grep -c "下载失败 — 已重试 3 次" /Volumes/DATA/Code/Vscode/wallhaven/src/views/DownloadWallpaper.vue` returns exactly 1 (UI-03)
    - `grep -c "failed-item" /Volumes/DATA/Code/Vscode/wallhaven/src/views/DownloadWallpaper.vue` returns exactly 2 (:class binding + CSS selector)
    - `grep -c "failed-message" /Volumes/DATA/Code/Vscode/wallhaven/src/views/DownloadWallpaper.vue` returns exactly 2 (v-show div + CSS)
    - `.dowload-countdown` CSS class exists in style section
    - The :class binding uses object syntax with both `pause-item` and `failed-item` keys
    - The retrying blocks are placed BEFORE the `<div class="dowloaded-size">` line
  </acceptance_criteria>
  <verify>
    <automated>
      grep -c "重试中 (第" /Volumes/DATA/Code/Vscode/wallhaven/src/views/DownloadWallpaper.vue && \
      grep -c "formatCountdown(getRetryRemaining" /Volumes/DATA/Code/Vscode/wallhaven/src/views/DownloadWallpaper.vue && \
      grep -c "下载失败 — 已重试 3 次" /Volumes/DATA/Code/Vscode/wallhaven/src/views/DownloadWallpaper.vue && \
      grep -c "failed-item" /Volumes/DATA/Code/Vscode/wallhaven/src/views/DownloadWallpaper.vue
    </automated>
  </verify>
  <done>
    DownloadWallpaper.vue shows retrying count/countdown for retrying tasks and failure message with gray bar for exhausted retries
  </done>
</task>

</tasks>

<verification>
1. Retrying block shows "重试中 (第X次/共3次)" — exact Chinese string verified by grep
2. Countdown block calls `formatCountdown(getRetryRemaining(item))` — reactive per second
3. Exhausted-failure block shows "下载失败 — 已重试 3 次" — conditional on `item.retryCount === 3`
4. Cancel button is NOT inside any state-specific v-show — always visible (D-03)
5. Pause button is NOT shown during retrying — only on `state === 'downloading'` (D-03)
6. Gray progress bar for exhausted retries via `.failed-item .dowloaded-process-block`
</verification>

<success_criteria>
- Template shows "重试中 (第X次/共3次)" for retrying tasks
- Countdown updates every second via reactive getRetryRemaining
- Exhausted retries show "下载失败 — 已重试 3 次" with gray progress bar
- Cancel button always visible during retry
- No pause button during retry
</success_criteria>

<output>
After completion, create `.planning/phases/35-retry-status-ui/35-03-SUMMARY.md`
</output>

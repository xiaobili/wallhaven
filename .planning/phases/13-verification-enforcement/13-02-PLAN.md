# Plan 13-02: Verify Code State and Run Checks

**Wave:** 2
**Depends on:** 13-01-PLAN.md (ESLint must be configured first)
**Files modified:** None (verification only)
**Autonomous:** true
**Requirements:** CLUP-01, CLUP-03

---

## Context

Verify that all views have no direct store imports and TypeScript compilation passes. This is a verification phase to confirm the architecture migration is complete.

**Current state (from context gathering):**
- All 4 views already migrated (no `useWallpaperStore`/`useDownloadStore` imports)
- `npm run type-check` passes

---

## Tasks

### Task 1: Verify no direct store imports in views

<read_first>
- `/Volumes/DATA/Code/Vscode/wallhaven/src/views/OnlineWallpaper.vue`
- `/Volumes/DATA/Code/Vscode/wallhaven/src/views/LocalWallpaper.vue`
- `/Volumes/DATA/Code/Vscode/wallhaven/src/views/DownloadWallpaper.vue`
- `/Volumes/DATA/Code/Vscode/wallhaven/src/views/SettingPage.vue`
</read_first>

<action>
Run grep command to verify no direct store imports exist in views:

```bash
grep -rn "useWallpaperStore\|useDownloadStore" src/views/
```

The command should return no matches (empty output).
</action>

<acceptance_criteria>
- Grep command `grep -rn "useWallpaperStore\|useDownloadStore" src/views/` returns no output
- OnlineWallpaper.vue does NOT contain `useWallpaperStore`
- OnlineWallpaper.vue does NOT contain `useDownloadStore`
- LocalWallpaper.vue does NOT contain `useWallpaperStore`
- LocalWallpaper.vue does NOT contain `useDownloadStore`
- DownloadWallpaper.vue does NOT contain `useWallpaperStore`
- DownloadWallpaper.vue does NOT contain `useDownloadStore`
- SettingPage.vue does NOT contain `useWallpaperStore`
- SettingPage.vue does NOT contain `useDownloadStore`
</acceptance_criteria>

---

### Task 2: Verify composables correctly use stores

<read_first>
- `/Volumes/DATA/Code/Vscode/wallhaven/src/composables/wallpaper/useWallpaperList.ts`
- `/Volumes/DATA/Code/Vscode/wallhaven/src/composables/download/useDownload.ts`
- `/Volumes/DATA/Code/Vscode/wallhaven/src/composables/settings/useSettings.ts`
</read_first>

<action>
Verify that composables DO import from stores (this is correct behavior):

```bash
grep -rn "useWallpaperStore\|useDownloadStore" src/composables/
```

The command should return matches showing composables import stores.
</action>

<acceptance_criteria>
- Grep command `grep -rn "useWallpaperStore\|useDownloadStore" src/composables/` returns matches
- `useWallpaperList.ts` contains `import { useWallpaperStore } from '@/stores/wallpaper'`
- `useDownload.ts` contains `import { useDownloadStore } from '@/stores/modules/download'`
- `useSettings.ts` contains `import { useWallpaperStore } from '@/stores/wallpaper'`
</acceptance_criteria>

---

### Task 3: Run TypeScript type check

<read_first>
- No file to read (bash command)
</read_first>

<action>
Run TypeScript type check to verify no type errors:

```bash
npm run type-check
```
</action>

<acceptance_criteria>
- Command `npm run type-check` completes with exit code 0
- No TypeScript errors are reported
- Command output shows successful compilation
</acceptance_criteria>

---

### Task 4: Run ESLint to verify rule enforcement

<read_first>
- No file to read (bash command)
</read_first>

<action>
Run ESLint to verify the no-restricted-imports rule works:

```bash
npm run lint
```

This should pass because views already have no store imports.
</action>

<acceptance_criteria>
- Command `npm run lint` completes with exit code 0
- No ESLint errors are reported
- No warnings about views importing from stores
</acceptance_criteria>

---

## Verification

After all tasks complete:

1. Grep confirms no store imports in views
2. Grep confirms composables correctly use stores
3. TypeScript compilation passes
4. ESLint passes with no errors

---

## must_haves

- [ ] All 4 views verified to have no `useWallpaperStore` import
- [ ] All 4 views verified to have no `useDownloadStore` import
- [ ] Composables verified to correctly import stores
- [ ] `npm run type-check` passes
- [ ] `npm run lint` passes

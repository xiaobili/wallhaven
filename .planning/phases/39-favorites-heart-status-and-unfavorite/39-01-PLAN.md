---
phase: 39-favorites-heart-status-and-unfavorite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/favorites/FavoriteWallpaperCard.vue
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "User can click the favorite badge on a card to unfavorite"
    - "Badge hover shows '点击取消收藏' instead of collection names"
    - "Badge click does not open the ImagePreview overlay"
    - "Badge cursor changes to pointer on hover"
  artifacts:
    - path: "src/components/favorites/FavoriteWallpaperCard.vue"
      provides: "Clickable unfavorite badge with hover tooltip"
      contains: "emit('unfavorite"
  key_links:
    - from: "FavoriteWallpaperCard.vue template"
      to: "FavoriteWallpaperCard.vue script"
      via: "unfavorite emit"
      pattern: "emit\\('unfavorite'"
---
<objective>
Make the favorite badge on FavoriteWallpaperCard clickable to emit an `unfavorite` event, with proper hover tooltip and click-stop propagation.

Purpose: Users can unfavorite a wallpaper directly from the card badge without opening the ImagePreview.
Output: Modified FavoriteWallpaperCard.vue with clickable badge emitting unfavorite(wallpaperId).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-favorites-heart-status-and-unfavorite/39-CONTEXT.md
@.planning/phases/39-favorites-heart-status-and-unfavorite/39-RESEARCH.md

<interfaces>
Current emit types from FavoriteWallpaperCard.vue (script setup, line 42-46):
```typescript
const emit = defineEmits<{
  preview: [wallpaperData: FavoriteItem['wallpaperData']]
  download: [wallpaperData: FavoriteItem['wallpaperData']]
  'set-bg': [wallpaperData: FavoriteItem['wallpaperData']]
}>()
```

Current badge template (line 4-7):
```html
<div class="favorite-badge" :title="collectionNames.join(', ')">
  <i class="fas fa-heart" />
  <span v-if="collectionCount > 1" class="badge-count">{{ collectionCount }}</span>
</div>
```

Current badge CSS (lines 151-179): `cursor: default`, no hover effect.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make favorite badge clickable with unfavorite emit</name>

  <read_first>
    - src/components/favorites/FavoriteWallpaperCard.vue (entire file — read current state before modifying)
  </read_first>

  <action>
    Modify `src/components/favorites/FavoriteWallpaperCard.vue` to:

    1. **Add `unfavorite` to defineEmits** — Add `unfavorite: [wallpaperId: string]` to the existing emit type definition (line 42-46). The payload must be `props.favorite.wallpaperId` (string).

    2. **Wire click on `.favorite-badge`** — Add `@click.stop="emit('unfavorite', props.favorite.wallpaperId)"` to the `.favorite-badge` div. The `@click.stop` is REQUIRED per Pitfall 2 to prevent event propagation to the `.preview` overlay (z-index 110), which would otherwise open ImagePreview.

    3. **Change badge `:title` attribute** — Replace `:title="collectionNames.join(', ')"` with `:title="'点击取消收藏'"` per D-03. The badge should show "点击取消收藏" on hover instead of the collection names.

    4. **CSS: Change cursor to pointer** — Change `cursor: default` on `.favorite-badge` (line 162) to `cursor: pointer`.

    5. **CSS: Add hover effect on badge** — Add `.favorite-badge:hover` rule that slightly reduces opacity (e.g., `opacity: 0.8`). This provides subtle visual feedback that the badge is interactive. Within Claude's Discretion per CONTEXT.md decisions section.

    Per D-03: "FavoriteWallpaperCard 左上角的收藏徽章变为可点击。左键点击触发取消收藏。hover 时显示提示文字 '点击取消收藏'。」
    Per D-01/D-02: No changes to heart button visibility or red dot indicator — only badge interaction.
  </action>

  <acceptance_criteria>
    - Grep confirms `unfavorite` in defineEmits: `grep -n "unfavorite" src/components/favorites/FavoriteWallpaperCard.vue` returns 2 matches (emit type + template usage)
    - Grep confirms `@click.stop` on `.favorite-badge`: `grep -c "@click.stop" src/components/favorites/FavoriteWallpaperCard.vue` returns >= 1
    - Badge title is "点击取消收藏": `grep -c "点击取消收藏" src/components/favorites/FavoriteWallpaperCard.vue` returns >= 1
    - CSS cursor is pointer: `grep -c "cursor: pointer" src/components/favorites/FavoriteWallpaperCard.vue` returns >= 1 (was 0 before)
    - `.favorite-badge:hover` exists: `grep -c "favorite-badge:hover" src/components/favorites/FavoriteWallpaperCard.vue` returns >= 1
    - `cursor: default` no longer present on `.favorite-badge`: the original default cursor line is removed/changed
  </acceptance_criteria>

  <verify>
    <automated>grep -n "unfavorite\|@click\.stop\|点击取消收藏\|cursor: pointer\|favorite-badge:hover" src/components/favorites/FavoriteWallpaperCard.vue | head -20</automated>
  </verify>

  <done>
    - Badge emits `unfavorite` with wallpaperId string on click
    - Badge does NOT trigger preview overlay (stopPropagation)
    - Hover shows "点击取消收藏"
    - Cursor changes to pointer on badge hover
    - All existing card functionality (preview, download, set-bg) is unchanged
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DOM event → component logic | User click on badge triggers unfavorite emit via composable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-01 | S (Spoofing) | Badge click | accept | No auth concern — badge emits event, parent validates operation |
| T-39-02 | T (Tampering) | DOM event | mitigate | `@click.stop` prevents event propagation to preview overlay |
</threat_model>

<verification>
- [ ] `emit('unfavorite', wallpaperId)` is defined and wired to `@click.stop`
- [ ] Badge title shows "点击取消收藏"
- [ ] CSS cursor is `pointer` on badge, hover effect exists
- [ ] No regression: preview, download, set-bg emits still work
</verification>

<success_criteria>
- FavoriteWallpaperCard emits `unfavorite` event on badge click
- Badge click does not trigger ImagePreview (stopPropagation verified)
- Hover tooltip shows "点击取消收藏"
- Cursor indicates clickability
</success_criteria>

<output>
After completion, create `.planning/phases/39-favorites-heart-status-and-unfavorite/39-01-SUMMARY.md`
</output>

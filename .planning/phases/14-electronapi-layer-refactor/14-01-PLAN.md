---
phase: 14
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/repositories/wallpaper.repository.ts
  - src/repositories/settings.repository.ts
autonomous: true
requirements:
  - EAPI-01
  - EAPI-02
---

# Plan 1: 扩展 Repository 层

<objective>
扩展 wallpaperRepository 和 settingsRepository，添加缺失的方法以支持 ElectronAPI 分层架构。
</objective>

<tasks>

## Task 1: 扩展 wallpaperRepository

<type>code</type>
<files>
- `src/repositories/wallpaper.repository.ts`
</files>
<read_first>
- `src/repositories/wallpaper.repository.ts` (当前实现)
- `src/clients/electron.client.ts` (参考 electronClient.setWallpaper 接口)
- `src/shared/types/ipc.ts` (参考 IpcResponse 类型)
</read_first>
<action>
在 `src/repositories/wallpaper.repository.ts` 中添加 `setWallpaper` 方法：

```typescript
/**
 * 设置桌面壁纸
 * @param imagePath - 图片文件路径
 */
async setWallpaper(imagePath: string): Promise<IpcResponse<void>> {
  return electronClient.setWallpaper(imagePath)
}
```

该方法需要：
1. 导入 electronClient（如果尚未导入）
2. 添加到 wallpaperRepository 对象中
3. 保持与其他方法相同的代码风格和注释格式
</action>
<acceptance_criteria>
- `grep -n "setWallpaper" src/repositories/wallpaper.repository.ts` 返回匹配行
- `grep -n "async setWallpaper" src/repositories/wallpaper.repository.ts` 返回方法定义
- 文件包含 `imagePath: string` 参数
- 文件包含 `Promise<IpcResponse<void>>` 返回类型
</acceptance_criteria>
<verify>
1. TypeScript 编译通过：`npm run typecheck`
2. 方法签名正确，返回 IpcResponse<void>
</verify>

## Task 2: 扩展 settingsRepository

<type>code</type>
<files>
- `src/repositories/settings.repository.ts`
</files>
<read_first>
- `src/repositories/settings.repository.ts` (当前实现)
- `src/clients/electron.client.ts` (参考 electronClient 方法接口)
- `src/shared/types/ipc.ts` (参考 IpcResponse 和 LocalFile 类型)
</read_first>
<action>
在 `src/repositories/settings.repository.ts` 中添加三个方法：

1. **openFolder 方法：**
```typescript
/**
 * 在系统文件管理器中打开文件夹
 * @param folderPath - 文件夹路径
 */
async openFolder(folderPath: string): Promise<IpcResponse<void>> {
  return electronClient.openFolder(folderPath)
}
```

2. **readDirectory 方法：**
```typescript
/**
 * 读取目录内容
 * @param dirPath - 目录路径
 * @returns 返回目录中的文件列表
 */
async readDirectory(dirPath: string): Promise<IpcResponse<LocalFile[]>> {
  return electronClient.readDirectory(dirPath)
}
```

3. **deleteFile 方法：**
```typescript
/**
 * 删除文件
 * @param filePath - 文件路径
 */
async deleteFile(filePath: string): Promise<IpcResponse<void>> {
  return electronClient.deleteFile(filePath)
}
```

需要：
1. 导入 LocalFile 类型（从 @/shared/types/ipc）
2. 添加三个方法到 settingsRepository 对象
3. 保持与其他方法相同的代码风格和注释格式
</action>
<acceptance_criteria>
- `grep -n "openFolder" src/repositories/settings.repository.ts` 返回匹配行
- `grep -n "readDirectory" src/repositories/settings.repository.ts` 返回匹配行
- `grep -n "deleteFile" src/repositories/settings.repository.ts` 返回匹配行
- 文件包含 `LocalFile[]` 类型导入或引用
- 所有方法返回 `Promise<IpcResponse<...>>` 类型
</acceptance_criteria>
<verify>
1. TypeScript 编译通过：`npm run typecheck`
2. 所有方法签名正确
</verify>

</tasks>

<verification>
1. 运行 `npm run typecheck` 确认无类型错误
2. 检查所有新增方法都返回 IpcResponse 类型
3. 确认 electronClient 导入正确
</verification>

<success_criteria>
- wallpaperRepository 包含 setWallpaper 方法
- settingsRepository 包含 openFolder, readDirectory, deleteFile 方法
- TypeScript 编译通过
</success_criteria>

<must_haves>
- Repository 方法直接调用 electronClient
- 返回类型统一为 IpcResponse<T>
- 方法命名与 electronClient 方法一致
</must_haves>

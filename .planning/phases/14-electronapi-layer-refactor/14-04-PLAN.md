---
phase: 14
plan: 4
type: execute
wave: 3
depends_on:
  - 14-02-PLAN.md
files_modified:
  - src/composables/local/useLocalFiles.ts
  - src/composables/index.ts
autonomous: true
requirements:
  - EAPI-01
---

# Plan 4: 创建 useLocalFiles Composable

<objective>
创建 useLocalFiles composable，封装本地文件操作（readDirectory, openFolder, deleteFile）。
</objective>

<tasks>

## Task 1: 创建 useLocalFiles 文件

<type>code</type>
<files>
- `src/composables/local/useLocalFiles.ts`
</files>
<read_first>
- `src/composables/settings/useSettings.ts` (参考现有 Composable 模式)
- `src/services/settings.service.ts` (参考 settingsService 新方法)
- `src/composables/core/useAlert.ts` (参考 useAlert 接口)
- `src/shared/types/ipc.ts` (参考 LocalFile 类型)
</read_first>
<action>
创建 `src/composables/local/useLocalFiles.ts` 文件：

```typescript
/**
 * 本地文件操作 Composable
 *
 * 封装本地文件操作逻辑，协调 SettingsService
 * 提供读取目录、打开文件夹、删除文件功能
 *
 * @example
 * ```typescript
 * const { readDirectory, openFolder, deleteFile } = useLocalFiles()
 *
 * // 读取目录
 * const result = await readDirectory('/path/to/folder')
 * if (result.success) {
 *   console.log('文件列表:', result.data)
 * }
 *
 * // 打开文件夹
 * await openFolder('/path/to/folder')
 *
 * // 删除文件
 * await deleteFile('/path/to/file.jpg')
 * ```
 */

import { useAlert } from '@/composables'
import { settingsService } from '@/services'
import type { IpcResponse } from '@/shared/types/ipc'
import type { LocalFile } from '@/shared/types/ipc'

/**
 * useLocalFiles 返回值接口
 */
export interface UseLocalFilesReturn {
  /** 读取目录内容 */
  readDirectory: (dirPath: string) => Promise<IpcResponse<LocalFile[]>>
  /** 在系统文件管理器中打开文件夹 */
  openFolder: (folderPath: string) => Promise<IpcResponse<void>>
  /** 删除文件 */
  deleteFile: (filePath: string) => Promise<IpcResponse<void>>
}

/**
 * 本地文件操作 Composable
 *
 * @returns 文件操作方法
 */
export function useLocalFiles(): UseLocalFilesReturn {
  const { showError } = useAlert()

  /**
   * 读取目录内容
   * @param dirPath - 目录路径
   * @returns 文件列表
   */
  const readDirectory = async (dirPath: string): Promise<IpcResponse<LocalFile[]>> => {
    const result = await settingsService.readDirectory(dirPath)

    if (!result.success) {
      showError(result.error?.message || '读取目录失败')
    }

    return result
  }

  /**
   * 在系统文件管理器中打开文件夹
   * @param folderPath - 文件夹路径
   */
  const openFolder = async (folderPath: string): Promise<IpcResponse<void>> => {
    const result = await settingsService.openFolder(folderPath)

    if (!result.success) {
      showError(result.error?.message || '打开文件夹失败')
    }

    return result
  }

  /**
   * 删除文件
   * @param filePath - 文件路径
   */
  const deleteFile = async (filePath: string): Promise<IpcResponse<void>> => {
    const result = await settingsService.deleteFile(filePath)

    if (!result.success) {
      showError(result.error?.message || '删除文件失败')
    }

    return result
  }

  return {
    readDirectory,
    openFolder,
    deleteFile,
  }
}
```
</action>
<acceptance_criteria>
- 文件 `src/composables/local/useLocalFiles.ts` 存在
- `grep -n "export function useLocalFiles" src/composables/local/useLocalFiles.ts` 返回匹配
- `grep -n "export interface UseLocalFilesReturn" src/composables/local/useLocalFiles.ts` 返回匹配
- 文件包含 `readDirectory` 方法
- 文件包含 `openFolder` 方法
- 文件包含 `deleteFile` 方法
- 文件正确导入 settingsService 和 useAlert
</acceptance_criteria>
<verify>
1. TypeScript 编译通过：`npm run typecheck`
2. 文件遵循现有 Composable 模式
</verify>

## Task 2: 更新 Composables 导出

<type>code</type>
<files>
- `src/composables/index.ts`
</files>
<read_first>
- `src/composables/index.ts` (当前导出)
- `src/composables/local/useLocalFiles.ts` (新建的 composable)
</read_first>
<action>
在 `src/composables/index.ts` 中添加 useLocalFiles 导出：

在文件末尾添加：
```typescript
// Local Files
export { useLocalFiles, type UseLocalFilesReturn } from './local/useLocalFiles'
```
</action>
<acceptance_criteria>
- `grep -n "useLocalFiles" src/composables/index.ts` 返回匹配
- `grep -n "UseLocalFilesReturn" src/composables/index.ts` 返回匹配
</acceptance_criteria>
<verify>
1. TypeScript 编译通过：`npm run typecheck`
2. 可以从 '@/composables' 导入 useLocalFiles
</verify>

</tasks>

<verification>
1. 运行 `npm run typecheck` 确认无类型错误
2. 确认 useLocalFiles 可以正确导入
3. 确认所有方法调用 settingsService
</verification>

<success_criteria>
- useLocalFiles.ts 文件创建成功
- composables/index.ts 包含导出
- TypeScript 编译通过
</success_criteria>

<must_haves>
- Composable 返回 readDirectory, openFolder, deleteFile 方法
- 使用 useAlert 显示错误提示
- 调用 settingsService 对应方法
</must_haves>

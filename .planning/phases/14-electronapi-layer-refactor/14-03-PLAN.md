---
phase: 14
plan: 3
type: execute
wave: 3
depends_on:
  - 14-02-PLAN.md
files_modified:
  - src/composables/wallpaper/useWallpaperSetter.ts
  - src/composables/index.ts
autonomous: true
requirements:
  - EAPI-01
  - EAPI-02
---

# Plan 3: 创建 useWallpaperSetter Composable

<objective>
创建 useWallpaperSetter composable，封装 setWallpaper 操作，提供统一的错误处理和成功提示。
</objective>

<tasks>

## Task 1: 创建 useWallpaperSetter 文件

<type>code</type>
<files>
- `src/composables/wallpaper/useWallpaperSetter.ts`
</files>
<read_first>
- `src/composables/settings/useSettings.ts` (参考现有 Composable 模式)
- `src/services/wallpaper.service.ts` (参考 wallpaperService.setWallpaper)
- `src/composables/core/useAlert.ts` (参考 useAlert 接口)
</read_first>
<action>
创建 `src/composables/wallpaper/useWallpaperSetter.ts` 文件：

```typescript
/**
 * 壁纸设置 Composable
 *
 * 封装壁纸设置逻辑，协调 WallpaperService
 * 提供设置壁纸功能，自动显示错误/成功提示
 *
 * @example
 * ```typescript
 * const { setWallpaper, loading } = useWallpaperSetter()
 *
 * // 设置壁纸
 * const success = await setWallpaper('/path/to/image.jpg')
 * if (success) {
 *   console.log('壁纸设置成功')
 * }
 * ```
 */

import { ref, type Ref } from 'vue'
import { wallpaperService } from '@/services'
import { useAlert } from '@/composables'

/**
 * useWallpaperSetter 返回值接口
 */
export interface UseWallpaperSetterReturn {
  /** 是否正在设置 */
  loading: Ref<boolean>
  /** 设置壁纸 */
  setWallpaper: (imagePath: string) => Promise<boolean>
}

/**
 * 壁纸设置 Composable
 *
 * @returns 设置状态和操作方法
 */
export function useWallpaperSetter(): UseWallpaperSetterReturn {
  const { showError, showSuccess } = useAlert()
  const loading = ref(false)

  /**
   * 设置桌面壁纸
   * @param imagePath - 图片文件路径
   * @returns 是否设置成功
   */
  const setWallpaper = async (imagePath: string): Promise<boolean> => {
    loading.value = true

    try {
      const result = await wallpaperService.setWallpaper(imagePath)

      if (!result.success) {
        showError(result.error?.message || '设置壁纸失败')
        return false
      }

      showSuccess('壁纸设置成功')
      return true
    } catch (error) {
      showError(error instanceof Error ? error.message : '设置壁纸失败')
      return false
    } finally {
      loading.value = false
    }
  }

  return {
    loading,
    setWallpaper,
  }
}
```
</action>
<acceptance_criteria>
- 文件 `src/composables/wallpaper/useWallpaperSetter.ts` 存在
- `grep -n "export function useWallpaperSetter" src/composables/wallpaper/useWallpaperSetter.ts` 返回匹配
- `grep -n "export interface UseWallpaperSetterReturn" src/composables/wallpaper/useWallpaperSetter.ts` 返回匹配
- 文件包含 `setWallpaper` 方法
- 文件包含 `loading` Ref
- 文件正确导入 wallpaperService 和 useAlert
</acceptance_criteria>
<verify>
1. TypeScript 编译通过：`npm run typecheck`
2. 文件遵循现有 Composable 模式
</verify>

## Task 2: 更新 Composables 导出

<type>code</type>
<files>
- `src/composables/index.ts`
</files>
<read_first>
- `src/composables/index.ts` (当前导出)
- `src/composables/wallpaper/useWallpaperSetter.ts` (新建的 composable)
</read_first>
<action>
在 `src/composables/index.ts` 中添加 useWallpaperSetter 导出：

在文件末尾添加：
```typescript
// Wallpaper Setter
export { useWallpaperSetter, type UseWallpaperSetterReturn } from './wallpaper/useWallpaperSetter'
```
</action>
<acceptance_criteria>
- `grep -n "useWallpaperSetter" src/composables/index.ts` 返回匹配
- `grep -n "UseWallpaperSetterReturn" src/composables/index.ts` 返回匹配
</acceptance_criteria>
<verify>
1. TypeScript 编译通过：`npm run typecheck`
2. 可以从 '@/composables' 导入 useWallpaperSetter
</verify>

</tasks>

<verification>
1. 运行 `npm run typecheck` 确认无类型错误
2. 确认 useWallpaperSetter 可以正确导入
3. 确认 setWallpaper 方法调用 wallpaperService
</verification>

<success_criteria>
- useWallpaperSetter.ts 文件创建成功
- composables/index.ts 包含导出
- TypeScript 编译通过
</success_criteria>

<must_haves>
- Composable 返回 loading 状态和 setWallpaper 方法
- 使用 useAlert 显示成功/错误提示
- 调用 wallpaperService.setWallpaper
</must_haves>

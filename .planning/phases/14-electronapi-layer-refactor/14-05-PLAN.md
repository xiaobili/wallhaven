---
phase: 14
plan: 5
type: execute
wave: 3
depends_on:
  - 14-02-PLAN.md
files_modified:
  - src/composables/settings/useSettings.ts
autonomous: true
requirements:
  - EAPI-02
---

# Plan 5: 扩展 useSettings Composable

<objective>
扩展 useSettings composable，添加 selectFolder 方法返回，使 Views 可以通过 composable 调用文件夹选择功能。
</objective>

<tasks>

## Task 1: 扩展 useSettings 接口和实现

<type>code</type>
<files>
- `src/composables/settings/useSettings.ts`
</files>
<read_first>
- `src/composables/settings/useSettings.ts` (当前实现)
- `src/services/settings.service.ts` (参考 settingsService.selectFolder)
- `src/shared/types/ipc.ts` (参考 IpcResponse 类型)
</read_first>
<action>
修改 `src/composables/settings/useSettings.ts`：

1. **更新 UseSettingsReturn 接口**，添加 selectFolder 方法：
```typescript
export interface UseSettingsReturn {
  // ... 现有字段 ...

  // 新增：文件夹选择
  selectFolder: () => Promise<IpcResponse<string | null>>
}
```

2. **在 useSettings 函数中添加 selectFolder 方法**：
```typescript
/**
 * 选择文件夹
 * 打开系统文件夹选择对话框
 * @returns 选中的文件夹路径，取消返回 null
 */
const selectFolder = async (): Promise<IpcResponse<string | null>> => {
  return settingsService.selectFolder()
}
```

3. **更新 return 语句**，添加 selectFolder：
```typescript
return {
  settings: computed(() => store.settings),
  editableSettings,
  startEdit,
  discardChanges,
  saveChanges,
  isDirty,
  load,
  update,
  reset,
  getDefaults,
  selectFolder, // 新增
}
```

需要：
1. 导入 IpcResponse 类型（如果尚未导入）
2. 更新接口定义
3. 添加 selectFolder 方法实现
4. 更新 return 语句
</action>
<acceptance_criteria>
- `grep -n "selectFolder" src/composables/settings/useSettings.ts` 至少返回 3 处匹配（接口、实现、return）
- `grep -n "selectFolder:" src/composables/settings/useSettings.ts` 返回 return 语句中的导出
- 文件包含 `Promise<IpcResponse<string | null>>` 返回类型
- selectFolder 方法调用 `settingsService.selectFolder()`
</acceptance_criteria>
<verify>
1. TypeScript 编译通过：`npm run typecheck`
2. useSettings 返回值包含 selectFolder 方法
</verify>

</tasks>

<verification>
1. 运行 `npm run typecheck` 确认无类型错误
2. 确认 selectFolder 方法正确调用 settingsService.selectFolder
3. 确认返回类型正确
</verification>

<success_criteria>
- useSettings 返回值包含 selectFolder 方法
- TypeScript 编译通过
</success_criteria>

<must_haves>
- selectFolder 方法返回 IpcResponse<string | null>
- 调用 settingsService.selectFolder
- 添加到 UseSettingsReturn 接口
</must_haves>

---
phase: 1
plan: 5
type: execute
wave: 2
depends_on: [01-PLAN-03, 01-PLAN-04]
files_modified:
  - src/main.ts
files_created: []
autonomous: true
requirements: [ARCH-05]
---

<objective>
添加全局错误处理器到 `src/main.ts`，处理 Vue 错误、未捕获的 Promise rejection 和全局 JavaScript 错误。
</objective>

<tasks>
<task id="1" type="modify">
<read_first>
- src/main.ts
- src/errors/index.ts
- src/composables/core/useAlert.ts
</read_first>
<action>
修改 `src/main.ts`，在 `app.use(router)` 之后、`app.mount('#app')` 之前添加全局错误处理器。

在文件顶部添加导入：
```typescript
import { AppError } from './errors'
```

在 `app.use(router)` 之后添加以下代码：

```typescript
// ==================== 全局错误处理器 ====================

/**
 * Vue 错误处理器
 * 捕获组件渲染和生命周期钩子中的错误
 */
app.config.errorHandler = (err, instance, info) => {
  console.error('[Vue Error]', err)
  console.error('[Component Info]', info)

  if (err instanceof AppError) {
    console.error('[Error Code]', err.code)
    if (err.context) {
      console.error('[Error Context]', err.context)
    }
  }

  // 注意：不在此处显示 Alert，因为应用可能未完全初始化
  // 组件级别的错误应由组件自行处理
}

/**
 * 未处理的 Promise rejection
 * 捕获 async 函数中未处理的错误
 */
window.addEventListener('unhandledrejection', (event) => {
  console.error('[Unhandled Rejection]', event.reason)

  if (event.reason instanceof AppError) {
    console.error('[Error Code]', event.reason.code)
    if (event.reason.context) {
      console.error('[Error Context]', event.reason.context)
    }
  }

  // 阻止默认的控制台警告
  event.preventDefault()
})

/**
 * 全局 JavaScript 错误
 * 捕获同步代码中的未捕获错误
 */
window.addEventListener('error', (event) => {
  console.error('[Global Error]', event.error)
})

// ==================== 应用挂载 ====================
```

完整的修改后文件应该保持以下结构：
1. 导入语句（包含新增的 `import { AppError } from './errors'`）
2. 变量声明（wallpaperStore, downloadStore）
3. app 创建和 pinia/router 注册
4. **新增：全局错误处理器**
5. app.mount('#app')
6. initializeApp() 函数定义
7. initializeApp() 调用
</action>
<acceptance_criteria>
- `grep -q "import { AppError } from './errors'" src/main.ts` 返回退出码 0
- `grep -q "app.config.errorHandler" src/main.ts` 返回退出码 0
- `grep -q "window.addEventListener('unhandledrejection'" src/main.ts` 返回退出码 0
- `grep -q "window.addEventListener('error'" src/main.ts` 返回退出码 0
- `grep -q "event.preventDefault()" src/main.ts` 返回退出码 0
</acceptance_criteria>
</task>
</tasks>

<verification>
```bash
# 验证导入
grep "import { AppError }" src/main.ts

# 验证错误处理器
grep "app.config.errorHandler" src/main.ts

# 验证事件监听器
grep "unhandledrejection" src/main.ts
grep "window.addEventListener('error'" src/main.ts

# TypeScript 编译检查
npm run typecheck

# 启动应用验证
npm run dev
# 预期：应用正常启动，无控制台错误
```
</verification>

<success_criteria>
- 全局错误处理器成功注册
- Vue 错误能被捕获并记录
- 未处理的 Promise rejection 能被捕获
- 全局 JavaScript 错误能被捕获
- TypeScript 编译无错误
- 应用正常启动和运行
</success_criteria>

<must_haves>
- AppError 实例的错误码和上下文被正确记录
- unhandledrejection 事件被 preventDefault 阻止默认警告
- 不影响应用的正常启动和运行
- 错误处理器在 app.mount 之前注册
</must_haves>

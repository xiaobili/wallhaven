---
phase: 1
plan: 4
type: execute
wave: 1
depends_on: []
files_created:
  - src/composables/core/useAlert.ts
  - src/composables/index.ts
files_modified: []
autonomous: true
requirements: [ARCH-04]
---

<objective>
创建 `useAlert` composable，解决 Alert 逻辑重复问题，为后续组件迁移提供统一的状态管理接口。
</objective>

<tasks>
<task id="1" type="create">
<read_first>
- src/views/OnlineWallpaper.vue
- src/components/Alert.vue
</read_first>
<action>
创建目录 `src/composables/core/` 和文件 `src/composables/core/useAlert.ts`，内容如下：

```typescript
/**
 * Alert 状态管理 composable
 *
 * 用于统一管理 Alert 提示框的状态和显示逻辑
 * 与现有 Alert.vue 组件配合使用
 *
 * @example
 * ```typescript
 * const { alert, showAlert, showSuccess, showError } = useAlert()
 *
 * // 显示成功提示
 * showSuccess('操作成功')
 *
 * // 显示错误提示（默认更长显示时间）
 * showError('操作失败')
 *
 * // 在模板中使用
 * // <Alert v-if="alert.visible" :type="alert.type" :message="alert.message" />
 * ```
 */

import { reactive, type Reactive } from 'vue'

/**
 * Alert 类型
 */
export type AlertType = 'success' | 'error' | 'warning' | 'info'

/**
 * Alert 状态接口
 */
export interface AlertState {
  visible: boolean
  type: AlertType
  message: string
  duration: number
}

/**
 * useAlert 返回值接口
 */
export interface UseAlertReturn {
  alert: Reactive<AlertState>
  showAlert: (message: string, type?: AlertType, duration?: number) => void
  hideAlert: () => void
  showSuccess: (message: string, duration?: number) => void
  showError: (message: string, duration?: number) => void
  showWarning: (message: string, duration?: number) => void
  showInfo: (message: string, duration?: number) => void
}

/**
 * 创建 Alert 状态管理
 *
 * @param defaultDuration 默认显示时长（毫秒），默认 3000ms
 * @returns Alert 状态和方法
 */
export function useAlert(defaultDuration = 3000): UseAlertReturn {
  const alert = reactive<AlertState>({
    visible: false,
    type: 'info',
    message: '',
    duration: defaultDuration,
  })

  /**
   * 显示 Alert
   *
   * @param message 提示消息
   * @param type 提示类型，默认 'info'
   * @param duration 显示时长（毫秒），默认使用 defaultDuration
   */
  const showAlert = (
    message: string,
    type: AlertType = 'info',
    duration: number = defaultDuration
  ): void => {
    alert.message = message
    alert.type = type
    alert.duration = duration
    alert.visible = true
  }

  /**
   * 隐藏 Alert
   */
  const hideAlert = (): void => {
    alert.visible = false
  }

  /**
   * 显示成功提示
   *
   * @param message 提示消息
   * @param duration 显示时长（毫秒），默认使用 defaultDuration
   */
  const showSuccess = (message: string, duration?: number): void => {
    showAlert(message, 'success', duration)
  }

  /**
   * 显示错误提示
   * 错误提示默认显示更长时间（5000ms）
   *
   * @param message 提示消息
   * @param duration 显示时长（毫秒），默认 5000ms
   */
  const showError = (message: string, duration = 5000): void => {
    showAlert(message, 'error', duration)
  }

  /**
   * 显示警告提示
   *
   * @param message 提示消息
   * @param duration 显示时长（毫秒），默认使用 defaultDuration
   */
  const showWarning = (message: string, duration?: number): void => {
    showAlert(message, 'warning', duration)
  }

  /**
   * 显示信息提示
   *
   * @param message 提示消息
   * @param duration 显示时长（毫秒），默认使用 defaultDuration
   */
  const showInfo = (message: string, duration?: number): void => {
    showAlert(message, 'info', duration)
  }

  return {
    alert,
    showAlert,
    hideAlert,
    showSuccess,
    showError,
    showWarning,
    showInfo,
  }
}
```
</action>
<acceptance_criteria>
- `test -d src/composables/core && echo "exists"` 返回 "exists"
- `test -f src/composables/core/useAlert.ts && echo "exists"` 返回 "exists"
- `grep -q "export function useAlert" src/composables/core/useAlert.ts` 返回退出码 0
- `grep -q "export type AlertType" src/composables/core/useAlert.ts` 返回退出码 0
- `grep -q "export interface AlertState" src/composables/core/useAlert.ts` 返回退出码 0
- `grep -q "showSuccess" src/composables/core/useAlert.ts` 返回退出码 0
- `grep -q "showError" src/composables/core/useAlert.ts` 返回退出码 0
</acceptance_criteria>
</task>

<task id="2" type="create">
<read_first>
- src/composables/core/useAlert.ts
</read_first>
<action>
创建文件 `src/composables/index.ts`，内容如下：

```typescript
/**
 * Composables 统一导出
 */

export { useAlert, type AlertType, type AlertState, type UseAlertReturn } from './core/useAlert'
```
</action>
<acceptance_criteria>
- `test -f src/composables/index.ts && echo "exists"` 返回 "exists"
- `grep -q "export { useAlert" src/composables/index.ts` 返回退出码 0
</acceptance_criteria>
</task>
</tasks>

<verification>
```bash
# 验证文件存在
ls -la src/composables/
ls -la src/composables/core/

# 验证导出
grep "export" src/composables/index.ts

# 验证 useAlert 签名
grep "export function useAlert" src/composables/core/useAlert.ts
grep "showSuccess" src/composables/core/useAlert.ts
grep "showError" src/composables/core/useAlert.ts

# TypeScript 编译检查
npm run typecheck
```
</verification>

<success_criteria>
- composable 文件创建成功
- 接口定义与现有 Alert.vue 组件 props 兼容
- 返回值包含 alert 响应式对象和便捷方法
- TypeScript 编译无错误
- 可通过 `@/composables` 导入
</success_criteria>

<must_haves>
- alert 状态对象结构与现有组件使用模式一致（visible, type, message, duration）
- showError 默认显示时间更长（5000ms vs 3000ms）
- 提供 showSuccess, showError, showWarning, showInfo 便捷方法
- 与 Alert.vue 组件 props 完全兼容
</must_haves>

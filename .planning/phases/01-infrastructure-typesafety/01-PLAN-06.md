---
phase: 1
plan: 6
type: execute
wave: 2
depends_on: [01-PLAN-01, 01-PLAN-02]
files_modified:
  - src/stores/modules/wallpaper/actions.ts
files_created: []
autonomous: true
requirements: [ARCH-06]
---

<objective>
消除 Store 中的 `any` 类型，将 `src/stores/modules/wallpaper/actions.ts` 第 13 行的 `any` 替换为正确的 `ShallowRef<TotalPageData>` 类型。
</objective>

<tasks>
<task id="1" type="modify">
<read_first>
- src/stores/modules/wallpaper/actions.ts
- src/types/index.ts
</read_first>
<action>
修改 `src/stores/modules/wallpaper/actions.ts`，进行以下变更：

1. 在文件顶部添加 `ShallowRef` 类型导入：

找到：
```typescript
import type { Ref, Reactive } from 'vue'
```

替换为：
```typescript
import type { Ref, Reactive, ShallowRef } from 'vue'
```

2. 修改 `createWallpaperActions` 函数第一个参数的类型：

找到第 12-13 行：
```typescript
export function createWallpaperActions(
  totalPageData: any, // shallowRef<TotalPageData>
```

替换为：
```typescript
export function createWallpaperActions(
  totalPageData: ShallowRef<TotalPageData>,
```

注意：删除注释 `// shallowRef<TotalPageData>`，因为类型已经明确。
</action>
<acceptance_criteria>
- `grep -q "import type { Ref, Reactive, ShallowRef } from 'vue'" src/stores/modules/wallpaper/actions.ts` 返回退出码 0
- `grep -q "totalPageData: ShallowRef<TotalPageData>" src/stores/modules/wallpaper/actions.ts` 返回退出码 0
- `grep -v "totalPageData: any" src/stores/modules/wallpaper/actions.ts | grep -q "totalPageData"` 返回退出码 0（确认 any 已被替换）
</acceptance_criteria>
</task>
</tasks>

<verification>
```bash
# 验证 ShallowRef 导入
grep "import type { Ref, Reactive, ShallowRef }" src/stores/modules/wallpaper/actions.ts

# 验证 any 类型已消除
grep "totalPageData: ShallowRef<TotalPageData>" src/stores/modules/wallpaper/actions.ts

# 确认没有其他 any 类型在这个参数上
grep "totalPageData: any" src/stores/modules/wallpaper/actions.ts && echo "FAIL: any still exists" || echo "PASS: any removed"

# TypeScript 编译检查
npm run typecheck

# 启动应用验证功能
npm run dev
# 测试：在线壁纸浏览、搜索、加载更多
```
</verification>

<success_criteria>
- `ShallowRef` 类型成功导入
- `totalPageData` 参数类型从 `any` 变更为 `ShallowRef<TotalPageData>`
- TypeScript 编译无错误
- 壁纸浏览、搜索、加载更多功能正常
</success_criteria>

<must_haves>
- 类型定义正确，与 store 实际使用方式一致
- 不改变任何运行时行为
- 保持与其他代码的兼容性
</must_haves>

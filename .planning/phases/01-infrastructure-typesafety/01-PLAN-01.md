---
phase: 1
plan: 1
type: execute
wave: 1
depends_on: []
files_modified: []
files_created:
  - src/types/domain/index.ts
  - src/types/api/index.ts
  - src/types/ipc/index.ts
autonomous: true
requirements: [ARCH-01]
---

<objective>
创建 `src/types/` 目录结构，按领域组织类型定义，为后续阶段提供类型基础设施。
</objective>

<tasks>
<task id="1" type="create">
<read_first>
- src/types/index.ts
</read_first>
<action>
创建目录 `src/types/domain/` 和文件 `src/types/domain/index.ts`，内容如下：

```typescript
/**
 * 领域类型统一导出
 * 阶段 1：创建空导出文件，后续阶段逐步迁移类型
 *
 * 计划迁移的类型：
 * - WallpaperItem
 * - WallpaperMeta
 * - PageData
 * - TotalPageData
 * - DownloadItem
 * - DownloadState
 * - AppSettings
 * - WallpaperFit
 */

// 阶段 1 仅创建目录结构，暂不迁移类型
// 后续阶段将逐步从 src/types/index.ts 迁移类型定义

export {}
```
</action>
<acceptance_criteria>
- `test -d src/types/domain && echo "exists"` 返回 "exists"
- `test -f src/types/domain/index.ts && echo "exists"` 返回 "exists"
- `grep -q "领域类型统一导出" src/types/domain/index.ts` 返回退出码 0
</acceptance_criteria>
</task>

<task id="2" type="create">
<read_first>
- src/types/index.ts
</read_first>
<action>
创建目录 `src/types/api/` 和文件 `src/types/api/index.ts`，内容如下：

```typescript
/**
 * API 相关类型统一导出
 * 阶段 1：创建空导出文件，后续阶段逐步迁移类型
 *
 * 计划迁移的类型：
 * - GetParams
 * - CustomParams
 * - WallpaperQuery
 * - WallpaperThumb
 * - SearchBarProps
 * - WallpaperListProps
 * - ResolutionLine
 * - RatioLine
 * - ColorLine
 */

// 阶段 1 仅创建目录结构，暂不迁移类型

export {}
```
</action>
<acceptance_criteria>
- `test -d src/types/api && echo "exists"` 返回 "exists"
- `test -f src/types/api/index.ts && echo "exists"` 返回 "exists"
- `grep -q "API 相关类型统一导出" src/types/api/index.ts` 返回退出码 0
</acceptance_criteria>
</task>

<task id="3" type="create">
<read_first>
- src/types/index.ts
</read_first>
<action>
创建目录 `src/types/ipc/` 和文件 `src/types/ipc/index.ts`，内容如下：

```typescript
/**
 * IPC 相关类型统一导出（渲染进程专用）
 * 阶段 1：创建空导出文件，后续阶段逐步迁移类型
 *
 * 注意：主进程和渲染进程共享的类型定义在 src/shared/types/ipc.ts
 *
 * 计划迁移的类型：
 * - ElectronAPI 接口（从 preload 迁移）
 * - 各 IPC 通道的请求/响应类型
 */

// 阶段 1 仅创建目录结构，暂不迁移类型

export {}
```
</action>
<acceptance_criteria>
- `test -d src/types/ipc && echo "exists"` 返回 "exists"
- `test -f src/types/ipc/index.ts && echo "exists"` 返回 "exists"
- `grep -q "IPC 相关类型统一导出" src/types/ipc/index.ts` 返回退出码 0
</acceptance_criteria>
</task>
</tasks>

<verification>
```bash
# 验证目录结构
ls -la src/types/domain/
ls -la src/types/api/
ls -la src/types/ipc/

# 验证文件内容
head -5 src/types/domain/index.ts
head -5 src/types/api/index.ts
head -5 src/types/ipc/index.ts

# TypeScript 编译检查
npm run typecheck
```
</verification>

<success_criteria>
- 三个目录全部创建成功
- 三个 index.ts 文件内容符合预期
- TypeScript 编译无错误
- 现有代码功能不受影响
</success_criteria>

<must_haves>
- 目录结构存在且可被 TypeScript 解析
- 不修改现有 src/types/index.ts 文件（保持向后兼容）
- 为后续阶段的类型迁移做好准备
</must_haves>

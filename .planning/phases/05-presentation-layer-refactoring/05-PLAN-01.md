---
wave: 1
depends_on: []
files_modified:
  - src/components/ErrorBoundary.vue
  - src/App.vue
autonomous: true
requirements: [UI-01]
---

# Plan 1: Create ErrorBoundary Component

## Objective

Create a Vue 3 ErrorBoundary component to isolate component-level errors and prevent full app crashes.

## Tasks

### Task 1.1: Create ErrorBoundary.vue component

<read_first>
- src/App.vue (to understand App structure for integration)
- src/components/Alert.vue (for consistent styling patterns)
- src/views/OnlineWallpaper.vue (lines 38-52 for error-container pattern reference)
</read_first>

<action>
Create new file `src/components/ErrorBoundary.vue` with the following implementation:

1. Template structure:
   - If no error: render default slot content
   - If error: show fallback UI with error message and retry button
   - Style to match existing `error-container` class (dark background, centered, rounded corners)

2. Script setup:
   - Use `errorCaptured` lifecycle hook to catch child component errors
   - Maintain `error` and `errorInfo` reactive state
   - Provide `resetError()` method to clear error and retry
   - Expose slots for custom fallback UI

3. Error captured logic:
   - `errorCaptured(err, instance, info)` - set error state, log to console
   - Return `false` to prevent error from propagating further

4. Exact implementation pattern:
```vue
<script setup lang="ts">
import { ref, onErrorCaptured, type Ref } from 'vue'

interface ErrorInfo {
  message: string
  component: string | null
}

const error: Ref<Error | null> = ref(null)
const errorInfo: Ref<ErrorInfo> = ref({ message: '', component: null })

onErrorCaptured((err, instance, info) => {
  error.value = err
  errorInfo.value = {
    message: err.message,
    component: instance?.$options?.name || null
  }
  console.error('[ErrorBoundary] Captured error:', err, info)
  return false // Prevent error from propagating
})

const resetError = () => {
  error.value = null
  errorInfo.value = { message: '', component: null }
}
</script>
```
</action>

<acceptance_criteria>
- File `src/components/ErrorBoundary.vue` exists
- File contains `onErrorCaptured` hook
- File contains `resetError` function
- File contains `return false` in errorCaptured handler
- File exports component with proper Vue 3 script setup syntax
</acceptance_criteria>

---

### Task 1.2: Integrate ErrorBoundary into App.vue

<read_first>
- src/App.vue (current structure)
- src/Main.vue (to understand router-view location)
</read_first>

<action>
Modify `src/App.vue` to wrap Main component with ErrorBoundary:

1. Add import:
```typescript
import ErrorBoundary from '@/components/ErrorBoundary.vue'
```

2. Update template to:
```vue
<template>
  <ErrorBoundary>
    <div>
      <PageHeader title="" />
      <Main />
    </div>
  </ErrorBoundary>
</template>
```

3. Keep existing styles unchanged
</action>

<acceptance_criteria>
- `src/App.vue` contains `import ErrorBoundary from '@/components/ErrorBoundary.vue'`
- `src/App.vue` template contains `<ErrorBoundary>` wrapper around existing content
- `src/App.vue` template contains `</ErrorBoundary>` closing tag
- Original PageHeader and Main imports preserved
</acceptance_criteria>

---

## Verification

After completing tasks:

1. Run `npm run dev` - app should start without errors
2. ErrorBoundary component should wrap all page content
3. Console should show no TypeScript errors

## Notes

- ErrorBoundary uses Vue 3's `onErrorCaptured` lifecycle hook (equivalent to React's error boundaries)
- The `return false` prevents errors from propagating to parent components
- Global error handler (from Phase 1) continues to handle uncaught Promise rejections

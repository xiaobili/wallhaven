---
wave: 2
depends_on: [05-PLAN-01]
files_modified:
  - src/views/OnlineWallpaper.vue
autonomous: true
requirements: [UI-02, UI-06]
---

# Plan 2: Refactor OnlineWallpaper.vue

## Objective

Migrate OnlineWallpaper.vue to use the `useAlert` composable and remove duplicate inline Alert state management.

## Tasks

### Task 2.1: Replace inline Alert state with useAlert composable

<read_first>
- src/views/OnlineWallpaper.vue (current implementation)
- src/composables/core/useAlert.ts (composable to use)
</read_first>

<action>
Modify `src/views/OnlineWallpaper.vue`:

1. Update imports - add useAlert:
```typescript
import { useWallpaperList, useDownload, useSettings, useAlert } from '@/composables'
```

2. Remove inline Alert state (DELETE these lines ~106-111):
```typescript
// DELETE THIS:
const alert = reactive({
  visible: false,
  type: 'info' as 'success' | 'error' | 'warning' | 'info',
  message: '',
  duration: 3000
})
```

3. Remove inline showAlert function (DELETE these lines ~114-123):
```typescript
// DELETE THIS:
const showAlert = (
  message: string,
  type: 'success' | 'error' | 'warning' | 'info' = 'info',
  duration: number = 3000
) => {
  alert.message = message
  alert.type = type
  alert.duration = duration
  alert.visible = true
}
```

4. Add useAlert composable usage after other composables:
```typescript
// Composables
const { fetch: fetchWallpapers, loadMore: loadMoreWallpapers, loadSavedParams } = useWallpaperList()
const { addTask, startDownload, loadHistory, isDownloading } = useDownload()
const { update: updateSettings } = useSettings()
const { alert, showAlert, showSuccess, showError, hideAlert } = useAlert()
```

5. Update template Alert usage - change `@close="alert.visible = false"` to `@close="hideAlert"`:
```vue
<Alert
  v-if="alert.visible"
  :type="alert.type"
  :message="alert.message"
  :duration="alert.duration"
  @close="hideAlert"
/>
```

6. Update `reactive` import - remove it if no longer used:
```typescript
// Change from:
import { computed, onMounted, onUnmounted, reactive, ref, shallowRef } from 'vue'
// To:
import { computed, onMounted, onUnmounted, ref, shallowRef } from 'vue'
```

7. Replace any `showAlert('✅ ...', 'success')` calls with `showSuccess('...')`:
   - Line ~147: `showAlert('参数已保存', 'success')` → `showSuccess('参数已保存')`
   - Line ~214: `showAlert('✅ 已添加 ...', 'success')` → `showSuccess('已添加 ...')`
   - Line ~252: `showAlert('✅ 壁纸设置成功！', 'success')` → `showSuccess('壁纸设置成功！')`
   - Line ~266: `showAlert('✅ 已添加到下载队列...', 'success')` → `showSuccess('已添加到下载队列，请在下载中心查看进度')`

8. Replace error alerts with `showError()`:
   - Line ~177: `showAlert('请先选择要下载的壁纸', 'warning')` → `showWarning('请先选择要下载的壁纸')`
   - Line ~199: `showAlert('未找到选中的壁纸信息', 'error')` → `showError('未找到选中的壁纸信息')`
   - Line ~244: `showAlert('下载壁纸失败: ...', 'error')` → `showError('下载壁纸失败: ...')`
   - Line ~254: `showAlert('设置壁纸失败: ...', 'error')` → `showError('设置壁纸失败: ...')`
   - Line ~269: `showAlert('添加下载任务失败: ...', 'error')` → `showError('添加下载任务失败: ...')`
</action>

<acceptance_criteria>
- `src/views/OnlineWallpaper.vue` contains `import { useWallpaperList, useDownload, useSettings, useAlert } from '@/composables'`
- `src/views/OnlineWallpaper.vue` contains `const { alert, showAlert, showSuccess, showError, hideAlert } = useAlert()`
- `src/views/OnlineWallpaper.vue` does NOT contain inline `const alert = reactive({...})`
- `src/views/OnlineWallpaper.vue` template contains `@close="hideAlert"` on Alert component
- `src/views/OnlineWallpaper.vue` contains at least one `showSuccess(` call
- `src/views/OnlineWallpaper.vue` contains at least one `showError(` call
- `src/views/OnlineWallpaper.vue` contains at least one `showWarning(` call
</acceptance_criteria>

---

## Verification

After completing tasks:

1. Run `npm run dev` - app should compile without TypeScript errors
2. Navigate to online wallpaper page - Alert functionality should work
3. Test search and download - success/error messages should display correctly

## Notes

- The `useAlert` composable already exists from Phase 1 - just need to import and use it
- Template Alert binding remains the same structure, only the close handler changes
- All existing functionality must be preserved - only the implementation changes

---
wave: 2
depends_on: [05-PLAN-01]
files_modified:
  - src/views/DownloadWallpaper.vue
autonomous: true
requirements: [UI-04, UI-06]
---

# Plan 4: Refactor DownloadWallpaper.vue

## Objective

Migrate DownloadWallpaper.vue to use the `useAlert` composable and remove duplicate inline Alert state management.

## Tasks

### Task 4.1: Replace inline Alert state with useAlert composable

<read_first>
- src/views/DownloadWallpaper.vue (current implementation)
- src/composables/core/useAlert.ts (composable to use)
</read_first>

<action>
Modify `src/views/DownloadWallpaper.vue`:

1. Update imports - add useAlert:
```typescript
import { computed, onMounted, onUnmounted, reactive } from 'vue'
import { useDownloadStore } from '@/stores/modules/download'
import { useDownload, useAlert } from '@/composables'
import { formatFileSize, formatResolution, formatSpeed, formatTime } from '@/utils/helpers'
import Alert from '@/components/Alert.vue'
```

2. Remove inline Alert state (DELETE these lines ~113-118):
```typescript
// DELETE THIS:
const alert = reactive({
  visible: false,
  type: 'info' as 'success' | 'error' | 'warning' | 'info',
  message: '',
  duration: 3000
})
```

3. Remove inline showAlert function (DELETE these lines ~121-130):
```typescript
// DELETE THIS:
const showAlert = (
  message: string,
  type: 'success' | 'error' | 'warning' | 'info' = 'info',
  duration: number = 3000
) => {
  alert.message = message
  alert.type = type
  alert.duration = duration
  alert.visible = true
}
```

4. Add useAlert composable usage after other composables:
```typescript
// Composables
const { loadHistory, removeFinished } = useDownload()
const { alert, showSuccess, showError, showInfo, showWarning, hideAlert } = useAlert()
```

5. Update template Alert usage - change `@close="alert.visible = false"` to `@close="hideAlert"`:
```vue
<Alert
  v-if="alert.visible"
  :type="alert.type"
  :message="alert.message"
  :duration="alert.duration"
  @close="hideAlert"
/>
```

6. Update `reactive` import - remove it if no longer used:
```typescript
// Change from:
import { computed, onMounted, onUnmounted, reactive } from 'vue'
// To:
import { computed, onMounted, onUnmounted } from 'vue'
```

7. Replace alert calls with appropriate methods:
   - Line ~142: `showAlert('已取消下载', 'info')` → `showInfo('已取消下载')`
   - Line ~149: `showAlert('已暂停下载', 'info')` → `showInfo('已暂停下载')`
   - Line ~155: `showAlert('恢复下载...', 'info')` → `showInfo('恢复下载...')`
   - Line ~163: `showAlert('记录已删除', 'success')` → `showSuccess('记录已删除')`
   - Line ~178: `showAlert('打开文件夹失败: ...', 'error')` → `showError('打开文件夹失败: ...')`
   - Line ~181: `showAlert('请在Electron环境中使用此功能', 'warning')` → `showWarning('请在Electron环境中使用此功能')`
   - Line ~186: `showAlert('打开文件夹失败', 'error')` → `showError('打开文件夹失败')`
</action>

<acceptance_criteria>
- `src/views/DownloadWallpaper.vue` contains `import { useDownload, useAlert } from '@/composables'`
- `src/views/DownloadWallpaper.vue` contains `const { alert, showSuccess, showError, showInfo, showWarning, hideAlert } = useAlert()`
- `src/views/DownloadWallpaper.vue` does NOT contain inline `const alert = reactive({...})`
- `src/views/DownloadWallpaper.vue` template contains `@close="hideAlert"` on Alert component
- `src/views/DownloadWallpaper.vue` contains at least one `showInfo(` call
- `src/views/DownloadWallpaper.vue` contains at least one `showSuccess(` call
- `src/views/DownloadWallpaper.vue` contains at least one `showError(` call
</acceptance_criteria>

---

## Verification

After completing tasks:

1. Run `npm run dev` - app should compile without TypeScript errors
2. Navigate to download center page - Alert functionality should work
3. Test pause/cancel/delete operations - messages should display correctly

## Notes

- The `useAlert` composable already exists from Phase 1
- DownloadWallpaper.vue uses all four alert types (success, error, warning, info)
- No business logic changes - only Alert state management refactoring

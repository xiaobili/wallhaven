---
wave: 2
depends_on: [05-PLAN-01]
files_modified:
  - src/views/SettingPage.vue
autonomous: true
requirements: [UI-05, UI-06]
---

# Plan 5: Refactor SettingPage.vue

## Objective

Migrate SettingPage.vue to use the `useAlert` composable and remove duplicate inline Alert state management.

## Tasks

### Task 5.1: Replace inline Alert state with useAlert composable

<read_first>
- src/views/SettingPage.vue (current implementation)
- src/composables/core/useAlert.ts (composable to use)
</read_first>

<action>
Modify `src/views/SettingPage.vue`:

1. Update imports - add useAlert:
```typescript
import { reactive, toRaw, ref } from 'vue'
import { useWallpaperStore } from '@/stores/wallpaper'
import { useSettings, useAlert } from '@/composables'
import type { WallpaperFit } from '@/types'
import Alert from '@/components/Alert.vue'
```

2. Remove inline Alert state (DELETE these lines ~157-162):
```typescript
// DELETE THIS:
const alert = reactive({
  visible: false,
  type: 'info' as 'success' | 'error' | 'warning' | 'info',
  message: '',
  duration: 3000
})
```

3. Remove inline showAlert function (DELETE these lines ~174-183):
```typescript
// DELETE THIS:
const showAlert = (
  message: string,
  type: 'success' | 'error' | 'warning' | 'info' = 'info',
  duration: number = 3000
) => {
  alert.message = message
  alert.type = type
  alert.duration = duration
  alert.visible = true
}
```

4. Add useAlert composable usage after other composables:
```typescript
// Composables
const { update: updateSettings, reset: resetSettingsComposable } = useSettings()
const { alert, showSuccess, showError, showWarning, hideAlert } = useAlert()
```

5. Update template Alert usage - change `@close="alert.visible = false"` to `@close="hideAlert"`:
```vue
<Alert
  v-if="alert.visible"
  :type="alert.type"
  :message="alert.message"
  :duration="alert.duration"
  @close="hideAlert"
/>
```

6. Note: The `reactive` import is still needed for `cacheInfo` reactive object - do NOT remove it.

7. Replace alert calls with appropriate methods:
   - Line ~192: `showAlert('❌ Electron API 未加载...', 'error', 5000)` → `showError('Electron API 未加载\n\n可能原因：\n1. 应用未在 Electron 环境中运行\n2. Preload 脚本加载失败\n\n请检查控制台日志')` (note: showError defaults to 5000ms)
   - Line ~213: `showAlert('选择文件夹失败: ...', 'error')` → `showError('选择文件夹失败: ...')`
   - Line ~220: `showAlert('多线程下载数量必须在 1-10 之间', 'warning')` → `showWarning('多线程下载数量必须在 1-10 之间')`
   - Line ~233: `showAlert('✅ 设置已保存', 'success')` → `showSuccess('设置已保存')`
   - Line ~236: `showAlert('保存设置失败: ...', 'error')` → `showError('保存设置失败: ...')`
   - Line ~259: `showAlert('✅ 已恢复默认设置', 'success')` → `showSuccess('已恢复默认设置')`
   - Line ~294: `showAlert('❌ Electron API 未加载', 'error')` → `showError('Electron API 未加载')`
   - Line ~339-341: Replace the complex message with showSuccess for cache clear
   - Line ~346-347: `showAlert('⚠️ 部分缓存清理失败...', 'warning', 8000)` → `showWarning('部分缓存清理失败\n' + cacheResult.errors.join('\n'))`
   - Line ~350: `showAlert('❌ 清空缓存失败: ...', 'error')` → `showError('清空缓存失败: ...')`
</action>

<acceptance_criteria>
- `src/views/SettingPage.vue` contains `import { useSettings, useAlert } from '@/composables'`
- `src/views/SettingPage.vue` contains `const { alert, showSuccess, showError, showWarning, hideAlert } = useAlert()`
- `src/views/SettingPage.vue` does NOT contain inline `const alert = reactive({...})` for Alert state
- `src/views/SettingPage.vue` template contains `@close="hideAlert"` on Alert component
- `src/views/SettingPage.vue` contains at least one `showSuccess(` call
- `src/views/SettingPage.vue` contains at least one `showError(` call
- `src/views/SettingPage.vue` contains at least one `showWarning(` call
- `src/views/SettingPage.vue` still contains `const cacheInfo = reactive({...})` (this is NOT the alert state)
</acceptance_criteria>

---

## Verification

After completing tasks:

1. Run `npm run dev` - app should compile without TypeScript errors
2. Navigate to settings page - Alert functionality should work
3. Test save settings and cache clear - messages should display correctly

## Notes

- The `reactive` import must be kept for `cacheInfo` object
- SettingPage.vue has the most alert usage of all components
- The showError default duration of 5000ms is appropriate for error messages

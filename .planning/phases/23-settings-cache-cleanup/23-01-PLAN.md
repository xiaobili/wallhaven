---
phase: 23
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/views/SettingPage.vue
autonomous: true
requirements: []
---

<objective>
Modify the clearCache function in SettingPage.vue to only delete thumbnails and temp files, preserving user settings, favorites, and download history.
</objective>

<read_first>
Files the executor MUST read before starting:
- `src/views/SettingPage.vue` — (reason: the file being modified, need to see current clearCache implementation)
- `.planning/phases/23-settings-cache-cleanup/23-RESEARCH.md` — (reason: detailed analysis of the bug and fix approach)
</read_first>

<tasks>
### Task 1: Remove clearStore() call and related logic from clearCache function

**Type:** execute

**Files:**
- `src/views/SettingPage.vue` (modify)

**Action:**
1. Locate the `clearCache` function (starts at line 296)
2. Remove lines 320-331 which contain:
   - Line 320: Comment `// 2. 清空 Store 数据`
   - Lines 321-325: The `clearStore()` call and result handling
   - Lines 327-331: The `load()` and `startEdit()` calls for reloading settings after store clear

The code to REMOVE:
```typescript
    // 2. 清空 Store 数据
    const storeResult = await settingsService.clearStore()

    if (!storeResult.success) {
      console.warn('[SettingPage] Store clear failed:', storeResult.error?.message)
    }

    // 3. 重新从存储加载设置（Store 已被清空，需要重新加载默认值）
    await load()

    // 4. 刷新本地可编辑副本
    startEdit()
```

3. Renumber the comments after removal:
   - Change `// 5. 更新缓存信息` to `// 2. 更新缓存信息`
   - Change `// 6. 显示成功消息` to `// 3. 显示成功消息`

**Verify:**
After changes:
- `grep -n "clearStore" src/views/SettingPage.vue` should return no matches
- `grep -n "await load()" src/views/SettingPage.vue` should return no matches within clearCache function

<acceptance_criteria>
- `src/views/SettingPage.vue` contains NO `clearStore()` call in the `clearCache` function
- The `clearCache` function no longer calls `load()` or `startEdit()` after clearing cache
- The function only calls `settingsService.clearAppCache()` and updates local cacheInfo state
</acceptance_criteria>

### Task 2: Update confirmation dialog message in clearCache function

**Type:** execute

**Files:**
- `src/views/SettingPage.vue` (modify)

**Action:**
1. Locate the confirmation dialog in `clearCache` function (lines 297-304)
2. Replace the current message with the updated version that removes the misleading "设置将被重置" text

Replace this code (lines 297-304):
```typescript
  const confirmed = window.confirm(
    '确定要清空应用缓存吗？\n\n' +
    '这将删除：\n' +
    '• 缩略图缓存（下次访问时会重新生成）\n' +
    '• 下载临时文件\n' +
    '• 应用存储数据（设置将被重置）\n\n' +
    '注意：不会删除已下载的壁纸文件。'
  )
```

With this updated code:
```typescript
  const confirmed = window.confirm(
    '确定要清空应用缓存吗？\n\n' +
    '这将删除：\n' +
    '• 缩略图缓存（下次访问时会重新生成）\n' +
    '• 下载临时文件\n\n' +
    '注意：不会删除已下载的壁纸文件和您的设置。'
  )
```

**Verify:**
After changes:
- `grep -n "设置将被重置" src/views/SettingPage.vue` should return no matches
- `grep -n "不会删除已下载的壁纸文件和您的设置" src/views/SettingPage.vue` should return one match

<acceptance_criteria>
- `src/views/SettingPage.vue` confirmation dialog does NOT contain "设置将被重置" text
- `src/views/SettingPage.vue` confirmation dialog contains "不会删除已下载的壁纸文件和您的设置" text
- The confirmation dialog accurately describes that only thumbnails and temp files will be deleted
</acceptance_criteria>

### Task 3: Update cache management section description text

**Type:** execute

**Files:**
- `src/views/SettingPage.vue` (modify)

**Action:**
1. Locate the cache management section description (lines 131-136)
2. Update the description text to remove mention of "应用存储数据"

Replace this code (lines 131-136):
```html
          <p
            class="setting-hint"
            style="margin-bottom: 1em;"
          >
            清理应用产生的缓存数据，包括缩略图、临时文件和应用存储数据。<br>
            注意：清理后缩略图会在下次访问时重新生成，不会影响已下载的壁纸文件。
          </p>
```

With this updated code:
```html
          <p
            class="setting-hint"
            style="margin-bottom: 1em;"
          >
            清理应用产生的缓存数据，包括缩略图和临时文件。<br>
            注意：清理后缩略图会在下次访问时重新生成，不会影响已下载的壁纸文件和您的设置。
          </p>
```

**Verify:**
After changes:
- `grep -n "应用存储数据" src/views/SettingPage.vue` should return no matches

<acceptance_criteria>
- `src/views/SettingPage.vue` cache management description does NOT contain "应用存储数据" text
- The description mentions that settings will not be affected
</acceptance_criteria>
</tasks>

<verification>
Manual verification steps:
1. Open Settings page
2. Verify the cache management section description says "包括缩略图和临时文件" (not "应用存储数据")
3. Click "清空缓存" button
4. Verify confirmation dialog shows:
   - "缩略图缓存（下次访问时会重新生成）"
   - "下载临时文件"
   - Does NOT show "应用存储数据（设置将被重置）"
   - Shows "不会删除已下载的壁纸文件和您的设置"
5. Confirm and verify only thumbnails and temp files are deleted
6. Verify settings remain intact (API key, download path, etc.)
</verification>

<success_criteria>
- Clear cache only deletes thumbnails and temp files
- User settings (API key, download path, wallpaper fit) remain intact after clearing cache
- Confirmation dialog accurately describes what will be deleted
- Cache management section description accurately describes the feature
</success_criteria>

<must_haves>
<truths>
- User data (settings, favorites, history) must never be deleted by cache clear
- Confirmation dialog must accurately describe what will be deleted
- The clearAppCache IPC handler already correctly deletes only thumbnails and temp files
</truths>
<features>
- Clear cache button removes only regenerable files (thumbnails, temp files)
- Settings page description matches actual behavior
- Users can clear cache without fear of losing their preferences
</features>
</must_haves>

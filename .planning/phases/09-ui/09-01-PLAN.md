---
phase: 09-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/composables/local/useLocalFiles.ts
  - src/components/LocalWallpaperMain.vue
  - src/views/LocalWallpaper.vue
autonomous: true
requirements: [PAG-03, PAG-04]
must_haves:
  truths:
    - "用户能在壁纸网格下方看到分页导航栏，显示当前页码、总页数和总壁纸数"
    - "用户点击页码时切换到对应页的壁纸列表"
    - "切换到之前访问过的页面时，数据从缓存读取，无网络/文件系统等待"
    - "点击刷新按钮时清除所有缓存并重置到第 1 页"
    - "图片预览的 prev/next 导航只遍历当前页的壁纸，不会超出当前页范围"
  artifacts:
    - path: src/composables/local/useLocalFiles.ts
      provides: "goToPage 方法、clearCache 方法、页面缓存 Map"
      exports: ["goToPage", "clearCache", "currentPage", "totalPages", "total"]
    - path: src/components/LocalWallpaperMain.vue
      provides: "PaginationBar 渲染位、分页 props 接收、go-to-page emit 转发"
      contains: "PaginationBar 组件引用"
    - path: src/views/LocalWallpaper.vue
      provides: "分页状态透传、go-to-page 事件处理、刷新时清除缓存"
      contains: "goToPage 调用链"
  key_links:
    - from: LocalWallpaper.vue (handleGoToPage)
      to: useLocalFiles.goToPage
      via: "await goToPage(downloadPath, page)"
      pattern: "goToPage"
    - from: LocalWallpaperMain.vue (template)
      to: PaginationBar
      via: component prop binding
      pattern: "PaginationBar"
    - from: LocalWallpaperMain.vue (go-to-page emit)
      to: LocalWallpaper.vue (handleGoToPage)
      via: "@go-to-page=\"handleGoToPage\""
      pattern: "go-to-page"
    - from: LocalWallpaper.vue (refreshList)
      to: useLocalFiles (clearCache + goToPage)
      via: "clearCache() + goToPage(downloadPath, 1)"
      pattern: "clearCache"

---
<objective>
为 `LocalWallpaper.vue` 集成 `PaginationBar` 组件，实现页码导航和页面缓存。

**Purpose:** 用户可以在本地壁纸列表下方看到分页控件，通过点击页码浏览不同页的壁纸。切换页面时缓存当前页数据，返回已访问页面时直接从缓存读取，减少重复文件系统读取。

**Output:**
- `useLocalFiles` composable 新增 `goToPage()` 和 `clearCache()` 方法及页面缓存
- `LocalWallpaperMain.vue` 底部渲染 `PaginationBar`，接收分页 props 并转发 `go-to-page` emit
- `LocalWallpaper.vue` 串联分页状态，管理页面切换和数据加载
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/billy/Code/wallhaven/.planning/phases/09-ui/09-CONTEXT.md
@/home/billy/Code/wallhaven/.planning/ROADMAP.md

<interfaces>
从 PaginationBar.vue (已有组件):

```typescript
interface PaginationBarProps {
  currentPage: number   // 1-based
  totalPages: number
  totalCount: number
  loading?: boolean
}

interface PaginationBarEmits {
  (e: 'go-to-page', page: number): void
}
```

从 LocalWallpaperMain.vue (当前接口):

```typescript
export interface LocalWallpaper {
  name: string
  path: string
  thumbnailPath?: string
  size: number
  modifiedTime: string
  width?: number
  height?: number
}

interface CurrentProps {
  localWallpapers: LocalWallpaper[]
  loading: boolean
  downloadPath: string
}

interface CurrentEmits {
  refresh: []
  'open-folder': []
  'set-wallpaper': [wallpaper: LocalWallpaper]
  'delete-wallpaper': [wallpaper: LocalWallpaper, index: number]
  preview: [wallpaper: LocalWallpaper]
  'image-error': [index: number]
}
```

从 useLocalFiles.ts (当前返回接口):

```typescript
interface UseLocalFilesReturn {
  readDirectory: (dirPath: string, page?: number, pageSize?: number) => Promise<IpcResponse<LocalFile[]>>
  openFolder: (folderPath: string) => Promise<IpcResponse<void>>
  deleteFile: (filePath: string) => Promise<IpcResponse<void>>
  currentPage: Ref<number>
  totalPages: ComputedRef<number>
  pageSize: Ref<number>
  total: Ref<number>
}
```

从 src/types/ipc.ts:

```typescript
interface PaginationMeta {
  total: number
  page: number
  pageSize: number
}

interface IpcResponse<T = unknown> {
  success: boolean
  data?: T
  error?: IpcErrorInfo
  pagination?: PaginationMeta
}
```
</interfaces>
</context>

<tasks>

<task type="modify">
  <name>Task 1: useLocalFiles — 新增 goToPage、clearCache 方法和页面缓存</name>
  <files>
    src/composables/local/useLocalFiles.ts
  </files>
  <action>
    在 `UseLocalFilesReturn` 接口和 `useLocalFiles()` 函数中实现以下修改：

    1. **新增导入:**
       - `import type { LocalWallpaper } from '@/components/LocalWallpaperMain.vue'`
       - (LocalWallpaper 来自 `src/components/LocalWallpaperMain.vue` 的具名导出)

    2. **扩展 `UseLocalFilesReturn` 接口，新增三个成员:**
       ```typescript
       /** 跳转到指定页面（支持缓存） */
       goToPage: (dirPath: string, page: number) => Promise<LocalWallpaper[]>
       /** 清除页面缓存 */
       clearCache: () => void
       /** 当前页的壁纸列表（给 preview 导航使用，限制在当前页） */
       localWallpapers: import('vue').Ref<LocalWallpaper[]>
       ```

       注意 D-08: `localWallpapers` 用于 ImagePreview 的 prev/next 导航。实现后，`LocalWallpaper.vue` 的 `wallpaperList` computed 将基于此 ref 计算，使得导航限制在当前页范围。

    3. **在函数体内新增内部状态:**
       ```typescript
       /** 页面缓存: Map<页码, LocalWallpaper[]> */
       const pageCache = new Map<number, LocalWallpaper[]>()
       const localWallpapers = ref<LocalWallpaper[]>([])
       ```

    4. **实现 `clearCache` 方法 (per D-06, D-07):**
       ```typescript
       const clearCache = (): void => {
         pageCache.clear()
         localWallpapers.value = []
         currentPage.value = 1
       }
       ```
       - 清除整个缓存 Map
       - 清空 localWallpapers
       - 重置 currentPage 到 1

    5. **实现 `goToPage` 方法 (per D-04, D-05):**

       ```typescript
       const goToPage = async (dirPath: string, page: number): Promise<LocalWallpaper[]> => {
         // 缓存命中：直接返回缓存数据
         if (pageCache.has(page)) {
           currentPage.value = page
           localWallpapers.value = pageCache.get(page)!
           return localWallpapers.value
         }

         // 缓存未命中：调用 readDirectory 获取数据
         const result = await readDirectory(dirPath, page, pageSize.value)

         if (!result.success || !result.data) {
           localWallpapers.value = []
           return []
         }

         // 映射 LocalFile[] 到 LocalWallpaper[]
         const mapped: LocalWallpaper[] = result.data.map((file) => ({
           name: file.name,
           path: file.path,
           thumbnailPath: file.thumbnailPath || '',
           size: file.size,
           modifiedTime: new Date(file.modifiedAt).toISOString(),
           width: file.width,
           height: file.height,
         }))

         // 存入缓存
         pageCache.set(page, mapped)
         localWallpapers.value = mapped

         return mapped
       }
       ```

       - `currentPage` 已在 `readDirectory` 内部更新（从 `result.pagination.page`）
       - 映射逻辑与原 `LocalWallpaper.vue` 的 `refreshList` 保持一致

    6. **更新 return 语句:**
       ```typescript
       return {
         readDirectory,
         openFolder,
         deleteFile,
         currentPage,
         totalPages,
         pageSize,
         total,
         goToPage,
         clearCache,
         localWallpapers,
       }
       ```

    **不修改:**
    - 不修改 `readDirectory` 方法的现有逻辑
    - 不修改 `openFolder` 和 `deleteFile` 方法
    - 不改变现有分页状态更新机制（readDirectory 已自动更新分页状态）
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - UseLocalFilesReturn 接口新增 goToPage、clearCache、localWallpapers
    - goToPage 方法支持缓存检查（Map 命中直接返回）
    - goToPage 在缓存未命中时调用 readDirectory 获取数据并存入缓存
    - clearCache 清除 Map 并重置到第 1 页
    - 类型检查通过
  </done>
</task>

<task type="modify">
  <name>Task 2: LocalWallpaperMain — 添加 PaginationBar 渲染和分页 props</name>
  <files>
    src/components/LocalWallpaperMain.vue
  </files>
  <action>
    1. **新增组件导入 (script setup):**
       ```typescript
       import PaginationBar from '@/components/PaginationBar.vue'
       ```

    2. **扩展 Props 接口 (per D-02):**

       在现有 `interface Props` 中添加三个新字段:
       ```typescript
       interface Props {
         localWallpapers: LocalWallpaper[]
         loading: boolean
         downloadPath: string
         /** 当前页码（从 1 开始） */
         currentPage: number
         /** 总页数 */
         totalPages: number
         /** 总壁纸数量 */
         totalCount: number
       }
       ```

       在 `withDefaults` 中添加默认值:
       ```typescript
       const props = withDefaults(defineProps<Props>(), {
         localWallpapers: () => [],
         loading: false,
         downloadPath: '',
         currentPage: 1,
         totalPages: 1,
         totalCount: 0,
       })
       ```

    3. **扩展 Emits 接口 (per D-03):**

       在 `defineEmits` 中添加:
       ```typescript
       defineEmits<{
         refresh: []
         'open-folder': []
         'set-wallpaper': [wallpaper: LocalWallpaper]
         'delete-wallpaper': [wallpaper: LocalWallpaper, index: number]
         preview: [wallpaper: LocalWallpaper]
         'image-error': [index: number]
         'go-to-page': [page: number]
       }>()
       ```

    4. **在 template 中添加 PaginationBar (per D-01):**

       在 `<main class="local-wallpaper-page">` 内部，现有 `<div v-if="localWallpapers.length > 0">` 块的 `</section>` 闭合标签之后（即 thumb-listing-page 之后），但在该 div 的闭合之前：

       ```html
         <!-- 分页控件 -->
         <PaginationBar
           :current-page="currentPage"
           :total-pages="totalPages"
           :total-count="totalCount"
           :loading="loading"
           @go-to-page="(page: number) => $emit('go-to-page', page)"
         />
       ```

       **渲染位置说明:** PaginationBar 放在 `thumbs-container` div 内部、`thumb-listing-page section` 之后、`</div>` 闭合之前。这样分页控件在壁纸网格正下方，与网格在同一个容器内。

    5. **不需要修改任何 CSS 样式** — PaginationBar 已有自己的样式，无需额外调整。

    **不修改:**
    - 不修改现有的 grid/card 布局
    - 不修改现有的按钮和工具栏
    - 不修改 `LocalWallpaper` 类型定义
    - 不修改 `getImageUrl`、`formatDate`、`formatFileSize` 等工具方法
    - 不修改任何 CSS
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - LocalWallpaperMain props 接口新增 currentPage/totalPages/totalCount
    - LocalWallpaperMain emits 接口新增 go-to-page
    - PaginationBar 在 thumb-listing-page 之后渲染
    - PaginationBar 接收正确的 props 并转发 go-to-page emit
    - 类型检查通过
  </done>
</task>

<task type="modify">
  <name>Task 3: LocalWallpaper.vue — 串联分页状态，集成 goToPage 和缓存</name>
  <files>
    src/views/LocalWallpaper.vue
  </files>
  <action>
    1. **更新 `useLocalFiles` 解构 (per D-02, D-05, D-07):**

       从:
       ```typescript
       const { readDirectory, openFolder: openFolderAction, deleteFile } = useLocalFiles()
       ```
       改为:
       ```typescript
       const {
         readDirectory,
         openFolder: openFolderAction,
         deleteFile,
         goToPage,
         clearCache,
         currentPage,
         totalPages,
         total,
         localWallpapers: composableLocalWallpapers,
       } = useLocalFiles()
       ```

       - 重命名 `localWallpapers` 为 `composableLocalWallpapers` 以避免与组件本地 ref 冲突
       - 注意：当前组件已有 `const localWallpapers = ref<LocalWallpaper[]>([])`

    2. **移除局部 `localWallpapers` ref 声明 (per D-08):**

       删除这一行:
       ```typescript
       const localWallpapers = ref<LocalWallpaper[]>([])
       ```
       因为 composable 现在通过 `localWallpapers` ref 管理当前页数据。
       改为将组件的响应式数据指向 composable:
       ```typescript
       const localWallpapers = composableLocalWallpapers
       ```

       **确保 `localWallpapers` 在 template 中的引用仍然有效** — 所有 template 中引用 `localWallpapers` 的地方（v-for、computed、方法等）将继续正常工作。

    3. **更新 `refreshList` 方法 (per D-06):**

       从:
       ```typescript
       const refreshList = async (): Promise<void> => {
         if (!downloadPath.value) {
           console.warn('未设置下载目录')
           return
         }
         loading.value = true
         try {
           const result = await readDirectory(downloadPath.value)
           if (!result.success || !result.data) {
             localWallpapers.value = []
             return
           }
           localWallpapers.value = result.data.map((file) => ({
             name: file.name,
             path: file.path,
             thumbnailPath: file.thumbnailPath || '',
             size: file.size,
             modifiedTime: new Date(file.modifiedAt).toISOString(),
             width: file.width,
             height: file.height,
           }))
           console.log(`已加载 ${localWallpapers.value.length} 张本地壁纸`)
         } catch (error) {
           console.error('读取本地壁纸失败:', error)
           localWallpapers.value = []
         } finally {
           loading.value = false
         }
       }
       ```

       改为:
       ```typescript
       const refreshList = async (): Promise<void> => {
         if (!downloadPath.value) {
           console.warn('未设置下载目录')
           return
         }
         loading.value = true
         try {
           clearCache()
           await goToPage(downloadPath.value, 1)
           console.log(`已加载 ${localWallpapers.value.length} 张本地壁纸`)
         } catch (error) {
           console.error('读取本地壁纸失败:', error)
           localWallpapers.value = []
         } finally {
           loading.value = false
         }
       }
       ```

       - `clearCache()` 清除缓存并重置当前页到第 1 页
       - `goToPage(downloadPath, 1)` 加载第 1 页，内部处理数据映射
       - `localWallpapers` 由 composable 管理，无需手动 map

    4. **新增 `handleGoToPage` 方法 (per D-03):**

       ```typescript
       const handleGoToPage = async (page: number): Promise<void> => {
         if (!downloadPath.value) return

         loading.value = true
         try {
           await goToPage(downloadPath.value, page)
         } catch (error) {
           console.error('切换页面失败:', error)
           localWallpapers.value = []
         } finally {
           loading.value = false
         }
       }
       ```

    5. **更新 template 中 LocalWallpaperMain 的绑定 (per D-02):**

       ```html
       <LocalWallpaperMain
         :local-wallpapers="localWallpapers"
         :loading="loading"
         :download-path="downloadPath"
         :current-page="currentPage"
         :total-pages="totalPages"
         :total-count="total"
         @refresh="refreshList"
         @open-folder="openFolder"
         @set-wallpaper="setAsWallpaper"
         @delete-wallpaper="deleteWallpaper"
         @preview="previewWallpaper"
         @image-error="handleImageError"
         @go-to-page="handleGoToPage"
       />
       ```

    6. **验证 `wallpaperList` computed 仍然正确 (per D-08):**

       `wallpaperList` computed 基于 `localWallpapers` 生成，而 `localWallpapers` 现在由 composable 管理（始终指向当前页数据）。ImagePreview 的 prev/next 导航（`handleNavigate` 方法中检查 `newIndex >= 0 && newIndex < localWallpapers.value.length`）自然限制在当前页内。

       - `refreshList` 和 `handleGoToPage` 都通过 `goToPage` 更新 `localWallpapers`
       - `deleteWallpaper` 中的 `localWallpapers.value.splice(index, 1)` 仍然有效（删除当前页中的项）
       - `handleImageError` 中的 `localWallpapers.value[index]` 仍然有效

    **不修改:**
    - 不修改 ImagePreview 组件或它的 props 传递
    - 不修改 `wallpaperList` computed（它已自动响应 localWallpapers 的变化）
    - 不修改 `handleNavigate` 方法（它已自然限制在当前页）
    - 不修改 `previewWallpaper`、`closePreview`、`setAsWallpaper`、`deleteWallpaper`、`handleImageError` 方法
    - 不修改 `getImageUrl`、`getImageType`、`decodeWallhavenUrl` 工具方法
    - 不修改 CSS 样式
    - 不改变现有网格/卡片布局
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - useLocalFiles 解构新增 goToPage、clearCache、currentPage、totalPages、total
    - refreshList 调用 clearCache + goToPage(1) 替代原来的 readDirectory + 手动 map
    - LocalWallpaperMain 收到正确的分页 props（currentPage/totalPages/totalCount）
    - LocalWallpaperMain 的 go-to-page emit 被 handleGoToPage 处理
    - localWallpapers 由 composable 管理，预览导航限制在当前页范围
    - 类型检查通过
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| goToPage -> readDirectory | 用户通过 PaginationBar 触发的页码输入可能超出范围，但 goToPage 内部 readDirectory 调用不涉及用户输入。PaginationBar 已有自己的输入验证。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | S (Spoofing) | PaginationBar page input | accept | PaginationBar 已有输入范围验证（min=1, max=totalPages），输入无效时重置为当前页 |
| T-09-02 | I (Info Disclosure) | pageCache Map | accept | 缓存仅存储 LocalWallpaper 元数据（文件名、路径、大小等），无非公开信息。路径已是用户在设置中配置的下载目录下的文件 |
| T-09-03 | E (Elevation) | goToPage IPC call | accept | goToPage 透传用户已配置的下载目录路径和页码，不引入新的 IPC 调用。已有 readDirectory 的 IPC 通道在 Phase 7 已验证 |
</threat_model>

<verification>

### 自动化验证
1. `npx tsc --noEmit` 无类型错误
2. `npx electron-vite build` 构建通过

### 手动验证
1. 打开本地壁纸页面，确认 PaginationBar 显示在壁纸网格下方
2. 确认 PaginationBar 显示正确的当前页码、总页数和总壁纸数
3. 点击第 2 页，确认壁纸列表更新为第 2 页内容
4. 点击返回第 1 页，确认数据从缓存读取（无加载等待）
5. 点击刷新按钮，确认缓存清除并重置回第 1 页
6. 打开图片预览，确认 prev/next 导航只在当前页面范围内

</verification>

<success_criteria>
1. PaginationBar 在 LocalWallpaperMain 组件底部正确渲染
2. 页码切换调用 useLocalFiles.goToPage，返回正确的页面数据
3. 页面缓存正常工作：返回已访问页面时直接从缓存读取，无加载状态
4. 刷新按钮清除所有缓存并重置到第 1 页
5. 图片预览导航限制在当前页范围内
6. `npx tsc --noEmit` 无类型错误
7. `npx electron-vite build` 构建通过
</success_criteria>

<output>
After completion, create `.planning/phases/09-ui/09-01-SUMMARY.md`
</output>

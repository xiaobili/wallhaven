---
phase: 08-data-flow-pagination
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/ipc.ts
  - env.d.ts
  - src/clients/file.client.ts
  - src/clients/index.ts
  - src/repositories/settings.repository.ts
  - src/services/settings.service.ts
  - src/composables/local/useLocalFiles.ts
autonomous: true
requirements:
  - PAG-02
must_haves:
  truths:
    - "settingsService.readDirectory 接受 page/pageSize 可选参数并传递到 repository"
    - "settingsRepository.readDirectory 接受 page/pageSize 可选参数并传递到 client"
    - "fileClient.readDirectory 接受 page/pageSize 可选参数并传递到 window.electronAPI"
    - "window.electronAPI.readDirectory 类型声明与 preload 实际签名一致"
    - "IpcResponse 支持 pagination 元数据字段（total/page/pageSize）"
    - "useLocalFiles composable 暴露响应式分页状态（currentPage/totalPages/pageSize/total）"
    - "readDirectory 调用后自动更新 composable 的分页状态"
    - "所有中间层不修改分页数据，仅透传参数和结果"
  artifacts:
    - path: src/types/ipc.ts
      provides: "PaginationMeta 接口 + IpcResponse 扩展（pagination 字段）"
      contains: "interface PaginationMeta"
    - path: env.d.ts
      provides: "ElectronAPI.readDirectory 签名匹配 preload 实现"
      pattern: "page.*pageSize.*total.*page.*pageSize"
    - path: src/clients/file.client.ts
      provides: "支持 page/pageSize 的 readDirectory 方法，返回 pagination 元数据"
      pattern: "pagination:"
    - path: src/clients/index.ts
      provides: "electronClient.readDirectory 转发 page/pageSize 到 fileClient"
      pattern: "readDirectory:.*page.*pageSize"
    - path: src/repositories/settings.repository.ts
      provides: "settingsRepository.readDirectory 接受 page/pageSize"
      pattern: "page\\?:.*number.*pageSize\\?:.*number"
    - path: src/services/settings.service.ts
      provides: "settingsService.readDirectory 接受 page/pageSize"
      pattern: "page\\?:.*number.*pageSize\\?:.*number"
    - path: src/composables/local/useLocalFiles.ts
      provides: "readDirectory 支持分页参数 + 响应式分页状态"
      contains: "currentPage.*Ref"
  key_links:
    - from: "src/clients/file.client.ts"
      to: "env.d.ts::ElectronAPI.readDirectory"
      via: "window.electronAPI.readDirectory"
      pattern: "window\\.electronAPI\\.readDirectory"
    - from: "src/clients/file.client.ts"
      to: "src/types/ipc.ts::IpcResponse"
      via: "return type with pagination"
      pattern: "pagination"
    - from: "src/clients/index.ts"
      to: "src/clients/file.client.ts::fileClient"
      via: "method delegation"
      pattern: "fileClient\\.readDirectory"
    - from: "src/repositories/settings.repository.ts"
      to: "src/clients/index.ts::electronClient"
      via: "method delegation"
      pattern: "electronClient\\.readDirectory"
    - from: "src/services/settings.service.ts"
      to: "src/repositories/settings.repository.ts::settingsRepository"
      via: "method delegation"
      pattern: "settingsRepository\\.readDirectory"
    - from: "src/composables/local/useLocalFiles.ts"
      to: "src/services/settings.service.ts::settingsService"
      via: "method delegation + pagination state sync"
      pattern: "settingsService\\.readDirectory"
---

<objective>
将 preload 已实现的分页参数向下游（渲染进程侧）逐层贯通，使完整的调用链路支持 `page`/`pageSize` 分页参数传递，并使 `useLocalFiles` composable 管理响应式分页状态。

Purpose: 打通从 composable 到 preload 的完整分页参数链路。preload（Phase 7）已完成，本计划完成后 locawallpaper 页面即可通过 composable 获取分页数据和状态。

Output:
1. `src/types/ipc.ts` — 新增 `PaginationMeta` 接口，扩展 `IpcResponse` 添加 `pagination` 可选字段
2. `env.d.ts` — 更新 `ElectronAPI.readDirectory` 签名匹配 preload 的实际签名
3. `src/clients/file.client.ts` — `readDirectory` 接受 page/pageSize，从原始响应提取 pagination 元数据
4. `src/clients/index.ts` — `electronClient.readDirectory` 转发 page/pageSize
5. `src/repositories/settings.repository.ts` — `readDirectory` 转发 page/pageSize
6. `src/services/settings.service.ts` — `readDirectory` 转发 page/pageSize
7. `src/composables/local/useLocalFiles.ts` — `readDirectory` 支持分页参数 + 响应式分页状态
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-backend-pagination/07-01-SUMMARY.md

<interfaces>
<!-- 当前 IpcResponse 类型 -->
From src/types/ipc.ts:

```typescript
export interface IpcResponse<T = unknown> {
  success: boolean
  data?: T
  error?: IpcErrorInfo
}
```

<!-- 当前 env.d.ts readDirectory 签名 -->
From env.d.ts:

```typescript
readDirectory: (dirPath: string) => Promise<{ error: string | null; files: any[] }>
```

<!-- preload 实际 readDirectory 签名（已由 Phase 7 更新） -->
From electron/preload/index.ts:

```typescript
readDirectory: (dirPath: string, page?: number, pageSize?: number) => Promise<{ error: string | null; files: any[]; total: number; page: number; pageSize: number }>
```

<!-- 当前 electronClient.readDirectory -->
From src/clients/index.ts:

```typescript
readDirectory: (dirPath: string) => fileClient.readDirectory(dirPath),
```

<!-- 当前 settingsRepository.readDirectory -->
From src/repositories/settings.repository.ts:

```typescript
async readDirectory(dirPath: string): Promise<IpcResponse<LocalFile[]>> {
  return electronClient.readDirectory(dirPath)
}
```

<!-- 当前 settingsService.readDirectory -->
From src/services/settings.service.ts:

```typescript
async readDirectory(dirPath: string): Promise<IpcResponse<LocalFile[]>> {
  return settingsRepository.readDirectory(dirPath)
}
```

<!-- 当前 UseLocalFilesReturn -->
From src/composables/local/useLocalFiles.ts:

```typescript
export interface UseLocalFilesReturn {
  readDirectory: (dirPath: string) => Promise<IpcResponse<LocalFile[]>>
  openFolder: (folderPath: string) => Promise<IpcResponse<void>>
  deleteFile: (filePath: string) => Promise<IpcResponse<void>>
}
```

<!-- BaseClient.createUnavailableResponse（供 fileClient 使用） -->
From src/clients/base.client.ts:

```typescript
protected createUnavailableResponse<T>(): IpcResponse<T> {
  return this.createErrorResponse<T>('ELECTRON_UNAVAILABLE', 'Electron API is not available')
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 添加 PaginationMeta 类型 + 更新 env.d.ts 声明</name>

  <files>
    src/types/ipc.ts
    env.d.ts
  </files>

  <read_first>
    src/types/ipc.ts
    env.d.ts
    electron/preload/index.ts
  </read_first>

  <action>
    对以下两个文件进行修改：

    **1. `src/types/ipc.ts`**

    在 `IpcErrorInfo` 接口之后（约第 91 行）、`SelectFolderResponse` 接口之前，新增 `PaginationMeta` 接口：

    ```typescript
    /**
     * 分页元数据
     * 用于文件列表操作的分页响应
     */
    export interface PaginationMeta {
      /** 符合条件的文件总数 */
      total: number
      /** 当前页码（从 1 开始） */
      page: number
      /** 每页数量 */
      pageSize: number
    }
    ```

    在 `IpcResponse` 接口中新增可选字段 `pagination`（约第 83 行，在 `error` 字段后面）：

    ```typescript
    export interface IpcResponse<T = unknown> {
      success: boolean
      data?: T
      error?: IpcErrorInfo
      /** 分页元数据（文件列表分页操作时存在） */
      pagination?: PaginationMeta
    }
    ```

    **2. `env.d.ts`**

    更新 `ElectronAPI` 接口中的 `readDirectory` 签名（第 18 行），使其匹配 `electron/preload/index.ts` 中 `ElectronAPI` 接口的实际签名：

    修改前：
    ```typescript
    readDirectory: (dirPath: string) => Promise<{ error: string | null; files: any[] }>
    ```

    修改后：
    ```typescript
    readDirectory: (dirPath: string, page?: number, pageSize?: number) => Promise<{ error: string | null; files: any[]; total: number; page: number; pageSize: number }>
    ```

    保持内联类型书写方式，与 env.d.ts 现有风格一致。
  </action>

  <acceptance_criteria>
    - `grep -c "interface PaginationMeta" src/types/ipc.ts` 返回 1
    - `grep -c "pagination" src/types/ipc.ts | head -1` 返回大于 1（接口定义 + IpcResponse 字段各出现一次）
    - `grep -c "total: number" src/types/ipc.ts` 返回大于 1（PaginationMeta 中 +…… 扩展 ReadDirectoryResponse 中）
    - `grep "readDirectory:" env.d.ts | grep -c "page.*pageSize"` 返回 1
    - `grep "readDirectory:" env.d.ts | grep -c "total.*page.*pageSize"` 返回 1
  </acceptance_criteria>

  <verify>
    <automated>grep -c "interface PaginationMeta" src/types/ipc.ts && grep "readDirectory:" env.d.ts | grep -E "page\?.*number.*pageSize\?.*number"</automated>
  </verify>

  <done>
    PaginationMeta 接口定义存在；IpcResponse 包含 pagination 可选字段；env.d.ts 中 readDirectory 签名匹配 preload 实际签名。
  </done>
</task>

<task type="auto">
  <name>Task 2: 更新数据链路中间层传递分页参数</name>

  <files>
    src/clients/file.client.ts
    src/clients/index.ts
    src/repositories/settings.repository.ts
    src/services/settings.service.ts
  </files>

  <read_first>
    src/clients/file.client.ts
    src/clients/index.ts
    src/repositories/settings.repository.ts
    src/services/settings.service.ts
  </read_first>

  <action>
    对以下四个文件进行修改。每个文件的变更都是「在方法签名中增加 `page?` 和 `pageSize?` 可选参数，并向下游透传」。

    **1. `src/clients/file.client.ts`**

    修改 `readDirectory` 方法（第 36-58 行）：
    - 方法签名增加 `(dirPath: string, page?: number, pageSize?: number)`：
    - 调用 `window.electronAPI.readDirectory(dirPath, page, pageSize)` 传递分页参数
    - 成功返回时（第 50 行），从原始响应中提取 `total/page/pageSize` 作为 `pagination` 元数据返回

    ```typescript
    async readDirectory(dirPath: string, page?: number, pageSize?: number): Promise<IpcResponse<LocalFile[]>> {
      if (!this.isAvailable()) {
        return this.createUnavailableResponse<LocalFile[]>()
      }

      try {
        const result = await window.electronAPI.readDirectory(dirPath, page, pageSize)
        if (result.error) {
          return {
            success: false,
            data: [],
            pagination: { total: result.total, page: result.page, pageSize: result.pageSize },
            error: { code: 'READ_DIRECTORY_ERROR', message: result.error },
          }
        }
        return {
          success: true,
          data: result.files as LocalFile[],
          pagination: { total: result.total, page: result.page, pageSize: result.pageSize },
        }
      } catch (error) {
        return {
          success: false,
          data: [],
          error: { code: 'READ_DIRECTORY_ERROR', message: String(error) },
        }
      }
    }
    ```

    注意：错误返回（第 52-57 行）保持原样——不添加 `pagination` 字段（pagiation 是可选的）。`createUnavailableResponse` 也不需要修改。

    **2. `src/clients/index.ts`**

    修改 `electronClient` 中的 `readDirectory`（第 54 行）：

    修改前：
    ```typescript
    readDirectory: (dirPath: string) => fileClient.readDirectory(dirPath),
    ```

    修改后：
    ```typescript
    readDirectory: (dirPath: string, page?: number, pageSize?: number) => fileClient.readDirectory(dirPath, page, pageSize),
    ```

    **3. `src/repositories/settings.repository.ts`**

    修改 `readDirectory` 方法（第 87-89 行）：
    - 方法签名增加 `page?`/`pageSize?` 可选参数
    - JSDoc `@param` 增加分页参数说明

    ```typescript
    /**
     * 读取目录内容
     * @param dirPath - 目录路径
     * @param page - 页码（从 1 开始，可选）
     * @param pageSize - 每页数量（可选，默认 50）
     * @returns 返回目录中的文件列表
     */
    async readDirectory(dirPath: string, page?: number, pageSize?: number): Promise<IpcResponse<LocalFile[]>> {
      return electronClient.readDirectory(dirPath, page, pageSize)
    }
    ```

    **4. `src/services/settings.service.ts`**

    修改 `readDirectory` 方法（第 130-132 行）：
    - 方法签名增加 `page?`/`pageSize?` 可选参数
    - JSDoc `@param` 增加分页参数说明

    ```typescript
    /**
     * 读取目录内容
     * @param dirPath - 目录路径
     * @param page - 页码（从 1 开始，可选）
     * @param pageSize - 每页数量（可选，默认 50）
     * @returns 返回目录中的文件列表
     */
    async readDirectory(dirPath: string, page?: number, pageSize?: number): Promise<IpcResponse<LocalFile[]>> {
      return settingsRepository.readDirectory(dirPath, page, pageSize)
    }
    ```
  </action>

  <acceptance_criteria>
    - file.client.ts: `grep "readDirectory.*dirPath.*page.*pageSize" src/clients/file.client.ts | grep -c "page"` 返回 1
    - file.client.ts: `grep "pagination:" src/clients/file.client.ts | grep -c "total.*page.*pageSize"` 返回至少 1
    - file.client.ts: `grep "window.electronAPI.readDirectory" src/clients/file.client.ts | grep -c "page, pageSize"` 返回 1
    - index.ts: `grep "readDirectory:" src/clients/index.ts | grep -c "page.*pageSize"` 返回 1
    - settings.repository.ts: `grep "readDirectory.*dirPath.*page.*pageSize" src/repositories/settings.repository.ts | grep -c "page"` 返回 1
    - settings.repository.ts: `grep "electronClient.readDirectory" src/repositories/settings.repository.ts | grep -c "page, pageSize"` 返回 1
    - settings.service.ts: `grep "readDirectory.*dirPath.*page.*pageSize" src/services/settings.service.ts | grep -c "page"` 返回 1
    - settings.service.ts: `grep "settingsRepository.readDirectory" src/services/settings.service.ts | grep -c "page, pageSize"` 返回 1
  </acceptance_criteria>

  <verify>
    <automated>grep "electronClient.readDirectory" src/repositories/settings.repository.ts | grep -c "page, pageSize" && grep "settingsRepository.readDirectory" src/services/settings.service.ts | grep -c "page, pageSize" && grep "window.electronAPI.readDirectory" src/clients/file.client.ts | grep -c "page, pageSize"</automated>
  </verify>

  <done>
    四个中间层文件的方法签名均支持 page/pageSize 可选参数；参数完整透传不发修改或丢弃；fileClient 返回结果包含 pagination 元数据。
  </done>
</task>

<task type="auto">
  <name>Task 3: 更新 useLocalFiles composable 添加分页参数和响应式分页状态</name>

  <files>
    src/composables/local/useLocalFiles.ts
  </files>

  <read_first>
    src/composables/local/useLocalFiles.ts
    src/services/settings.service.ts
  </read_first>

  <action>
    修改 `src/composables/local/useLocalFiles.ts`，增加分页参数传递和响应式分页状态：

    **修改 1：更新 `import` 语句**

    新增 `ref`、`computed`、`type Ref`、`type ComputedRef` 的导入：

    ```typescript
    import { ref, computed, type Ref } from 'vue'
    ```

    **修改 2：更新 `UseLocalFilesReturn` 接口**

    修改 `readDirectory` 签名增加分页参数，新增 `currentPage`、`totalPages`、`pageSize`、`total` 四个响应式字段：

    ```typescript
    export interface UseLocalFilesReturn {
      /** 读取目录内容 */
      readDirectory: (dirPath: string, page?: number, pageSize?: number) => Promise<IpcResponse<LocalFile[]>>
      /** 在系统文件管理器中打开文件夹 */
      openFolder: (folderPath: string) => Promise<IpcResponse<void>>
      /** 删除文件 */
      deleteFile: (filePath: string) => Promise<IpcResponse<void>>
      /** 当前页码（从 1 开始） */
      currentPage: Ref<number>
      /** 总页数 */
      totalPages: Ref<number>
      /** 每页数量 */
      pageSize: Ref<number>
      /** 文件总数 */
      total: Ref<number>
    }
    ```

    **修改 3：在 `useLocalFiles` 函数体内添加分页状态定义**

    在 `const { showError } = useAlert()` 之后（第 47 行之后）添加：

    ```typescript
    // 分页状态
    const currentPage = ref(1)
    const pageSize = ref(50)
    const total = ref(0)
    const totalPages = computed(() => Math.ceil(total.value / pageSize.value) || 1)
    ```

    **修改 4：更新 `readDirectory` 函数**

    - 方法签名增加 `page?`/`pageSize?` 参数
    - 传递分页参数到 `settingsService.readDirectory`
    - 调用后从 `result.pagination` 更新本地分页状态

    ```typescript
    const readDirectory = async (dirPath: string, page?: number, pageSizeParam?: number): Promise<IpcResponse<LocalFile[]>> => {
      const result = await settingsService.readDirectory(dirPath, page, pageSizeParam)

      if (!result.success) {
        showError(result.error?.message || '读取目录失败')
      }

      // 更新分页状态
      if (result.pagination) {
        currentPage.value = result.pagination.page
        pageSize.value = result.pagination.pageSize
        total.value = result.pagination.total
        // totalPages 是 computed，自动更新
      }

      return result
    }
    ```

    **修改 5：更新 `return` 语句**

    新增分页状态字段：

    ```typescript
    return {
      readDirectory,
      openFolder,
      deleteFile,
      currentPage,
      totalPages,
      pageSize,
      total,
    }
    ```

    不动 `openFolder` 和 `deleteFile` 方法的实现——它们不涉及分页逻辑。
  </action>

  <acceptance_criteria>
    - `grep "readDirectory.*dirPath.*page.*pageSize" src/composables/local/useLocalFiles.ts | grep -c "page"` 返回 1
    - `grep "const currentPage" src/composables/local/useLocalFiles.ts | grep -c "ref"` 返回 1
    - `grep "const totalPages" src/composables/local/useLocalFiles.ts | grep -c "computed"` 返回 1
    - `grep "const pageSize" src/composables/local/useLocalFiles.ts | grep -c "ref"` 返回 1
    - `grep "const total " src/composables/local/useLocalFiles.ts | grep -c "ref"` 返回 1（注意末尾空格，匹配 `const total` 而非 `const totalPages`）
    - `grep "currentPage:" src/composables/local/useLocalFiles.ts | grep -c "currentPage"` 返回 1（return 语句中）
    - `grep "result.pagination" src/composables/local/useLocalFiles.ts | grep -c "currentPage"` 返回 1（状态更新逻辑）
  </acceptance_criteria>

  <verify>
    <automated>grep "currentPage:" src/composables/local/useLocalFiles.ts && grep "totalPages:" src/composables/local/useLocalFiles.ts && grep "result.pagination" src/composables/local/useLocalFiles.ts | grep -c "currentPage"</automated>
  </verify>

  <done>
    useLocalFiles.readDirectory 支持 page/pageSize 可选参数；composable 暴露 currentPage/totalPages/pageSize/total 响应式分页状态；调用 readDirectory 后自动从结果更新分页状态。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer -> main | composable->service->repository->client->preload->IPC，所有层传递相同的 pagination 参数 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | I (Information Disclosure) | 数据链路各层 page/pageSize | accept | page/pageSize 仅为数字索引参数，所有中间层仅透传不做变换，不引入新风险。中间层无权限检查职责，参数在 handler 层已有 T-07-01 的 accept 判定。 |
| T-08-02 | T (Tampering) | file.client.ts pagination 赋值 | accept | pagination 元数据直接从 IPC 原始响应中提取（`result.total`/`result.page`/`result.pageSize`），中间层不做任何变换。渲染进程中的篡改仅影响本地 UI 展示，不改变主进程行为。 |
</threat_model>

<verification>
1. 类型定义验证：`npx tsc --noEmit` 无类型错误
2. 构建验证：`npx electron-vite build` 编译通过
3. 数据链路验证（grep chain）：
   - `src/services/settings.service.ts` → `settingsRepository.readDirectory(dirPath, page, pageSize)`
   - `src/repositories/settings.repository.ts` → `electronClient.readDirectory(dirPath, page, pageSize)`
   - `src/clients/index.ts` → `fileClient.readDirectory(dirPath, page, pageSize)`
   - `src/clients/file.client.ts` → `window.electronAPI.readDirectory(dirPath, page, pageSize)`
4. 分页状态验证：composable 的 return 值包含 currentPage/totalPages/pageSize/total 四个字段
</verification>

<success_criteria>
- `PaginationMeta` 接口在 `src/types/ipc.ts` 中定义
- `IpcResponse` 包含 `pagination?: PaginationMeta` 可选字段
- `env.d.ts` 中 `ElectronAPI.readDirectory` 签名参数列表包含 `page?` 和 `pageSize?`，返回值包含 `total`/`page`/`pageSize`
- `file.client.ts` 中 `readDirectory` 签名支持 page/pageSize，返回响应包含 pagination 元数据
- `index.ts`、`settings.repository.ts`、`settings.service.ts` 中 `readDirectory` 均转发 page/pageSize 参数
- `useLocalFiles.ts` 中 `readDirectory` 支持分页参数，composable 返回 `currentPage`/`totalPages`/`pageSize`/`total` 响应式状态
- `npx tsc --noEmit` 通过
- `npx electron-vite build` 通过
</success_criteria>

<output>
After completion, create `.planning/phases/08-data-flow-pagination/08-01-SUMMARY.md`
</output>

---
phase: 37-favoritespage-vue-onlinewallpaper-vue-handlesetbg-setbg-down
plan: 02
type: execute
wave: 2
depends_on:
  - 37-01
files_modified:
  - src/views/FavoritesPage.vue
  - src/views/OnlineWallpaper.vue
autonomous: true
requirements:
  - REQ-03
  - REQ-04
  - REQ-05
must_haves:
  truths:
    - "FavoritesPage.vue 的 handleSetBg 是一行调用 setBgFromUrl 的委托方法"
    - "OnlineWallpaper.vue 的 setBg 是一行调用 setBgFromUrl 的委托方法"
    - "FavoritesPage.vue 不再包含 downloadWallpaperFile 函数定义"
    - "OnlineWallpaper.vue 不再包含 downloadWallpaperFile 函数定义"
    - "FavoritesPage.vue 不再使用 useSettings（已无调用方）"
    - "OnlineWallpaper.vue 精简 useSettings 只保留 settings（供 apiKey 使用）"
  artifacts:
    - path: src/views/FavoritesPage.vue
      provides: "handleSetBg 委托给 setBgFromUrl, 移除了 downloadWallpaperFile 和 useSettings"
      not_contains: "downloadWallpaperFile"
    - path: src/views/OnlineWallpaper.vue
      provides: "setBg 委托给 setBgFromUrl, 移除了 downloadWallpaperFile 和 selectFolder/update"
      not_contains: "downloadWallpaperFile"
  key_links:
    - from: "FavoritesPage.vue handleSetBg"
      to: "useWallpaperSetter.setBgFromUrl"
      pattern: "setBgFromUrl"
    - from: "OnlineWallpaper.vue setBg"
      to: "useWallpaperSetter.setBgFromUrl"
      pattern: "setBgFromUrl"
---

<objective>
更新 FavoritesPage.vue 和 OnlineWallpaper.vue 使用提取后的 composable 方法，移除重复代码

Purpose: 两个 View 不再包含各自独立的 `downloadWallpaperFile` 和 `setBg`/`handleSetBg` 实现，改为委托到 `useWallpaperSetter` 的新方法。消除约 110 行重复代码。

Output:
- src/views/FavoritesPage.vue — handleSetBg 简化为一行委托，移除 downloadWallpaperFile 函数和 useSettings 导入
- src/views/OnlineWallpaper.vue — setBg 简化为一行委托，移除 downloadWallpaperFile 函数，精简 useSettings 导入
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37-favoritespage-vue-onlinewallpaper-vue-handlesetbg-setbg-down/37-01-PLAN.md
@.planning/codebase/CONVENTIONS.md
@src/composables/wallpaper/useWallpaperSetter.ts

<interfaces>
From Plan 37-01 (now implemented):
```typescript
// 在 useWallpaperSetter.ts 中
export interface DownloadResult {
  success: boolean
  filePath: string | null
  error: string | null
}

export interface UseWallpaperSetterReturn {
  loading: Ref<boolean>
  setWallpaper: (imagePath: string) => Promise<boolean>
  downloadWallpaperFile: (imgItem: WallpaperItem) => Promise<DownloadResult>
  setBgFromUrl: (imgItem: WallpaperItem) => Promise<void>
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 清理 FavoritesPage.vue — 使用 composable 替代重复逻辑</name>
  <files>
    src/views/FavoritesPage.vue
  </files>
  <read_first>
    src/views/FavoritesPage.vue
    src/composables/wallpaper/useWallpaperSetter.ts
  </read_first>
  <action>
    对 `src/views/FavoritesPage.vue` 做以下三个修改：

    **修改 1：更新 `useWallpaperSetter` 解构**

    将现有的第 97 行：
    ```
    const { setWallpaper } = useWallpaperSetter()
    ```
    修改为：
    ```
    const { setWallpaper, setBgFromUrl } = useWallpaperSetter()
    ```

    **修改 2：替换 `handleSetBg` 函数体**

    将现有的 `handleSetBg` 函数（第 201-217 行，包含 try/catch 块和 `downloadWallpaperFile` 调用）替换为一个一行委托：
    ```typescript
    const handleSetBg = (imgItem: WallpaperItem): Promise<void> => {
      return setBgFromUrl(imgItem)
    }
    ```

    注意：`setBgFromUrl` 已内部处理错误提示（showError），所以 View 层不再需要 try/catch 包装。这保持了相同的用户可见行为 — 错误时仍然显示 toast 提示。

    **修改 3：移除 `downloadWallpaperFile` 函数**

    删除第 218-268 行的整个 `downloadWallpaperFile` 函数（从 `/** 下载壁纸文件 */` 注释到函数结束的 `}` 之间的全部代码）。

    **修改 4：清理不再使用的导入**

    删除第 96 行：
    ```
    const { settings, selectFolder, update: updateSettings } = useSettings()
    ```

    注意：在删除前，确认 `settings`、`selectFolder`、`updateSettings` 在当前文件其他地方没有被引用。经过代码审计，这三个变量仅在 `downloadWallpaperFile` 函数中使用，该函数已被删除，因此可以安全移除整行 `useSettings()` 调用。

    **关于 `showError`**：`handleSetBg` 原函数使用 `showError`，替换为一行委托后不再需要。但如果 `showError` 在文件其他地方仍被引用，则保留在 `useAlert()` 的解构中。如果仅被 `handleSetBg` 使用，可以安全移除。让 executor 自行判断 — 保留未使用的解构不会导致编译错误，仅可能产生 lint warning。

    **不需要做**：不改动 `handleDownload`、`handlePreview`、`closePreview`、`handleNavigate` 等功能 — 它们与本次重构无关。
  </action>
  <verify>
    <automated>
      1. `grep -c "downloadWallpaperFile" src/views/FavoritesPage.vue` — 应返回 0（已移除）
      2. `grep -n "handleSetBg" src/views/FavoritesPage.vue` — 应显示为 `return setBgFromUrl(imgItem)` 的简洁实现
      3. `grep -c "useSettings" src/views/FavoritesPage.vue` — 应返回 0（已移除）
      4. `grep -n "setBgFromUrl" src/views/FavoritesPage.vue` — 应返回至少一行（在 useWallpaperSetter 解构和 handleSetBg 中）
    </automated>
    <manual>确认 handleSetBg 只在 `@set-bg` 事件绑定和 `@set-bg="handleSetBg"` 模板绑定中使用，以及函数定义本身。这些绑定都不需要修改。</manual>
  </verify>
  <acceptance_criteria>
    - `grep -c "downloadWallpaperFile" src/views/FavoritesPage.vue` 返回 `0`
    - `grep -c "useSettings" src/views/FavoritesPage.vue` 返回 `0`
    - `grep -c "setBgFromUrl" src/views/FavoritesPage.vue` 返回 `>= 1`（解构 + handleSetBg 调用）
    - `handleSetBg` 函数体仅包含 `return setBgFromUrl(imgItem)`
    - `useWallpaperSetter()` 解构包含 `setBgFromUrl`
    - TypeScript 编译通过
  </acceptance_criteria>
  <done>
    FavoritesPage.vue 中已无 downloadWallpaperFile 函数，handleSetBg 是一行委托调用 setBgFromUrl，useSettings 不再导入
  </done>
</task>

<task type="auto">
  <name>Task 2: 清理 OnlineWallpaper.vue — 使用 composable 替代重复逻辑</name>
  <files>
    src/views/OnlineWallpaper.vue
  </files>
  <read_first>
    src/views/OnlineWallpaper.vue
    src/composables/wallpaper/useWallpaperSetter.ts
  </read_first>
  <action>
    对 `src/views/OnlineWallpaper.vue` 做以下四个修改：

    **修改 1：更新 `useWallpaperSetter` 解构**

    将现有的第 122 行：
    ```
    const { setWallpaper } = useWallpaperSetter()
    ```
    修改为：
    ```
    const { setWallpaper, setBgFromUrl } = useWallpaperSetter()
    ```

    **修改 2：替换 `setBg` 函数体**

    将现有的 `setBg` 函数（第 312-328 行，包含 try/catch 块和 `downloadWallpaperFile` 调用）替换为一个一行委托：
    ```typescript
    const setBg = async (imgItem: WallpaperItem): Promise<void> => {
      return setBgFromUrl(imgItem)
    }
    ```

    **修改 3：移除 `downloadWallpaperFile` 函数**

    删除第 341-391 行的整个 `downloadWallpaperFile` 函数（从 `/** 下载壁纸文件 */` 注释到函数结束的 `}` 之间的全部代码）。

    **修改 4：精简 `useSettings` 导入**

    将现有的第 120 行：
    ```
    const { settings, selectFolder, update: updateSettings } = useSettings()
    ```
    修改为：
    ```
    const { settings } = useSettings()
    ```

    原因：`settings` 在第 146 行 `const apiKey = computed(() => settings.value.apiKey)` 中使用，必须保留。但 `selectFolder` 和 `update`（别名 `updateSettings`）仅在 `downloadWallpaperFile` 中使用，该函数已被删除。

    **不需要做**：不改动其他使用 `useSettings` 的地方（`settings` 仍然通过精简后的解构可用）。不改动 `downloadImg`、`downloadSelected`、`addToDownloadQueue`、`generateFilename` 等功能。
  </action>
  <verify>
    <automated>
      1. `grep -c "downloadWallpaperFile" src/views/OnlineWallpaper.vue` — 应返回 0
      2. `grep -n "setBg" src/views/OnlineWallpaper.vue` — 确认 setBg 函数定义使用 setBgFromUrl
      3. `grep "useSettings" src/views/OnlineWallpaper.vue` — 确认仅解构 `{ settings }`，不再有 selectFolder 或 update
      4. `grep -n "setBgFromUrl" src/views/OnlineWallpaper.vue` — 应返回至少一行
    </automated>
    <manual>确认 `const apiKey = computed(() => settings.value.apiKey)` 仍然可用且类型正确（settings 仍然通过 useSettings 导入）。</manual>
  </verify>
  <acceptance_criteria>
    - `grep -c "downloadWallpaperFile" src/views/OnlineWallpaper.vue` 返回 `0`
    - `grep "selectFolder" src/views/OnlineWallpaper.vue` 返回 `0`（已移除）
    - `grep "updateSettings" src/views/OnlineWallpaper.vue` 返回 `0`（已移除）
    - `grep "const { settings } = useSettings()" src/views/OnlineWallpaper.vue` 返回匹配
    - `setBg` 函数体仅包含 `return setBgFromUrl(imgItem)`
    - `useWallpaperSetter()` 解构包含 `setBgFromUrl`
    - TypeScript 编译通过
  </acceptance_criteria>
  <done>
    OnlineWallpaper.vue 中已无 downloadWallpaperFile 函数，setBg 是一行委托调用 setBgFromUrl，useSettings 精简为仅保留 settings
  </done>
</task>

</tasks>

<verification>
- [ ] `grep -c "downloadWallpaperFile" src/views/FavoritesPage.vue` 返回 0
- [ ] `grep -c "downloadWallpaperFile" src/views/OnlineWallpaper.vue` 返回 0
- [ ] `grep -c "setBgFromUrl" src/views/FavoritesPage.vue` 返回 >= 1
- [ ] `grep -c "setBgFromUrl" src/views/OnlineWallpaper.vue` 返回 >= 1
- [ ] FavoritesPage.vue 无 `useSettings` 调用
- [ ] OnlineWallpaper.vue 的 `useSettings` 仅解构 `{ settings }`
- [ ] TypeScript 编译通过（`npx vue-tsc --noEmit --strict` 无新增错误）
</verification>

<success_criteria>
- 两个 View 中的 `downloadWallpaperFile` 函数完全移除
- 两个 View 中的 `setBg`/`handleSetBg` 简化为一行的 `setBgFromUrl` 委托
- 导入清理完成（FavoritesPage 移除 useSettings，OnlineWallpaper 精简 useSettings）
- UI 行为完全不变（功能委托给 composable，错误处理一致）
- TypeScript 编译无错误
</success_criteria>

<output>
After completion, create `.planning/phases/37-favoritespage-vue-onlinewallpaper-vue-handlesetbg-setbg-down/37-02-SUMMARY.md`
</output>

---
phase: 3
plan: 5
type: composable
wave: 2
depends_on:
  - 03-PLAN-02
files_modified:
  - src/composables/download/useDownload.ts
  - src/composables/index.ts
autonomous: true
requirements:
  - BIZ-05
---

<objective>
创建 useDownload composable，封装下载管理逻辑。

核心职责：
- 协调 DownloadService 和 DownloadStore
- 管理下载任务生命周期
- 订阅并处理进度更新
- 自动显示错误提示
</objective>

<tasks>
  <task id="1">
    <read_first>
      - src/services/download.service.ts
      - src/composables/core/useAlert.ts
      - src/stores/modules/download/index.ts
      - src/types/index.ts
    </read_first>
    <action>
      创建目录 `src/composables/download/`（如果不存在）

      创建文件 `src/composables/download/useDownload.ts`：

      1. 定义返回值接口：
         ```typescript
         export interface UseDownloadReturn {
           // 状态（ComputedRef）
           downloadingList: ComputedRef<DownloadItem[]>
           finishedList: ComputedRef<FinishedDownloadItem[]>
           totalActive: ComputedRef<number>
           totalPaused: ComputedRef<number>
           totalFinished: ComputedRef<number>
           
           // 方法
           addTask: (task: Omit<DownloadItem, 'id' | 'offset' | 'progress' | 'speed' | 'state'>) => string
           startDownload: (id: string) => Promise<boolean>
           pauseDownload: (id: string) => void
           resumeDownload: (id: string) => void
           cancelDownload: (id: string) => boolean
           removeFinished: (id: string) => Promise<boolean>
           clearFinished: () => Promise<void>
           isDownloading: (wallpaperId: string) => boolean
           loadHistory: () => Promise<void>
         }
         ```

      2. 实现 `useDownload()` 函数：
         ```typescript
         export function useDownload(): UseDownloadReturn {
           const store = useDownloadStore()
           const { showError } = useAlert()
           
           // 进度订阅取消函数
           let unsubscribe: (() => void) | null = null
           
           // 处理进度更新
           const handleProgress = (data: DownloadProgressData): void => {
             const { taskId, progress, offset, speed, state, filePath, error } = data
             
             if (error) {
               const task = store.downloadingList.find(item => item.id === taskId)
               if (task) {
                 task.state = 'waiting'
                 task.progress = 0
               }
               showError(`下载失败: ${error}`)
               return
             }
             
             if (state === 'completed' && filePath) {
               store.completeDownload(taskId, filePath)
             } else {
               store.updateProgress(taskId, progress, offset, speed, filePath)
             }
           }
           
           // 生命周期钩子：订阅进度
           onMounted(() => {
             unsubscribe = downloadService.onProgress(handleProgress)
           })

           // 生命周期钩子：取消订阅
           onUnmounted(() => {
             if (unsubscribe) {
               unsubscribe()
               unsubscribe = null
             }
           })
           
           return {
             downloadingList: computed(() => store.downloadingList),
             finishedList: computed(() => store.finishedList),
             totalActive: computed(() => store.totalActive),
             totalPaused: computed(() => store.totalPaused),
             totalFinished: computed(() => store.totalFinished),
             addTask,
             startDownload,
             pauseDownload,
             resumeDownload,
             cancelDownload,
             removeFinished,
             clearFinished,
             isDownloading,
             loadHistory,
           }
         }
         ```

      3. addTask 方法实现：
         ```typescript
         const addTask = (task: Omit<DownloadItem, 'id' | 'offset' | 'progress' | 'speed' | 'state'>): string => {
           return store.addDownloadTask(task)
         }
         ```

      4. startDownload 方法实现：
         ```typescript
         const startDownload = async (id: string): Promise<boolean> => {
           const task = store.downloadingList.find(item => item.id === id)
           if (!task) {
             showError('任务不存在')
             return false
           }
           
           task.state = 'downloading'
           task.time = new Date().toISOString()
           
           const result = await downloadService.startDownload(id, task.url, task.filename)
           
           if (!result.success) {
             task.state = 'waiting'
             showError(result.error?.message || '启动下载失败')
             return false
           }
           
           return true
         }
         ```

      5. 其他方法实现：
         - pauseDownload: `store.pauseDownload(id)`
         - resumeDownload: `store.resumeDownload(id)`
         - cancelDownload: `store.cancelDownload(id)`
         - removeFinished: 调用 `downloadService.removeFinishedRecord(id)`，同步更新 Store
         - clearFinished: 调用 `downloadService.clearFinishedRecords()`，清空 Store
         - isDownloading: `store.isDownloading(wallpaperId)`
         - loadHistory: 调用 `downloadService.getFinishedRecords()`，更新 Store
    </action>
    <acceptance_criteria>
      - 文件存在：`src/composables/download/useDownload.ts`
      - 文件包含导出：`export interface UseDownloadReturn`
      - 文件包含导出：`export function useDownload()`
      - 函数返回类型为 `UseDownloadReturn`
      - 函数导入自：`@/stores/modules/download`
      - 函数导入自：`@/services`
      - 函数导入自：`@/composables`
      - 函数包含 `onMounted(() => { unsubscribe = downloadService.onProgress(handleProgress) })`
      - 函数包含 `onUnmounted(() => { if (unsubscribe) { unsubscribe(); unsubscribe = null } })`
      - TypeScript 编译无错误
    </acceptance_criteria>
  </task>

  <task id="2">
    <read_first>
      - src/composables/index.ts
      - src/composables/download/useDownload.ts
    </read_first>
    <action>
      更新文件 `src/composables/index.ts`，添加 useDownload 导出：

      在文件末尾添加：
      ```typescript
      // Download
      export { useDownload, type UseDownloadReturn } from './download/useDownload'
      ```

      保留已有的 useAlert 和 useWallpaperList 导出。
    </action>
    <acceptance_criteria>
      - 文件包含导出：`export { useDownload, type UseDownloadReturn }`
      - 文件保留已有的 useAlert 和 useWallpaperList 导出
      - TypeScript 编译无错误
    </acceptance_criteria>
  </task>
</tasks>

<verification>
  ## 编译验证

  ```bash
  npm run typecheck || npx tsc --noEmit
  ```

  ## 导入验证

  ```bash
  grep -r "useDownload" src/composables/index.ts
  ```

  ## 功能验证（集成后）

  - addTask 正确添加任务到 Store
  - startDownload 正确调用 downloadService
  - 进度订阅自动管理（挂载时订阅，卸载时取消）
  - 错误时自动显示提示
</verification>

<success_criteria>
  <must_haves>
    - useDownload 可从 `@/composables` 导入
    - 进度订阅在 onMounted 中建立
    - 进度订阅在 onUnmounted 中取消
    - 正确协调 Service 和 Store
    - 错误时自动显示提示
    - TypeScript 编译无错误
  </must_haves>
</success_criteria>

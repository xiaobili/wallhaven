---
phase: 3
plan: 3
type: service
wave: 1
depends_on: []
files_modified:
  - src/services/settings.service.ts
  - src/services/index.ts
autonomous: true
requirements:
  - BIZ-03
---

<objective>
创建 SettingsService，实现设置业务逻辑。

核心职责：
- 应用设置管理
- 设置加载与保存
- 默认设置提供
- 内存缓存优化
</objective>

<tasks>
  <task id="1">
    <read_first>
      - src/repositories/settings.repository.ts
      - src/shared/types/ipc.ts
      - src/types/index.ts
    </read_first>
    <action>
      创建文件 `src/services/settings.service.ts`，实现 SettingsService 类：

      1. 添加正确的导入语句：
         ```typescript
         import type { IpcResponse } from '@/shared/types/ipc'
         import type { AppSettings, WallpaperFit } from '@/types'
         import { settingsRepository } from '@/repositories'
         ```

      2. 定义默认设置常量：
         ```typescript
         const DEFAULT_SETTINGS: AppSettings = {
           downloadPath: '',
           maxConcurrentDownloads: 3,
           apiKey: '',
           wallpaperFit: 'fill' as WallpaperFit,
         }
         ```

      3. 创建类 `SettingsServiceImpl`，包含：
         - `private cachedSettings: AppSettings | null = null`

      4. 实现公开方法：
         - `async get(): Promise<IpcResponse<AppSettings | null>>`
           * 优先返回缓存：`if (this.cachedSettings) return { success: true, data: this.cachedSettings }`
           * 调用 `settingsRepository.get()`
           * 成功时更新缓存：`this.cachedSettings = result.data`
           * 返回结果
         
         - `async set(settings: AppSettings): Promise<IpcResponse<void>>`
           * 调用 `settingsRepository.set(settings)`
           * 成功时更新缓存：`this.cachedSettings = { ...settings }`
           * 返回结果
         
         - `async update(partial: Partial<AppSettings>): Promise<IpcResponse<void>>`
           * 调用 `await this.get()` 获取当前设置
           * 合并：`const merged: AppSettings = { ...current, ...partial }`
           * 调用 `this.set(merged)`
         
         - `getDefaults(): AppSettings`
           * 返回 `{ ...DEFAULT_SETTINGS }`
         
         - `async reset(): Promise<IpcResponse<void>>`
           * 清除缓存：`this.cachedSettings = null`
           * 调用 `this.set(this.getDefaults())`
         
         - `clearCache(): void`
           * 设置 `this.cachedSettings = null`

      5. 导出单例：
         ```typescript
         export const settingsService = new SettingsServiceImpl()
         ```
    </action>
    <acceptance_criteria>
      - 文件存在：`src/services/settings.service.ts`
      - 文件包含导入：`import type { IpcResponse } from '@/shared/types/ipc'`
      - 文件包含导入：`import type { AppSettings, WallpaperFit } from '@/types'`
      - 文件包含导出：`export const settingsService`
      - 文件包含类：`class SettingsServiceImpl`
      - 类包含方法：`get()`
      - 类包含方法：`set(settings: AppSettings)`
      - 类包含方法：`update(partial: Partial<AppSettings>)`
      - 类包含方法：`getDefaults()`
      - 类包含方法：`reset()`
      - 类包含方法：`clearCache()`
      - 文件包含常量：`DEFAULT_SETTINGS`
      - 文件导入自：`@/repositories`
      - TypeScript 编译无错误
    </acceptance_criteria>
  </task>

  <task id="2">
    <read_first>
      - src/services/index.ts
      - src/services/settings.service.ts
    </read_first>
    <action>
      更新文件 `src/services/index.ts`，添加 settingsService 导出：

      在文件末尾添加：
      ```typescript
      // 设置服务
      export { settingsService } from './settings.service'
      ```

      保留已有的 wallpaperService 和 downloadService 导出。
    </action>
    <acceptance_criteria>
      - 文件包含导出：`export { settingsService }`
      - 文件保留已有的 wallpaperService 和 downloadService 导出
      - TypeScript 编译无错误
    </acceptance_criteria>
  </task>
</tasks>

<verification>
  ## 编译验证

  ```bash
  npm run typecheck || npx tsc --noEmit
  ```

  ## 导入验证

  ```bash
  grep -r "settingsService" src/services/index.ts
  ```

  ## 功能验证（集成后）

  - 首次调用 get() 从 Repository 获取
  - 第二次调用 get() 返回缓存
  - update() 正确合并部分设置
  - reset() 恢复默认设置
</verification>

<success_criteria>
  <must_haves>
    - SettingsService 单例可从 `@/services` 导入
    - 内存缓存正确实现（避免重复 IPC 调用）
    - 默认设置正确提供
    - update 方法正确合并部分设置
    - TypeScript 编译无错误
  </must_haves>
</success_criteria>

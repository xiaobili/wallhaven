---
phase: 3
plan: 7
type: store-refactor
wave: 3
depends_on:
  - 03-PLAN-04
  - 03-PLAN-05
  - 03-PLAN-06
files_modified:
  - src/stores/modules/wallpaper/index.ts
  - src/stores/modules/wallpaper/state.ts
  - src/stores/modules/wallpaper/actions.ts
  - src/stores/modules/wallpaper/storage.ts
  - src/stores/modules/wallpaper/settings-storage.ts
  - src/stores/modules/download/index.ts
autonomous: false
requirements:
  - BIZ-07
---

<objective>
重构 Store，移除业务逻辑，仅保留响应式状态。

核心目标：
- WallpaperStore：合并文件，移除 actions.ts、storage.ts、settings-storage.ts
- DownloadStore：移除业务逻辑方法，保留状态和简单操作
- 保持响应式系统不变
</objective>

<tasks>
  <task id="1">
    <read_first>
      - src/stores/modules/wallpaper/index.ts
      - src/stores/modules/wallpaper/state.ts
      - src/stores/modules/wallpaper/actions.ts
      - src/types/index.ts
    </read_first>
    <action>
      重写文件 `src/stores/modules/wallpaper/index.ts`，合并 state.ts 内容：

      ```typescript
      import { defineStore } from 'pinia'
      import { ref, reactive, shallowRef } from 'vue'
      import type { TotalPageData, GetParams, CustomParams, AppSettings, WallpaperFit } from '@/types'

      /**
       * 创建默认设置
       */
      function createDefaultSettings(): AppSettings {
        return {
          downloadPath: '',
          maxConcurrentDownloads: 3,
          apiKey: '',
          wallpaperFit: 'fill' as WallpaperFit,
        }
      }

      export const useWallpaperStore = defineStore('wallpaper', () => {
        // ==================== 状态 ====================
        
        /** 壁纸数据（使用 shallowRef 优化性能） */
        const totalPageData = shallowRef<TotalPageData>({
          totalPage: 0,
          currentPage: 0,
          sections: [],
        })
        
        /** 加载状态 */
        const loading = ref<boolean>(false)
        
        /** 错误状态 */
        const error = ref<boolean>(false)
        
        /** 当前查询参数 */
        const queryParams = ref<GetParams | null>(null)
        
        /** 已保存的自定义参数 */
        const savedParams = ref<CustomParams | null>(null)
        
        /** 应用设置 */
        const settings = reactive<AppSettings>(createDefaultSettings())

        // ==================== 方法（由 Composable 调用） ====================

        /**
         * 重置状态
         */
        function resetState(): void {
          totalPageData.value = { totalPage: 0, currentPage: 0, sections: [] }
          queryParams.value = null
          error.value = false
        }

        return {
          // 状态
          totalPageData,
          loading,
          error,
          queryParams,
          savedParams,
          settings,
          
          // 方法
          resetState,
        }
      })
      ```

      关键变更：
      1. 移除 `createWallpaperActions` 调用
      2. 移除 `fetchWallpapers`、`loadMoreWallpapers`、`saveCustomParams`、`getSavedParams`、`updateSettings`、`loadSettings` 方法
      3. 保留 `resetState` 方法（简单状态重置）
      4. 合并 state.ts 的内容
    </action>
    <acceptance_criteria>
      - 文件 `src/stores/modules/wallpaper/index.ts` 存在
      - 文件不包含 `createWallpaperActions` 导入
      - 文件不包含 `fetchWallpapers` 方法定义
      - 文件不包含 `loadMoreWallpapers` 方法定义
      - 文件不包含 `saveCustomParams` 方法定义
      - 文件不包含 `getSavedParams` 方法定义
      - 文件不包含 `updateSettings` 方法定义
      - 文件不包含 `loadSettings` 方法定义
      - 文件包含 `resetState` 方法
      - 文件导出 `useWallpaperStore`
      - TypeScript 编译无错误
    </acceptance_criteria>
  </task>

  <task id="2">
    <read_first>
      - src/stores/modules/wallpaper/actions.ts
      - src/stores/modules/wallpaper/storage.ts
      - src/stores/modules/wallpaper/settings-storage.ts
      - src/stores/modules/wallpaper/state.ts
    </read_first>
    <action>
      删除以下文件：
      - `src/stores/modules/wallpaper/actions.ts`
      - `src/stores/modules/wallpaper/storage.ts`
      - `src/stores/modules/wallpaper/settings-storage.ts`
      - `src/stores/modules/wallpaper/state.ts`

      这些文件的逻辑已迁移到：
      - actions.ts → useWallpaperList composable + WallpaperService
      - storage.ts → WallpaperService
      - settings-storage.ts → SettingsService
      - state.ts → 合并到 index.ts
    </action>
    <acceptance_criteria>
      - 文件 `src/stores/modules/wallpaper/actions.ts` 不存在
      - 文件 `src/stores/modules/wallpaper/storage.ts` 不存在
      - 文件 `src/stores/modules/wallpaper/settings-storage.ts` 不存在
      - 文件 `src/stores/modules/wallpaper/state.ts` 不存在
      - TypeScript 编译无错误
    </acceptance_criteria>
  </task>

  <task id="3">
    <read_first>
      - src/stores/modules/download/index.ts
      - src/services/download.service.ts
      - src/types/index.ts
    </read_first>
    <action>
      重写文件 `src/stores/modules/download/index.ts`，移除业务逻辑：

      保留：
      - 状态：`downloadingList`, `finishedList`
      - 计算属性：`activeDownloads`, `pausedDownloads`, `totalActive`, `totalPaused`, `totalFinished`
      - 简单方法：`generateId`, `addDownloadTask`, `updateProgress`, `completeDownload`, `pauseDownload`, `resumeDownload`, `cancelDownload`, `isDownloading`

      移除：
      - `startDownload` 中的业务逻辑（选择目录、启动下载）→ 迁移到 useDownload composable
      - `saveToStorage` → 迁移到 DownloadService
      - `loadFromStorage` → 迁移到 useDownload composable
      - `removeFinishedRecord` → 迁移到 useDownload composable
      - `clearFinishedList` → 迁移到 useDownload composable
      - `addBatchDownloadTasks` → 迁移到 useDownload composable

      精简后的代码：
      ```typescript
      import { defineStore } from 'pinia'
      import { ref, computed } from 'vue'
      import type { DownloadItem, FinishedDownloadItem } from '@/types'

      export const useDownloadStore = defineStore('download', () => {
        // ==================== 状态 ====================
        
        const downloadingList = ref<DownloadItem[]>([])
        const finishedList = ref<FinishedDownloadItem[]>([])
        
        // ==================== 计算属性 ====================
        
        const activeDownloads = computed(() => 
          downloadingList.value.filter(item => item.state === 'downloading')
        )
        
        const pausedDownloads = computed(() => 
          downloadingList.value.filter(item => item.state === 'paused')
        )
        
        const totalActive = computed(() => activeDownloads.value.length)
        const totalPaused = computed(() => pausedDownloads.value.length)
        const totalFinished = computed(() => finishedList.value.length)

        // ==================== 方法 ====================

        function generateId(): string {
          return `dl_${Date.now()}_${Math.random().toString(36).substr(2, 9)}`
        }

        function addDownloadTask(
          task: Omit<DownloadItem, 'id' | 'offset' | 'progress' | 'speed' | 'state'>
        ): string {
          const id = generateId()
          
          const downloadItem: DownloadItem = {
            id,
            ...task,
            offset: 0,
            progress: 0,
            speed: 0,
            state: 'waiting'
          }
          
          downloadingList.value.push(downloadItem)
          console.log('[DownloadStore] 添加下载任务:', id)
          
          return id
        }

        function updateProgress(
          id: string, 
          progress: number, 
          offset: number, 
          speed: number,
          filePath?: string
        ): void {
          const task = downloadingList.value.find(item => item.id === id)
          if (task) {
            task.progress = progress
            task.offset = offset
            task.speed = speed
            
            if (progress >= 100 && filePath) {
              completeDownload(id, filePath)
            }
          }
        }

        function completeDownload(id: string, filePath?: string): void {
          const index = downloadingList.value.findIndex(item => item.id === id)
          if (index === -1) return
          
          const task = downloadingList.value[index]
          
          const finishedItem: FinishedDownloadItem = {
            id: task.id,
            url: task.url,
            filename: task.filename,
            small: task.small,
            resolution: task.resolution,
            size: task.size,
            offset: task.offset,
            progress: 100,
            speed: task.speed,
            state: 'completed',
            path: filePath || task.path || '',
            time: new Date().toISOString(),
            wallpaperId: task.wallpaperId
          }
          
          finishedList.value.unshift(finishedItem)
          downloadingList.value.splice(index, 1)
          
          console.log('[DownloadStore] 下载完成:', id)
        }

        function pauseDownload(id: string): void {
          const task = downloadingList.value.find(item => item.id === id)
          if (task && task.state === 'downloading') {
            task.state = 'paused'
          }
        }

        function resumeDownload(id: string): void {
          const task = downloadingList.value.find(item => item.id === id)
          if (task && task.state === 'paused') {
            task.state = 'downloading'
          }
        }

        function cancelDownload(id: string): boolean {
          const index = downloadingList.value.findIndex(item => item.id === id)
          if (index !== -1) {
            downloadingList.value.splice(index, 1)
            return true
          }
          return false
        }

        function isDownloading(wallpaperId: string): boolean {
          return downloadingList.value.some(item => item.wallpaperId === wallpaperId)
        }

        return {
          // 状态
          downloadingList,
          finishedList,
          
          // 计算属性
          activeDownloads,
          pausedDownloads,
          totalActive,
          totalPaused,
          totalFinished,
          
          // 方法
          addDownloadTask,
          updateProgress,
          completeDownload,
          pauseDownload,
          resumeDownload,
          cancelDownload,
          isDownloading,
        }
      })
      ```

      注意：
      - `addDownloadTask` 不再是 async，不再调用 `saveToStorage`
      - `pauseDownload` 和 `resumeDownload` 不再是 async
      - 移除了所有 `await saveToStorage()` 调用
    </action>
    <acceptance_criteria>
      - 文件 `src/stores/modules/download/index.ts` 存在
      - 文件不包含 `startDownload` 方法定义
      - 文件不包含 `saveToStorage` 方法定义
      - 文件不包含 `loadFromStorage` 方法定义
      - 文件不包含 `removeFinishedRecord` 方法定义
      - 文件不包含 `clearFinishedList` 方法定义
      - 文件不包含 `addBatchDownloadTasks` 方法定义
      - 文件包含 `addDownloadTask` 方法（非 async）
      - 文件包含 `updateProgress` 方法
      - 文件包含 `completeDownload` 方法
      - 文件包含 `pauseDownload` 方法（非 async）
      - 文件包含 `resumeDownload` 方法（非 async）
      - 文件包含 `cancelDownload` 方法
      - 文件包含 `isDownloading` 方法
      - TypeScript 编译无错误
    </acceptance_criteria>
  </task>
</tasks>

<verification>
  ## 编译验证

  ```bash
  npm run typecheck || npx tsc --noEmit
  ```

  ## 文件删除验证

  ```bash
  # 验证文件已删除
  test ! -f src/stores/modules/wallpaper/actions.ts && echo "actions.ts 已删除"
  test ! -f src/stores/modules/wallpaper/storage.ts && echo "storage.ts 已删除"
  test ! -f src/stores/modules/wallpaper/settings-storage.ts && echo "settings-storage.ts 已删除"
  test ! -f src/stores/modules/wallpaper/state.ts && echo "state.ts 已删除"
  ```

  ## Store 方法验证

  ```bash
  # 验证 WallpaperStore 不再包含业务逻辑方法
  ! grep -q "fetchWallpapers" src/stores/modules/wallpaper/index.ts && echo "fetchWallpapers 已移除"
  ! grep -q "loadMoreWallpapers" src/stores/modules/wallpaper/index.ts && echo "loadMoreWallpapers 已移除"
  ! grep -q "saveCustomParams" src/stores/modules/wallpaper/index.ts && echo "saveCustomParams 已移除"
  
  # 验证 DownloadStore 不再包含业务逻辑方法
  ! grep -q "startDownload" src/stores/modules/download/index.ts && echo "startDownload 已移除"
  ! grep -q "saveToStorage" src/stores/modules/download/index.ts && echo "saveToStorage 已移除"
  ```

  ## 功能验证（运行应用）

  - 应用正常启动
  - 壁纸搜索正常
  - 下载功能正常
  - 设置保存正常
</verification>

<success_criteria>
  <must_haves>
    - WallpaperStore 文件数量从 5 个减少到 1 个
    - WallpaperStore 不再包含业务逻辑方法
    - DownloadStore 不再包含 startDownload 业务逻辑
    - DownloadStore 不再直接调用存储操作
    - Store 仅保留响应式状态和简单操作
    - TypeScript 编译无错误
    - 应用正常运行（功能不变）
  </must_haves>
</success_criteria>

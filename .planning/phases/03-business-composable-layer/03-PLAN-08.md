---
phase: 3
plan: 8
type: integration
wave: 4
depends_on:
  - 03-PLAN-07
files_modified:
  - src/main.ts
autonomous: false
requirements:
  - BIZ-07
---

<objective>
更新 main.ts 中的下载进度监听，使用新的 downloadService。

核心变更：
- 使用 downloadService.onProgress() 替代 window.electronAPI.onDownloadProgress()
- 保持现有的进度处理逻辑不变
</objective>

<tasks>
  <task id="1">
    <read_first>
      - src/main.ts
      - src/services/download.service.ts
      - src/stores/modules/download/index.ts
    </read_first>
    <action>
      更新文件 `src/main.ts`：

      1. 在文件顶部添加 import：
         ```typescript
         import { downloadService } from '@/services'
         ```

      2. 修改 `initializeApp()` 函数中的进度监听代码：

         将：
         ```typescript
         window.electronAPI.onDownloadProgress((data) => {
           // ... 处理逻辑
         })
         ```

         改为：
         ```typescript
         downloadService.onProgress((data) => {
           console.log('[Main] 收到下载进度:', data)
           
           const { taskId, progress, offset, speed, state, filePath, error } = data
           
           if (error) {
             console.error('[Main] 下载失败:', error)
             const task = downloadStore.downloadingList.find((item: any) => item.id === taskId)
             if (task) {
               task.state = 'waiting'
               task.progress = 0
             }
           } else if (state === 'completed') {
             downloadStore.updateProgress(taskId, 100, offset, 0, filePath)
             console.log('[Main] 下载完成:', filePath)
           } else {
             downloadStore.updateProgress(taskId, progress, offset, speed)
           }
         })
         ```

      3. 移除 `if (window.electronAPI)` 条件判断，因为 downloadService 内部已处理

      4. 更新日志信息：
         ```typescript
         console.log('[Main] 下载进度监听器已通过 downloadService 注册')
         ```
    </action>
    <acceptance_criteria>
      - 文件 `src/main.ts` 包含导入：`import { downloadService } from '@/services'`
      - 文件不包含 `window.electronAPI.onDownloadProgress` 调用
      - 文件包含 `downloadService.onProgress` 调用
      - 进度处理逻辑保持不变（错误处理、完成处理、进度更新）
      - TypeScript 编译无错误
    </acceptance_criteria>
  </task>
</tasks>

<verification>
  ## 编译验证

  ```bash
  npm run typecheck || npx tsc --noEmit
  ```

  ## 代码验证

  ```bash
  # 验证使用 downloadService
  grep -q "downloadService.onProgress" src/main.ts && echo "使用 downloadService"

  # 验证不再直接使用 electronAPI
  ! grep -q "window.electronAPI.onDownloadProgress" src/main.ts && echo "已移除直接调用"
  ```

  ## 功能验证（运行应用）

  - 应用正常启动
  - 下载进度正确显示
  - 下载完成正确处理
  - 下载失败正确处理
</verification>

<success_criteria>
  <must_haves>
    - 使用 downloadService.onProgress 替代 window.electronAPI.onDownloadProgress
    - 进度处理逻辑保持不变
    - TypeScript 编译无错误
    - 下载功能正常（功能不变）
  </must_haves>
</success_criteria>

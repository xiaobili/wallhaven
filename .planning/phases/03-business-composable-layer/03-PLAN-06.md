---
phase: 3
plan: 6
type: composable
wave: 2
depends_on:
  - 03-PLAN-03
files_modified:
  - src/composables/settings/useSettings.ts
  - src/composables/index.ts
autonomous: true
requirements:
  - BIZ-06
---

<objective>
创建 useSettings composable，封装设置管理逻辑。

核心职责：
- 协调 SettingsService 和 WallpaperStore
- 管理设置加载、更新、重置
- 自动显示错误/成功提示
</objective>

<tasks>
  <task id="1">
    <read_first>
      - src/services/settings.service.ts
      - src/composables/core/useAlert.ts
      - src/stores/modules/wallpaper/index.ts
      - src/stores/modules/wallpaper/state.ts
      - src/types/index.ts
    </read_first>
    <action>
      创建目录 `src/composables/settings/`（如果不存在）

      创建文件 `src/composables/settings/useSettings.ts`：

      1. 定义返回值接口：
         ```typescript
         export interface UseSettingsReturn {
           // 状态（ComputedRef）
           settings: ComputedRef<AppSettings>
           
           // 方法
           load: () => Promise<boolean>
           update: (partial: Partial<AppSettings>) => Promise<boolean>
           reset: () => Promise<boolean>
           getDefaults: () => AppSettings
         }
         ```

      2. 实现 `useSettings()` 函数：
         ```typescript
         export function useSettings(): UseSettingsReturn {
           const store = useWallpaperStore()
           const { showError, showSuccess } = useAlert()
           
           const load = async (): Promise<boolean> => {
             const result = await settingsService.get()
             
             if (result.success && result.data) {
               Object.assign(store.settings, result.data)
               return true
             }
             
             // 加载失败，使用默认值
             Object.assign(store.settings, settingsService.getDefaults())
             return false
           }
           
           const update = async (partial: Partial<AppSettings>): Promise<boolean> => {
             // 先更新本地状态
             Object.assign(store.settings, partial)
             
             // 再持久化
             const result = await settingsService.update(partial)
             
             if (!result.success) {
               showError('保存设置失败')
               return false
             }
             
             return true
           }
           
           const reset = async (): Promise<boolean> => {
             const defaults = settingsService.getDefaults()
             Object.assign(store.settings, defaults)
             
             const result = await settingsService.reset()
             
             if (!result.success) {
               showError('重置设置失败')
               return false
             }
             
             showSuccess('已恢复默认设置')
             return true
           }
           
           const getDefaults = (): AppSettings => {
             return settingsService.getDefaults()
           }
           
           return {
             settings: computed(() => store.settings),
             load,
             update,
             reset,
             getDefaults,
           }
         }
         ```
    </action>
    <acceptance_criteria>
      - 文件存在：`src/composables/settings/useSettings.ts`
      - 文件包含导出：`export interface UseSettingsReturn`
      - 文件包含导出：`export function useSettings()`
      - 函数返回类型为 `UseSettingsReturn`
      - 函数导入自：`@/stores/wallpaper`
      - 函数导入自：`@/services`
      - 函数导入自：`@/composables`
      - TypeScript 编译无错误
    </acceptance_criteria>
  </task>

  <task id="2">
    <read_first>
      - src/composables/index.ts
      - src/composables/settings/useSettings.ts
    </read_first>
    <action>
      更新文件 `src/composables/index.ts`，添加 useSettings 导出：

      在文件末尾添加：
      ```typescript
      // Settings
      export { useSettings, type UseSettingsReturn } from './settings/useSettings'
      ```

      保留已有的 useAlert、useWallpaperList 和 useDownload 导出。
    </action>
    <acceptance_criteria>
      - 文件包含导出：`export { useSettings, type UseSettingsReturn }`
      - 文件保留已有的 useAlert、useWallpaperList 和 useDownload 导出
      - TypeScript 编译无错误
    </acceptance_criteria>
  </task>
</tasks>

<verification>
  ## 编译验证

  ```bash
  npm run typecheck || npx tsc --noEmit
  ```

  ## 导入验证

  ```bash
  grep -r "useSettings" src/composables/index.ts
  ```

  ## 功能验证（集成后）

  - load 正确调用 settingsService.get
  - update 正确调用 settingsService.update
  - reset 正确调用 settingsService.reset
  - 错误时自动显示提示
  - 重置成功时显示成功提示
</verification>

<success_criteria>
  <must_haves>
    - useSettings 可从 `@/composables` 导入
    - 返回类型化的状态和方法
    - 正确协调 Service 和 Store
    - 错误时自动显示提示
    - 成功重置时显示成功提示
    - TypeScript 编译无错误
  </must_haves>
</success_criteria>

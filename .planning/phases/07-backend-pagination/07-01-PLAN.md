---
phase: 07-backend-pagination
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/ipc.ts
  - electron/main/ipc/handlers/file.handler.ts
  - electron/preload/index.ts
autonomous: true
requirements:
  - PAG-01
must_haves:
  truths:
    - "file.handler.ts `read-directory` handler 可以接受 page/pageSize 参数"
    - "未传递分页参数时，默认使用 page=1, pageSize=50，不破坏现有调用"
    - "返回结果中包含 total 字段（符合条件的文件总数）"
    - "返回结果中包含 page 和 pageSize 字段（当前分页信息）"
    - "files 数组只返回当前页的子集，而非全部文件"
  artifacts:
    - path: src/types/ipc.ts
      provides: "ReadDirectoryParams 类型定义 + ReadDirectoryResponse 分页字段"
      contains: "interface ReadDirectoryParams"
    - path: electron/main/ipc/handlers/file.handler.ts
      provides: "支持 page/pageSize 参数的 read-directory handler"
      pattern: "page|pageSize|Array\\.slice|total"
    - path: electron/preload/index.ts
      provides: "更新后的 readDirectory 签名和 IPC 转发"
      pattern: "page.*pageSize.*ipcRenderer\\.invoke"
  key_links:
    - from: "electron/main/ipc/handlers/file.handler.ts"
      to: "src/types/ipc.ts::ReadDirectoryParams"
      via: "import"
      pattern: "ReadDirectoryParams"
    - from: "electron/main/ipc/handlers/file.handler.ts"
      to: "src/types/ipc.ts::ReadDirectoryResponse"
      via: "return type"
      pattern: "ReadDirectoryResponse"
    - from: "electron/preload/index.ts"
      to: "src/types/ipc.ts::ReadDirectoryResponse"
      via: "type reference in ElectronAPI"
      pattern: "ReadDirectoryResponse"
---

<objective>
为 `file.handler.ts` 中的 `read-directory` handler 添加分页支持，同时更新 IPC 类型定义和 preload 桥接。

Purpose: 避免一次性读取并处理目录中所有图片文件，改为按 page/pageSize 返回子集并附带总数，为后续前端分页 UI 提供数据基础。
Output:
1. `src/types/ipc.ts` — 新增 `ReadDirectoryParams` 类型，扩展 `ReadDirectoryResponse` 添加分页字段
2. `electron/main/ipc/handlers/file.handler.ts` — handler 接受 page/pageSize，实现 Array.slice 分页逻辑
3. `electron/preload/index.ts` — 更新签名，转发分页参数到 IPC
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- 当前 ReadDirectoryResponse 和 LocalFile 类型 -->
From src/types/ipc.ts:

```typescript
export interface LocalFile {
  name: string
  path: string
  thumbnailPath: string
  size: number
  modifiedAt: number
  width: number
  height: number
}

export interface ReadDirectoryResponse {
  error: string | null
  files: LocalFile[]
}
```

<!-- 参考: 收藏夹的分页参数 -->
From src/types/ipc.ts:

```typescript
export interface FavoritesGetPaginatedRequest {
  collectionId?: string
  limit: number
  offset: number
}
```

<!-- 当前 preload ElectronAPI.readDirectory 签名 -->
From electron/preload/index.ts:

```typescript
readDirectory: (dirPath: string) => Promise<{ error: string | null; files: any[] }>
```

<!-- 当前 preload 实现 -->
```typescript
readDirectory: (dirPath: string) => {
    console.log('[Preload] readDirectory called:', dirPath)
    return ipcRenderer.invoke(IPC_CHANNELS.READ_DIRECTORY, dirPath)
},
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 新增 ReadDirectoryParams 类型并扩展 ReadDirectoryResponse</name>

  <files>src/types/ipc.ts</files>

  <read_first>src/types/ipc.ts</read_first>

  <action>
    1. 在 `LocalFile` 接口（约第 105-113 行）之后、`ReadDirectoryResponse` 接口（约第 117-121 行）之前，新增 `ReadDirectoryParams` 接口：

    ```typescript
    /**
     * 读取目录请求参数
     */
    export interface ReadDirectoryParams {
      /** 页码，从 1 开始，默认 1 */
      page?: number
      /** 每页数量，默认 50 */
      pageSize?: number
    }
    ```

    2. 扩展 `ReadDirectoryResponse` 接口，新增三个必填字段：`total`, `page`, `pageSize`：

    ```typescript
    export interface ReadDirectoryResponse {
      error: string | null
      files: LocalFile[]
      /** 符合条件的文件总数 */
      total: number
      /** 当前页码 */
      page: number
      /** 每页数量 */
      pageSize: number
    }
    ```

    3. 在 `electron/preload/types.ts` 的 re-export 列表中（约第 11-37 行），添加 `ReadDirectoryParams` 到 type re-export 语句中（在 `ReadDirectoryResponse` 旁边或后面新增一行）。
  </action>

  <acceptance_criteria>
    - `grep -n "ReadDirectoryParams" src/types/ipc.ts` 返回包含 `interface` 定义的行
    - `grep -n "total:" src/types/ipc.ts | grep "number"` 返回 ReadDirectoryResponse 中的 total 字段行
    - `grep -n "page:" src/types/ipc.ts | grep "number"` 返回 ReadDirectoryResponse 中的 page 字段行
    - `grep -n "pageSize:" src/types/ipc.ts | grep "number"` 返回 ReadDirectoryResponse 中的 pageSize 字段行
    - `grep "ReadDirectoryParams" electron/preload/types.ts` 返回 re-export 行
  </acceptance_criteria>

  <verify>
    <automated>grep -c "interface ReadDirectoryParams" src/types/ipc.ts && grep -c "total:" src/types/ipc.ts && grep "ReadDirectoryParams" electron/preload/types.ts | grep -c "export type"</automated>
  </verify>

  <done>
    ReadDirectoryParams 接口定义存在；ReadDirectoryResponse 包含 total/page/pageSize 字段；preload/types.ts re-exports ReadDirectoryParams。
  </done>
</task>

<task type="auto">
  <name>Task 2: 在 file.handler.ts 中实现 read-directory 分页逻辑</name>

  <files>electron/main/ipc/handlers/file.handler.ts</files>

  <read_first>electron/main/ipc/handlers/file.handler.ts</read_first>

  <action>
    修改 `read-directory` handler（第 32-85 行）：

    1. 在 handler 签名中增加 `page` 和 `pageSize` 两个可选参数，跟在 `dirPath` 之后：

    原签名：
    ```typescript
    ipcMain.handle('read-directory', async (_event, dirPath: string) => {
    ```

    新签名：
    ```typescript
    ipcMain.handle('read-directory', async (_event, dirPath: string, page: number = 1, pageSize: number = 50) => {
    ```

    2. 在 `imageFiles` 筛选完成之后（第 42 行之后）、`Promise.all` 之前，计算总文件数量：

    ```typescript
    const total = imageFiles.length
    ```

    3. 使用 `Array.slice` 计算分页偏移并截取当前页的子集：

    ```typescript
    const startIndex = (page - 1) * pageSize
    const pagedImageFiles = imageFiles.slice(startIndex, startIndex + pageSize)
    ```

    4. 将 `imageFiles` 替换为 `pagedImageFiles` 传入 `Promise.all`：

    修改第 44 行：
    原：`const fileDetails = await Promise.all(imageFiles.map(...`
    新：`const fileDetails = await Promise.all(pagedImageFiles.map(...`

    5. 修改 return 语句（第 80 行），返回分页信息：

    原：`return { error: null, files: fileDetails }`
    新：`return { error: null, files: fileDetails, total, page, pageSize }`

    6. 修改错误返回（第 83 行），保持 `files: []` 并添加 0 值分页字段：

    原：`return { error: error.message, files: [] }`
    新：`return { error: error.message, files: [], total: 0, page: 1, pageSize: 50 }`

    7. 同样修改目录不存在时的返回（第 35 行）：

    原：`return { error: '目录不存在', files: [] }`
    新：`return { error: '目录不存在', files: [], total: 0, page: 1, pageSize: 50 }`
  </action>

  <acceptance_criteria>
    - `grep -c "page: number = 1" electron/main/ipc/handlers/file.handler.ts` 返回 1（page 参数默认值正确）
    - `grep -c "pageSize: number = 50" electron/main/ipc/handlers/file.handler.ts` 返回 1（pageSize 参数默认值正确）
    - `grep -c "Array.prototype.slice\|imageFiles.slice" electron/main/ipc/handlers/file.handler.ts` 返回至少 1（使用 slice 分页）
    - `grep -c "const total = imageFiles.length" electron/main/ipc/handlers/file.handler.ts` 返回 1（计算总数）
    - `grep -c "total, page, pageSize" electron/main/ipc/handlers/file.handler.ts` 返回至少 1（返回值包含分页字段）
  </acceptance_criteria>

  <verify>
    <automated>grep -c "page: number = 1" electron/main/ipc/handlers/file.handler.ts && grep -c "pageSize: number = 50" electron/main/ipc/handlers/file.handler.ts && grep -c "\.slice(" electron/main/ipc/handlers/file.handler.ts && grep -c "total, page, pageSize" electron/main/ipc/handlers/file.handler.ts</automated>
  </verify>

  <done>
    read-directory handler 支持 page/pageSize 默认参数；使用 Array.slice 截取分页子集；返回值包含 total/page/pageSize 字段。
  </done>
</task>

<task type="auto">
  <name>Task 3: 更新 preload readDirectory 签名并转发分页参数</name>

  <files>electron/preload/index.ts</files>

  <read_first>electron/preload/index.ts</read_first>

  <action>
    1. 更新 `ElectronAPI` 接口中的 `readDirectory` 签名（第 14 行）：

    原：
    ```typescript
    readDirectory: (dirPath: string) => Promise<{ error: string | null; files: any[] }>
    ```

    新：
    ```typescript
    readDirectory: (dirPath: string, page?: number, pageSize?: number) => Promise<{ error: string | null; files: any[]; total: number; page: number; pageSize: number }>
    ```

    使用内联类型（非 import 类型引用），保持与现有代码风格一致（其他 `ElectronAPI` 方法也使用内联类型）。

    2. 更新 `electronAPI` 实现中的 `readDirectory`（第 134-137 行）：

    原：
    ```typescript
    readDirectory: (dirPath: string) => {
      console.log('[Preload] readDirectory called:', dirPath)
      return ipcRenderer.invoke(IPC_CHANNELS.READ_DIRECTORY, dirPath)
    },
    ```

    新：
    ```typescript
    readDirectory: (dirPath: string, page?: number, pageSize?: number) => {
      console.log('[Preload] readDirectory called:', dirPath, 'page:', page, 'pageSize:', pageSize)
      return ipcRenderer.invoke(IPC_CHANNELS.READ_DIRECTORY, dirPath, page, pageSize)
    },
    ```
  </action>

  <acceptance_criteria>
    - `grep "readDirectory:" electron/preload/index.ts | grep -c "page?.*number.*pageSize?.*number"` 返回 1（接口签名包含可选分页参数）
    - `grep "ipcRenderer.invoke(IPC_CHANNELS.READ_DIRECTORY" electron/preload/index.ts | grep -c "page, pageSize"` 返回 1（实现转发 page/pageSize）
  </acceptance_criteria>

  <verify>
    <automated>grep "readDirectory:" electron/preload/index.ts | grep -E "page\?.*number.*pageSize\?.*number" && grep "IPC_CHANNELS.READ_DIRECTORY" electron/preload/index.ts | grep "page, pageSize"</automated>
  </verify>

  <done>
    ElectronAPI.readDirectory 签名支持 page/pageSize 可选参数；实现将分页参数转发到 ipcRenderer.invoke。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer -> main | 渲染进程通过 IPC 传递 page/pageSize 参数到主进程 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | I (Information Disclosure) | file.handler.ts page/pageSize | accept | page/pageSize 仅为数字索引参数，不包含敏感信息。攻击者可通过构造参数探测目录文件数量，但文件列表本身已可通过正常功能获取，风险可接受。 |
| T-07-02 | S (Spoofing) | preload readDirectory | mitigate | 参数通过 contextBridge 暴露的白名单通道传递，preload/index.ts 中已有 VALID_INVOKE_CHANNELS 白名单机制确保只能调用已注册的 IPC 通道。 |
</threat_model>

<verification>
1. 类型定义验证：`npx tsc --noEmit --strict src/types/ipc.ts` 无类型错误
2. 构建验证：`npx electron-vite build` 或项目构建命令，确认三个文件修改后编译通过
3. 兼容性验证：现有调用 `readDirectory(dirPath)` 不传 page/pageSize 时，默认 page=1, pageSize=50，应返回第一页 50 条结果 + total 字段
</verification>

<success_criteria>
- `ReadDirectoryParams` 类型定义在 `src/types/ipc.ts` 中
- `ReadDirectoryResponse` 包含 `total: number`, `page: number`, `pageSize: number` 字段
- `file.handler.ts` 中 `read-directory` handler 签名接受 `page: number = 1, pageSize: number = 50`
- handler 使用 `Array.slice` 实现分页，计算 `total` 返回
- preload 的 `readDirectory` 签名和实现均支持 page/pageSize 可选参数并转发到 IPC
- 所有三个文件的修改编译通过，无类型错误
</success_criteria>

<output>
After completion, create `.planning/phases/07-backend-pagination/07-01-SUMMARY.md`
</output>

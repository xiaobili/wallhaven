---
phase: 5
plan: 04
type: verify
wave: 4
depends_on: ["05-01", "05-02", "05-03"]
files_modified: []
autonomous: true
requirements:
  - ARCH-01
  - ARCH-02
---

# Plan 05-04: 架构优化验证

<objective>
验证架构优化的正确性：Service 层无状态、状态在 Store 统一管理、架构边界清晰。
</objective>

<tasks>

## Task 1: TypeScript 类型检查

<read_first>
- tsconfig.json
</read_first>

<action>
运行完整的 TypeScript 类型检查：
```bash
npm run type-check
```

确保无类型错误。
</action>

<acceptance_criteria>
- `npm run type-check` 命令退出码为 0
- 无 TypeScript 错误
</acceptance_criteria>

<verify>
```bash
npm run type-check
echo "Exit code: $?"
```
</verify>

---

## Task 2: 验证 Service 层无缓存属性

<read_first>
- src/services/favorites.service.ts
- src/services/collections.service.ts
- src/services/wallpaper.service.ts
- src/services/settings.service.ts
- src/services/download.service.ts
</read_first>

<action>
验证所有 Service 文件不包含缓存属性：

```bash
# 检查是否还有缓存属性
grep -rn "private cached" src/services/ || echo "✓ 无缓存属性"
grep -rn "private cache" src/services/ || echo "✓ 无 cache 属性"
grep -rn "favoriteStatusCache" src/services/ || echo "✓ 无 favoriteStatusCache"
```
</action>

<acceptance_criteria>
- 所有 Service 文件无缓存相关属性
- grep 搜索结果为空
</acceptance_criteria>

<verify>
```bash
grep -rn "private cached" src/services/ && echo "FAIL" || echo "PASS"
grep -rn "private cache" src/services/ | grep -v "// " && echo "FAIL" || echo "PASS"
```
</verify>

---

## Task 3: 验证 DownloadService 不直接使用 Client

<read_first>
- src/services/download.service.ts
</read_first>

<action>
验证 DownloadService 不导入 electronClient：

```bash
grep "import.*electronClient" src/services/download.service.ts && echo "FAIL: 仍在使用 electronClient" || echo "PASS: 已移除 electronClient"
```
</action>

<acceptance_criteria>
- DownloadService 不导入 electronClient
- 所有 IPC 调用通过 Repository
</acceptance_criteria>

<verify>
```bash
grep "electronClient" src/services/download.service.ts && echo "FAIL" || echo "PASS"
```
</verify>

---

## Task 4: 功能测试 - 收藏功能

<read_first>
- src/stores/modules/favorites/index.ts
- src/composables/favorites/useFavorites.ts
</read_first>

<action>
手动测试收藏功能：

1. 启动应用
2. 浏览壁纸列表
3. 点击收藏按钮
4. 验证收藏状态显示正确
5. 刷新页面
6. 验证收藏状态保持
7. 切换收藏夹
8. 验证收藏项列表正确
</action>

<acceptance_criteria>
- 添加收藏成功
- 移除收藏成功
- 收藏状态缓存生效
- 刷新后状态保持
</acceptance_criteria>

<verify>
手动验证，记录测试结果
</verify>

---

## Task 5: 功能测试 - 壁纸搜索

<read_first>
- src/stores/modules/wallpaper/index.ts
- src/services/wallpaper.service.ts
</read_first>

<action>
手动测试壁纸搜索功能：

1. 启动应用
2. 搜索壁纸
3. 验证搜索结果显示正确
4. 翻页查看更多结果
5. 刷新页面
6. 验证搜索结果缓存生效
7. 验证收藏状态显示正确
</action>

<acceptance_criteria>
- 搜索功能正常
- 缓存生效（刷新后快速加载）
- 收藏状态显示正确
</acceptance_criteria>

<verify>
手动验证，记录测试结果
</verify>

---

## Task 6: 功能测试 - 下载功能

<read_first>
- src/repositories/download-task.repository.ts
- src/services/download.service.ts
</read_first>

<action>
手动测试下载功能：

1. 启动应用
2. 下载壁纸
3. 暂停下载
4. 恢复下载
5. 取消下载
6. 验证下载记录正确
</action>

<acceptance_criteria>
- 下载功能正常
- 暂停/恢复/取消正常
- 下载记录正确
</acceptance_criteria>

<verify>
手动验证，记录测试结果
</verify>

---

## Task 7: 更新文档

<read_first>
- .planning/codebase/CONCERNS.md
- .planning/STATE.md
</read_first>

<action>
1. 更新 `.planning/codebase/CONCERNS.md`：
   - 标记 MEDIUM-06（服务层职责边界模糊）为已解决
   - 标记 MEDIUM-07（状态管理分散）为已解决

2. 更新 `.planning/STATE.md`：
   - 更新服务层职责状态为"清晰定义 ✅"
   - 更新状态管理状态为"统一到 Store ✅"
</action>

<acceptance_criteria>
- CONCERNS.md 标记已解决
- STATE.md 更新完成
</acceptance_criteria>

<verify>
```bash
grep "MEDIUM-06.*✅" .planning/codebase/CONCERNS.md
grep "MEDIUM-07.*✅" .planning/codebase/CONCERNS.md
```
</verify>

</tasks>

<verification>
所有任务通过即验证完成。
</verification>

<success_criteria>
- TypeScript 编译通过
- 所有 Service 无缓存属性
- DownloadService 通过 Repository 访问 IPC
- 收藏、搜索、下载功能正常
- 文档更新完成
</success_criteria>

<must_haves>
- 功能行为不变
- 架构边界清晰
- 代码质量提升
</must_haves>

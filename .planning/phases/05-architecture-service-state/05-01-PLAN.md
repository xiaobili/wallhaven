---
phase: 5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/stores/modules/favorites/index.ts
  - src/services/favorites.service.ts
  - src/services/collections.service.ts
  - src/composables/favorites/useFavorites.ts
autonomous: true
requirements:
  - ARCH-02
---

# Plan 05-01: 迁移收藏功能缓存到 Store

<objective>
将 FavoritesService 和 CollectionsService 的缓存迁移到 useFavoritesStore，实现收藏相关状态的统一管理。
</objective>

<tasks>

## Task 1: 扩展 useFavoritesStore 缓存能力

<read_first>
- src/stores/modules/favorites/index.ts
- src/services/favorites.service.ts
- src/services/collections.service.ts
</read_first>

<action>
在 `src/stores/modules/favorites/index.ts` 中添加以下内容：

1. 添加 `favoriteStatusCache` 响应式 Map：
```typescript
/** 收藏状态缓存 (wallpaperId -> 0|1|2) */
const favoriteStatusCache = ref<Map<string, 0 | 1 | 2>>(new Map())
```

2. 添加缓存操作方法：
```typescript
function getFavoriteStatus(wallpaperId: string): 0 | 1 | 2 | undefined {
  return favoriteStatusCache.value.get(wallpaperId)
}

function setFavoriteStatus(wallpaperId: string, status: 0 | 1 | 2): void {
  favoriteStatusCache.value.set(wallpaperId, status)
}

function clearFavoriteStatusCache(): void {
  favoriteStatusCache.value.clear()
}

async function loadFavoriteStatusMap(ids: string[]): Promise<void> {
  if (ids.length === 0) return
  const result = await favoritesRepository.getFavoriteStatusMap(ids)
  if (result.success && result.data) {
    for (const [id, status] of Object.entries(result.data)) {
      favoriteStatusCache.value.set(id, status)
    }
  }
}
```

3. 在 return 语句中暴露新方法：
```typescript
return {
  // ...existing
  favoriteStatusCache,
  getFavoriteStatus,
  setFavoriteStatus,
  clearFavoriteStatusCache,
  loadFavoriteStatusMap,
}
```

4. 更新 `clearCache` 方法以清除 favoriteStatusCache：
```typescript
function clearCache(): void {
  favorites.value = []
  collections.value = []
  favoriteStatusCache.value.clear()
}
```
</action>

<acceptance_criteria>
- `src/stores/modules/favorites/index.ts` 包含 `favoriteStatusCache` ref
- Store 包含 `getFavoriteStatus`、`setFavoriteStatus`、`clearFavoriteStatusCache`、`loadFavoriteStatusMap` 方法
- `clearCache` 方法清除 `favoriteStatusCache`
- TypeScript 编译通过
</acceptance_criteria>

<verify>
```bash
npm run type-check
grep -n "favoriteStatusCache" src/stores/modules/favorites/index.ts
grep -n "getFavoriteStatus" src/stores/modules/favorites/index.ts
```
</verify>

---

## Task 2: 移除 FavoritesService 缓存

<read_first>
- src/services/favorites.service.ts
- src/stores/modules/favorites/index.ts
</read_first>

<action>
修改 `src/services/favorites.service.ts`：

1. 删除缓存属性：
```typescript
// 删除这两行
private cachedFavorites: FavoriteItem[] | null = null
private cachedData: FavoritesData | null = null
```

2. 修改 `getAll` 方法，直接调用 Repository：
```typescript
async getAll(): Promise<IpcResponse<FavoriteItem[]>> {
  return favoritesRepository.getFavorites()
}
```

3. 删除 `clearCache` 方法：
```typescript
// 删除整个 clearCache 方法
```

4. 更新 `add`、`remove`、`move` 方法，移除 `this.clearCache()` 调用：
```typescript
async add(...): Promise<IpcResponse<FavoriteItem>> {
  const item: FavoriteItem = {...}
  return favoritesRepository.addFavorite(item)
}

async remove(wallpaperId: string, collectionId: string): Promise<IpcResponse<void>> {
  return favoritesRepository.removeFavorite(wallpaperId, collectionId)
}

async move(...): Promise<IpcResponse<FavoriteItem>> {
  return favoritesRepository.moveFavorite(wallpaperId, fromCollectionId, toCollectionId)
}
```
</action>

<acceptance_criteria>
- `FavoritesServiceImpl` 类无 `cachedFavorites`、`cachedData` 属性
- `FavoritesServiceImpl` 无 `clearCache` 方法
- 所有方法直接返回 Repository 调用结果
- TypeScript 编译通过
</acceptance_criteria>

<verify>
```bash
npm run type-check
grep -c "cachedFavorites" src/services/favorites.service.ts || echo "已删除"
grep -c "clearCache" src/services/favorites.service.ts || echo "已删除"
```
</verify>

---

## Task 3: 移除 CollectionsService 缓存

<read_first>
- src/services/collections.service.ts
- src/services/favorites.service.ts
</read_first>

<action>
修改 `src/services/collections.service.ts`：

1. 删除缓存属性：
```typescript
// 删除这两行
private cachedCollections: Collection[] | null = null
private cachedData: FavoritesData | null = null
```

2. 修改 `getAll` 方法，直接调用 Repository：
```typescript
async getAll(): Promise<IpcResponse<Collection[]>> {
  return favoritesRepository.getCollections()
}
```

3. 更新 `getById` 和 `getDefault` 方法使用 Repository：
```typescript
async getById(id: string): Promise<IpcResponse<Collection | null>> {
  const result = await favoritesRepository.getCollections()
  if (!result.success) {
    return { success: false, data: null, error: result.error }
  }
  const collection = result.data?.find((c) => c.id === id) || null
  return { success: true, data: collection }
}

async getDefault(): Promise<IpcResponse<Collection | null>> {
  const result = await favoritesRepository.getCollections()
  if (!result.success) {
    return { success: false, data: null, error: result.error }
  }
  const defaultCollection = result.data?.find((c) => c.isDefault) || null
  return { success: true, data: defaultCollection }
}
```

4. 更新 `create`、`rename`、`delete`、`setDefault` 方法：
```typescript
async create(name: string): Promise<IpcResponse<Collection>> {
  return favoritesRepository.createCollection(name)
}

async rename(id: string, name: string): Promise<IpcResponse<Collection>> {
  return favoritesRepository.renameCollection(id, name)
}

async delete(id: string): Promise<IpcResponse<void>> {
  return favoritesRepository.deleteCollection(id)
}

async setDefault(id: string): Promise<IpcResponse<Collection>> {
  return favoritesRepository.setDefaultCollection(id)
}
```

5. 删除 `clearCache` 方法和 `favoritesService.clearCache()` 导入/调用
</action>

<acceptance_criteria>
- `CollectionsServiceImpl` 类无缓存属性
- `CollectionsServiceImpl` 无 `clearCache` 方法
- 不导入或调用 `favoritesService.clearCache()`
- TypeScript 编译通过
</acceptance_criteria>

<verify>
```bash
npm run type-check
grep -c "cachedCollections" src/services/collections.service.ts || echo "已删除"
grep -c "clearCache" src/services/collections.service.ts || echo "已删除"
```
</verify>

---

## Task 4: 更新 useFavorites composable

<read_first>
- src/composables/favorites/useFavorites.ts
- src/stores/modules/favorites/index.ts
</read_first>

<action>
修改 `src/composables/favorites/useFavorites.ts`：

1. 更新 `toggleFavorite` 方法以更新 Store 缓存：
```typescript
async function toggleFavorite(wallpaper: WallpaperItem, collectionId?: string) {
  const store = useFavoritesStore()
  // ...existing logic
  
  if (result.success) {
    // 更新 Store 状态
    await store.loadFavorites()
    // 更新收藏状态缓存
    store.setFavoriteStatus(wallpaper.id, collectionId ? 1 : 0)
  }
  
  return result
}
```

2. 确保所有状态操作通过 Store 进行
</action>

<acceptance_criteria>
- `useFavorites` 通过 Store 更新收藏状态缓存
- TypeScript 编译通过
</acceptance_criteria>

<verify>
```bash
npm run type-check
```
</verify>

</tasks>

<verification>
1. 运行 TypeScript 类型检查：`npm run type-check`
2. 手动测试收藏功能：
   - 添加/移除收藏
   - 切换收藏夹
   - 刷新页面验证状态保持
</verification>

<success_criteria>
- useFavoritesStore 包含 favoriteStatusCache
- FavoritesService 和 CollectionsService 无缓存属性
- 收藏功能正常工作
- TypeScript 编译无错误
</success_criteria>

<must_haves>
- Store 统一管理收藏状态
- Service 层无状态
- 功能行为不变
</must_haves>

---
wave: 1
depends_on: []
files_modified:
  - src/Main.vue
autonomous: true
requirements_addressed:
  - BROW-01
---

# PLAN 02: Add Navigation Entry

## Objective

Add "我的收藏" navigation entry to the left sidebar menu in Main.vue, positioned between "下载中心" and "设置".

## Tasks

### Task 1: Add Favorites Navigation Item

<read_first>
- src/Main.vue
</read_first>

<action>
1. Open `src/Main.vue`
2. In the template, locate the menu item for "下载中心" (around line 32-40)
3. Add a new menu item immediately after the "下载中心" item, before the "更多" label:

```vue
<li class="menu-item">
  <router-link
    to="/favorites"
    class="menu-native"
  >
    <i class="fas fa-heart" />
    我的收藏<span class="li-border" />
  </router-link>
</li>
```

4. The new item should be inserted between the "下载中心" item and the "更多" label section
</action>

<acceptance_criteria>
- `grep -n "to=\"/favorites\"" src/Main.vue` returns a match
- `grep -n "fas fa-heart" src/Main.vue` returns a match
- `grep -n "我的收藏" src/Main.vue` returns a match
- The favorites menu item is positioned in the "我的壁纸" section (before "更多")
</acceptance_criteria>

## Verification

1. Navigation item follows existing pattern (menu-item, router-link, icon, text, li-border)
2. Router link points to `/favorites`
3. Icon is `fas fa-heart`
4. Item is visually consistent with other menu items

## Must Haves

- Navigation uses `router-link` with `to="/favorites"`
- Icon is `fas fa-heart`
- Text is "我的收藏"
- Located in the "我的壁纸" menu section, after "下载中心"

/**
 * 领域类型统一导出
 * 阶段 1：创建空导出文件，后续阶段逐步迁移类型
 *
 * 计划迁移的类型：
 * - WallpaperItem
 * - WallpaperMeta
 * - PageData
 * - TotalPageData
 * - DownloadItem
 * - DownloadState
 * - AppSettings
 * - WallpaperFit
 */

// 阶段 1 仅创建目录结构，暂不迁移类型
// 后续阶段将逐步从 src/types/index.ts 迁移类型定义

export {}
